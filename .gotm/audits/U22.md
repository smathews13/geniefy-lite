# Audit report — content-claim / honesty audit — 2026-06-12

## Target audited
File: `README.md`
Unit: **U22** — "Close the U16-audit MED/LOW: reword the `geniefy_setup` job as not-yet-wired; surface the D30 database name"

Oracle:
- `.gotm/audits/U16.md` (the MED: README implied `geniefy_setup` was wired when it wasn't — honesty fix; the LOW: surface the D30 `geniefy` database name)
- `.gotm/DECISIONS.md` D30 (the working-store database name)
- `databricks.yml` (is `geniefy_setup` actually wired yet? It should **not** be — U62 wires it)

Independence: this auditor did **not** author U22 (U22's first dedicated audit, per D41). Mechanical only — no fixes, no ledger/source edits.

---

## What was checked

1. **honesty fix landed** — README describes `geniefy_setup` as *planned / not yet wired* (not as an existing capability), cross-checked against `databricks.yml` to confirm the job is indeed absent (so the README is now accurate).
2. **D30 surfaced** — README names the `geniefy` working-store database (D30).
3. **no contradiction** — README doesn't contradict other locked decisions or overstate current state.

---

## Findings

### HIGH severity
- None.

### MEDIUM severity
- None.

### LOW severity
- **`<repo>` placeholder in the clone command** (`README.md:30`: `git clone <repo> && cd geniefy-v3`). The repo URL is still a literal placeholder, so a customer copy-pasting the block hits a non-runnable first line. This is **pre-existing** (carried from the U16 audit's LOW list, not introduced by U22) and **out of U22's stated scope** (U22 covers only the `geniefy_setup` wording + D30 database name). Cosmetic; resolves when the public GitHub URL is known. Noted, not charged against U22.

### UNVERIFIED
- None. All three oracle files (`.gotm/audits/U16.md`, `.gotm/DECISIONS.md`, `databricks.yml`) were present and readable; all three checks were fully verifiable on disk.

---

## Detailed verification log

**Check 1 — honesty fix landed → PASS.**
The README's Database-setup section now reads (`README.md:43`):
> *"...(A `geniefy_setup` bundle job to run migrations automatically on deploy is **planned but not yet wired** into `databricks.yml`; for now use the runbook above.)"*

This is the exact honesty fix the U16 audit's MEDIUM requested. The prior present-tense "is wired in the deployment unit" (the U16 MED defect) is gone; the phrase "**planned but not yet wired**" is unambiguous and the parenthetical points the customer to the runbook for the operative path.

Cross-check against the bundle: `grep -ni 'geniefy_setup|jobs:|job' databricks.yml` returns **no matches** (exit 1). `databricks.yml` defines only `bundle`, `sync.exclude`, `variables`, `targets` (dev/prod), and `resources.apps.geniefy` — **no `jobs:` section and no `geniefy_setup` job**. So the README's "not yet wired into `databricks.yml`" claim is **accurate** against the current bundle. (Consistent with the U22 brief: U62 wires the job later; it should not be present now, and it isn't.)

The operative DB-setup instruction remains correct and self-contained: apply `migrations/001_init.sql` per the validated `migrations/APPLY.md` runbook (linked) — a customer following the README gets a working DB the documented way, with no expectation of automatic migration-on-deploy.

**Check 2 — D30 surfaced → PASS.**
The README names the working-store database (`README.md:43`):
> *"geniefy-v3 applies its schema into a dedicated **`geniefy`** Postgres database (schema `geniefy`) on the Lakebase instance."*

This is a faithful surfacing of D30, which records the app's working store as "a dedicated Postgres database named **`geniefy`** ... with the app objects in **schema `geniefy`** inside it" (`DECISIONS.md:424`). Both the **database name** (`geniefy`) and the **schema** (`geniefy`) are now stated in the README's Database-setup section — closing the U16 audit's LOW ("the Prerequisites/DB-setup section doesn't mention the dedicated `geniefy` database convention (D30)"). The "namespace-redundant but harmless" database/schema name overlap noted in D30 is reflected accurately (the README states both without implying they're the same namespace).

D30's `Consequences` note — that the DAB does not yet parameterize the **database name** and a `database` bundle variable (default `geniefy`) should be added "when the App's resource binding is wired" — is honored by omission: the README's Overridable-variables list (`README.md:39`) correctly does **not** list a `database` variable, matching `databricks.yml` (which has no such variable). No over-claim.

**Check 3 — no contradiction → PASS.**
- The "not yet wired" framing is consistent with **D35** (`DECISIONS.md:482`): DAB "stays the spine (app, `geniefy_setup` job, ...)" — i.e., the job is part of the *planned* DAB spine, designed but not yet built; the README's "planned but not yet wired" aligns, and D35 itself says it "Replaces the manual migration step (U16/U22) once the deploy unit lands" (future tense — the deploy unit has not landed). No contradiction.
- The Overridable-variables list (`README.md:39`) — `host`, `catalog`, `schema`, `warehouse_id`, `model_endpoint`, `lakebase_instance`, `demo_catalog` — is still an exact set-match to `databricks.yml:17-38` (seven variables). U22's edit did not perturb this.
- The D13 / Apache-2.0 license note, the D12-parameterization claim, and the D25 `sync.exclude` contributor note remain accurate against the bundle (unchanged by U22, re-confirmed not contradicted).
- The README does not overstate current state anywhere in the audited region: the only forward-looking capability (`geniefy_setup`) is now explicitly flagged as planned.

---

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 1 · UNVERIFIED: 0
Verdict: **PASS-FINDINGS**   (HIGH ⇒ FAIL; MEDIUM/LOW-only ⇒ PASS-FINDINGS; clean ⇒ PASS)

Both U22 targets are **closed**: the honesty fix landed (the `geniefy_setup` job is now described as "planned but not yet wired," and the bundle confirms it is indeed absent — README is accurate), and the D30 `geniefy` database name is surfaced (database + schema both named in the Database-setup section). No contradictions with locked decisions (D30, D35, D12, D13, D25) and no overstatement of current state. The single LOW (`<repo>` placeholder) is pre-existing and outside U22's scope — noted only, not charged against this unit. Were that LOW excluded as out-of-scope, U22 is effectively clean; the verdict is recorded as PASS-FINDINGS to keep the carried-over cosmetic finding visible.
