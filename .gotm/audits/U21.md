# Audit report — test-suite (U18-audit gap closure: coverage + run) — 2026-06-12

## Target audited
File: `/Users/rohit.dashora/fe-vibe/geniefy-v3/tests/test_profiler.py`
Unit: U21 — "Close the U18-audit MED/LOW: test the min-version floor branch (+ ProfileOptions request fields, percent bounds)"
(U21 owns the current test file; lineage U18 → U21.)

## Oracle
- `.gotm/audits/U18.md` — the prior audit's MED/LOW findings being closed.
- `src/geniefy_core/profiler.py` — the module under test.

## Method
1. **Gap mapping** — read the U18 audit's findings, then located the new tests in `test_profiler.py` that address each.
2. **Run** — `cd /Users/rohit.dashora/fe-vibe/geniefy-v3 && PYTHONPATH=src /tmp/geniefy_pgvenv/bin/python -m pytest tests/test_profiler.py -q` → **40 passed in 0.03s.** No skips/xfail/warnings. Hermetic (injected fakes; no infra).
3. **Mutation probes** — copied src+tests to a scratch dir and broke each load-bearing line the new tests defend. A meaningful test must turn red:
   - **MUT-A** neuter the floor branch (`if v < floor:` → `if False:`) → **1 failed** — `test_min_version_floor_rejects_older` (caught). The MEDIUM-finding branch is now live to the suite (was MUT7 / 0-caught in U18).
   - **MUT-B** drop `reuse_analyze_stats` from `ProfileOptions.to_dict` → **1 failed** — `test_profile_options_all_fields_in_request` (caught).
   - **MUT-C** break the percent upper bound (`<= 100` → `< 100`) → **1 failed** — `test_samplespec_percent_boundaries_ok[100]` (caught).
   - **MUT-D** break the constraints `key_column_usage` join → **1 failed** — `test_information_schema_constraints_query_shape` (caught).
   All reverted after probing; scratch dir removed.

## U18-audit findings → closure status

| U18 finding (severity) | New U21 test(s) | Mutation-confirmed |
|---|---|---|
| **MED** — min-version floor (`v < floor`) unexercised; `min_schema_version` never passed to ctor (`profiler.py:614-618`) | `test_min_version_floor_rejects_older` (tests:269-274, passes `min_schema_version="1.5"` vs provider `"1.0"`, asserts `code=="unsupported"`); `test_min_version_floor_accepts_at_or_above` (tests:277-279) covers the at/above side | **Yes** (MUT-A) |
| **LOW** — `constraints` query body unchecked (`profiler.py:407-417`) | `test_information_schema_constraints_query_shape` (tests:301-306) asserts `table_constraints`/`key_column_usage` present, catalog qualified, schema+table literals escaped | **Yes** (MUT-D) |
| **LOW** — `ProfileOptions` non-default fields not asserted (`include_samples`, `max_cardinality_for_topk`, `reuse_analyze_stats`; `profiler.py:137-143`) | `test_profile_options_all_fields_in_request` (tests:285-293) asserts the full 4-key `options` dict with non-default values | **Yes** (MUT-B) |
| **LOW** — `SampleSpec("percent")` 0/100 boundaries untested (`profiler.py:118`) | `test_samplespec_percent_boundaries_ok` (tests:296-298, parametrized `[0, 100]`) | **Yes** (MUT-C) |

All four U18-audit gaps (1 MED + 3 LOW) are now covered with meaningful, non-tautological assertions, each confirmed by a targeted mutation that turns the suite red.

Note: the U18 audit's MED finding self-noted it was a *test*-coverage gap, not a module defect (with default `min_schema_version="1.0"` the floor is unreachable). The fix correctly raises the floor in-test via the `min_schema_version` ctor arg (`profiler.py:462,469,606,614`) — exercising the real branch without mutating the module. Confirmed faithful to the U3 §4.2 version-gate contract.

## Findings

### HIGH severity
- (none)

### MEDIUM severity
- (none)

### LOW severity
- (none) — the U18 LOW findings are closed; no new LOW surfaced. The suite remains substantive (asserts parsed/merged output and the module's own `ProfileError.code`, not the fakes' literals) and fully hermetic.

### UNVERIFIED
- (none) — ran clean in the supplied venv; every new test was read and mutation-probed.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 0 · UNVERIFIED: 0
pytest: **40 passed, 0 failed** (`PYTHONPATH=src python -m pytest tests/test_profiler.py -q`)
gaps-now-covered: **yes** (1 MED + 3 LOW from U18 audit all covered + mutation-confirmed)
Verdict: **PASS**   (HIGH ⇒ FAIL; MEDIUM/LOW-only ⇒ PASS-FINDINGS; clean ⇒ PASS)
