# Audit report — content-claim + live-verification — 2026-06-23

## Target audited
File: `src/geniefy_app/api.py` (`SessionService.enumerate_tables`) + `tests/test_api.py` (enumerate tests)
Unit: U138

Claim under audit: `enumerate_tables` used `... NOT LIKE '\_\_%' ESCAPE '\'` to exclude internal `__%`
tables — INVALID Databricks SQL (a lone backslash in `'\'` escapes the closing quote →
`PARSE_SYNTAX_ERROR`) → enumeration always returned 0 tables → the hands-off schema-run Job produced
nothing. Fix: replace with `AND left(table_name, 2) <> '__'`, preserving the `include_internal`
opt-in; test updated.

## Findings

### HIGH severity
- None.

### MEDIUM severity
- None.

### LOW severity
- None.

### UNVERIFIED
- None.

## Detail by check

1. **existence** — PASS. `src/geniefy_app/api.py:269` now emits `sql += " AND left(table_name, 2) <> '__'"`.
   The broken `NOT LIKE '\_\_%' ESCAPE '\'` clause no longer appears in any live code path. A repo grep
   for `ESCAPE` / `NOT LIKE '\_\_%'` returns only (a) an explanatory comment at `api.py:265-267`,
   (b) the test assertion at `tests/test_api.py:511` that asserts `"ESCAPE" not in` the SQL, and
   (c) a fake-error string in `tests/test_providers.py:175` (U139's swallowed-failure test) — none of
   which compose the clause into a real query.

2. **spec match** — PASS. Opt-in preserved: the `left(...)` filter is gated on
   `if not filters.get("include_internal", False):` (`api.py:268`). `skip_documented` (Python-side,
   default True, `api.py:275,281`), `name_like` (SQL `LIKE` literal, `api.py:270-272`), and
   `max_tables` (slice cap, `api.py:284-286`) are all unchanged. The `table_type IN ('MANAGED','EXTERNAL')`
   filter is intact (`api.py:263`). Idents/literals stay quoted via `_q_ident`/`_q_lit`.

3. **cross-reference integrity** — PASS. The LEDGER U128 row (line 163) annotates
   `enumerate_tables superseded by U138`, and the U138 row (line 173) marks api.py "supersedes U128"
   and test "supersedes U124". The 2026-06-23 recovery-log entries (lines 227-228) accurately describe
   the parse-error root cause, the `left()` choice, the `include_internal` opt-in, and the live
   re-trigger result. U124 (line 158, the unit that introduced the now-superseded ESCAPE clause) and
   U110 (line 146) exist and read as cited.

4. **internal consistency** — PASS. `PYTHONPATH=src .venv/bin/python -m pytest tests/test_api.py -q`
   → **39 passed**. Full suite → **376 passed, 1 skipped**. The updated test
   `test_enumerate_tables_excludes_internal_by_default` (`tests/test_api.py:503-514`) asserts both the
   presence of `"left(table_name, 2) <> '__'"` and the absence of `"ESCAPE"`, and that
   `include_internal=True` drops the filter.

5. **decision fidelity** — PASS (n/a specific decision). The change is a SQL-predicate swap inside
   enumeration; the hands-off no-apply behavior (`run_hands_off` never writes UC) is untouched.

6. **LIVE verification** — PASS. On warehouse `0336d1a2b47936b4` (profile `fe-vm-classic`):
   - **Corrected query** `SELECT table_name FROM \`agent_spark\`.information_schema.tables WHERE
     table_schema='genify_1' AND table_type IN ('MANAGED','EXTERNAL') AND left(table_name, 2) <> '__'`
     → SUCCEEDED, returned **9 real tables** (ad_clicks, ad_impressions, conversions,
     click_conversion_features, click_conversion_predictions, + 4 `event_log_*`) with **no
     `__materialization_*` names**.
   - **Old form** `... NOT LIKE '\_\_%' ESCAPE '\'` → state FAILED with
     **`[PARSE_SYNTAX_ERROR] Syntax error at or near 'ESCAPE'. SQLSTATE: 42601`** — confirming the bug
     was real, exactly as claimed.
   - **Job `388957213112033`** recent runs: verify run `898135675774489` TERMINATED/SUCCESS; a newer
     run `1079085545058079` RUNNING.
   - **Production Lakebase** (`geniefy` db): `geniefy.schema_runs` shows verify run
     `eea228c0-a213-4246-b4ca-4fa916bea8c8` `completed` with `total_tables=1`, while the three prior
     runs (`ee8986ad`, `8bbdfd4b`, `eff5f2ef`) all show `total_tables=0` — matching the "was 0 → now 1"
     claim; the newest run `dfe4258d` has `total_tables=3`. The verify run's session
     `a1ad02fb-1eea-4b5b-9d5e-358fcc658265` has **1 table_draft + 24 column_drafts**, and the
     table_draft `proposed_comment` is the real prose "This table is a feature store for ad
     click-to-conversion analysis, joining and enriching records from three upstream sources —
     ad_clicks, ad_impressions, and conversions…" — matching the ledger verbatim.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 0 · UNVERIFIED: 0
Verdict: PASS
