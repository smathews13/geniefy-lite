# Audit report — harness-soundness + evidence-claim (verification unit) — 2026-06-13

## Target audited
Files:
- `scripts/local_ui_preview.py` — local preview harness
- `docs/UI_DEVLOOP_VERIFY.md` — evidence log
- `docs/assets/u92-review-profileviz.png` — screenshot
Unit: U92 — local UI devloop verification

## Oracle / intent
Verify the R2 frontend (U86 profile viz · U87 history/library · U88 regenerate) renders + wires
correctly against realistic data, locally, with no Databricks infra — by serving the built SPA
through the REAL FastAPI routes (`geniefy_app.api.create_app`) backed by a seeded fake
`SessionService`. This is a VERIFICATION unit (like U76): the audit checks harness soundness +
evidence claims, not a fresh browser run.

## Method
Read the harness, the evidence doc, the LEDGER U92 row, and cross-checked the real
`src/geniefy_app/store.py` (U84/SessionStore contract), `src/geniefy_app/api.py`
(`create_app` / `SessionService` / route status codes), and `src/geniefy_app/profiling.py`
(real `ColumnProfile` keys). Then RAN the harness on port 8771 with the venv
`/tmp/geniefy_pgvenv/bin/python` and probed the routes. Viewed the screenshot image directly.

## Findings

### HIGH severity
- None.

### MEDIUM severity
- None.

### LOW severity
- **favicon.ico → 404 (accepted).** `app/static/index.html` references no favicon; the
  browser's default request 404s. Cosmetic; the doc records it as *LOW, accepted*. No action
  required.
- **QuestionsPanel `textarea` has no `id`/`name` (accepted).** Minor a11y form-field nit, no
  functional impact. The doc records it as *LOW, accepted*. No action required.

(Both LOWs are the two the spec said must be recorded-not-hidden; they are present in
`UI_DEVLOOP_VERIFY.md` → "Console / findings" as accepted LOWs. Verified, not new.)

### UNVERIFIED
- None. (The screenshot was a static-image check, not a re-render; per the verification-unit
  scope this is in-scope-as-evidence, and the rendered values match the seed + the live route
  output exactly — see Check 3.)

## Checks (all five from the dispatch)

**Check 1 — harness is honest (not a mock of the thing under test): PASS.**
`local_ui_preview.py` imports the REAL `SessionService` + `create_app` from `geniefy_app.api`
(lines 18) and mounts the REAL built SPA at `app/static` via `create_app(svc, static_dir=...)`
(lines 124-128). Only the *store* (`_Store`) and *orchestrator* (`_Orch`) are fakes — exactly
the infra boundary the spec sanctions (no Lakebase / warehouse / FMAPI). The fake `_Store`
implements the U84 contract (`load`/`save`/`list_sessions`/`list_library`/`append_audit`/
`upsert_library_entry`) with the SAME field names the real `SessionStore` returns — verified by
cross-reading `store.py`: `list_sessions` → `session_id·target·status·created_by·created_at·
updated_at·n_columns·n_applied` (store.py:210-213) and `list_library` →
`id·scope·match_key·canonical_comment·tags·usage_count·source_table_ref·approved_by·updated_at`
(store.py:285-287). Live probes confirmed the harness emits exactly those key sets. The seed is
realistic and ready-for-review: table draft (conf 0.91) + 3 column drafts + per-column profile
(3 cols) + 1 open question + 1 library entry. The harness does NOT fake the frontend and does
NOT fabricate fields the real backend wouldn't emit — the `get()` response shape is produced by
the real `SessionService.get()` (api.py:166-178), not by the fake.

**Check 2 — harness actually runs: PASS (harness-runs = y).** Ran on 8771; all four
spec-mandated probes returned the predicted values:
- `GET /api/sessions/demo` → **200** (expected 200) ✓
- profile-cols/drafts → **`profile cols 3 drafts 3`** (expected 3 / 3) ✓
- `POST /regenerate {all:true}` → **202** (expected 202) ✓
- `GET /` → `id="root"` count **1** (expected 1 — SPA served) ✓
Extra corroboration: `POST /regenerate {targets:["__table__"]}` → 202; `POST /answers` → 202;
`POST /apply` → **501** (no applier wired). Harness log clean (no tracebacks; uvicorn at
warning level emitted nothing). Server killed via `pkill -f local_ui_preview.py` (post-kill
health probe = connection refused, confirmed down).

**Check 3 — evidence fidelity: PASS.** The doc's profile field names (null/distinct/min-max/
enum/top_k) are the REAL `ColumnProfile` keys — confirmed against `profiling.py:157-166`
(`null_fraction`, `distinct_count`, `cardinality_ratio`, `min`/`max`, `is_enum_candidate`,
`top_k`, `pii`) and against the live `GET /api/sessions/demo` response (key set per column
matched verbatim). The screenshot exists and is non-trivial (`426k`, PNG); viewing it confirms
a genuine ReviewView render whose per-column values match the seed exactly: o_orderkey
(85%, 7,500,000 distinct, 100% unique, 1…6000000), o_orderdate (94%, 2,406 distinct, 0% unique,
1992-01-01…1998-08-02), o_orderstatus (60% needs-input, 3 distinct, enum badge, top
O×3793296/F×3793218/P×13486) — plus header, 3-tab nav, readiness meter, Apply-approved(0)
disabled, questions panel, and per-draft Approve/Reject/Edit/Regenerate/Why?. The doc is HONEST
about scope: the "Scope note" explicitly states the harness wires no applier (so `/apply` is
the 501 stub — live-confirmed) and the agent loop is a no-op fake, deferring real OBO apply to
U85-tests/U89 and the live agent to U41/U42 + U76. No overclaim of a real UC write or a real
model run.

**Check 4 — two LOWs recorded: PASS.** Both the favicon 404 and the form-field a11y nit are in
`UI_DEVLOOP_VERIFY.md` → "Console / findings", each tagged *LOW, accepted* — not hidden.

**Check 5 — no production code touched: PASS.** The LEDGER U92 row (line 118) lists Output as
`docs/UI_DEVLOOP_VERIFY.md` + `scripts/local_ui_preview.py` (+ `docs/assets/u92-review-
profileviz.png`) only — no `src/` or `app/frontend/src/`. Repo is not under git, so corroborated
by mtimes: U92's three outputs are 01:37-01:40; the newest `src/` file (`api.py`) is 01:08 and
the newest `app/frontend/src/` file (`ReviewView.tsx`) is 01:27 — both predate U92's outputs.
No production code was modified by this verification unit.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 2 (both pre-recorded as accepted) · UNVERIFIED: 0
Verdict: **PASS-FINDINGS**   (HIGH ⇒ FAIL; MEDIUM/LOW-only ⇒ PASS-FINDINGS; clean ⇒ PASS)

Return summary: verdict PASS-FINDINGS · HIGH 0 / MED 0 / LOW 2 · harness-runs = y ·
real-routes-not-mocked = y · evidence-honest-about-scope = y
