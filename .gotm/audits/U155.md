# Audit report — content-claim + correctness — 2026-06-23

## Target audited
File: `src/geniefy_app/api.py` (helpers `_high_confidence_targets` + `_confidence_summary`, method `SessionService.approve_high_confidence`, route `POST /api/sessions/{id}/approve-high-confidence`, `get_schema_run` confidence_summary attach)
File: `tests/test_api.py` (4 new tests + `_conf_state` fixture)
Unit: U155

## Oracle
- `.gotm/LEDGER.md` (U155 row + 2026-06-23 "R5 design ratified + audited" Recent-updates entry — binding build-time deltas)
- `docs/design/LLD-amend-007.md` §2/§3/§4
- `.gotm/DECISIONS.md` D59 (also D7, D8, D22/D23, D48)

## Findings

### HIGH severity
- (none)

### MEDIUM severity
- (none)

### LOW severity
- **Perf caveat is acknowledged but the chosen v1 path is genuinely O(n) `store.load` per session.** `get_schema_run` (api.py:393-396) loops every session in the run and calls `self.store.load(sid)` once per session to recompute `_confidence_summary`. The code comment (api.py:390-392) explicitly names this and proposes a SQL aggregate / generation-time summary as a follow-on, so this is a documented, accepted v1 cost — not a defect. Flagged LOW only so the downstream Schema-view units (U157) and a future perf unit are aware that a 40-table run = 40 extra session loads on each run-detail fetch. No action required for correctness.
- **`get_schema_run` confidence_summary path has no direct unit test.** The 4 new tests cover `_high_confidence_targets`, `_confidence_summary`, the all-null case, and the approve endpoint, but none asserts that `get_schema_run` attaches `confidence_summary` per session (the existing FakeStore `list_sessions` returns a synthetic row whose `session_id`="sid-1" has no matching `load`, so the attach would yield `None`). The helper itself is well-tested, so the rollup logic is covered; only the wiring of summary→schema-run-view is untested at the api layer. Non-blocking — U157's verify (U158) exercises the rendered path. Suggested follow-on (optional): add one test that `get_schema_run` carries a non-null `confidence_summary` for a session that `store.load` resolves.

### UNVERIFIED
- (none)

## Detailed check results (5-point + correctness)

**1. Selection rule (LLD-amend-007 §3 + LOW1 delta) — PASS.**
`_high_confidence_targets` (api.py:90-98) returns only drafts where `d.status == DraftStatus.DRAFT AND d.confidence is not None AND d.confidence >= keep_threshold`. `status == DRAFT` is the primary selector (DraftStatus enum, state.py:82-90, distinct values for `needs_input`/`low_confidence`/`error`/`edited`/`approved`/`applied`/`rejected`), and `≥keep_threshold` is the defensive re-check. This structurally excludes `NEEDS_INPUT`, `LOW_CONFIDENCE`, `ERROR`, `EDITED`, `APPROVED`, `APPLIED`, `REJECTED`. Flagged drafts are never selected → no blanket approve (D7). Mutation-check: dropping the `status == DRAFT` clause would let `b=needs_input` (0.5<0.75, excluded by threshold) but more importantly any flagged-yet-high-conf draft slip through; dropping the threshold clause would let `d=0.6` (endpoint fixture) into the approved set and the endpoint test (line 452 `== {"__table__","a"}`, line 458 `by["d"]=="draft"`) would FAIL. Confirmed the tests bite.

**2. Approve semantics (delta MED1/MED2) — PASS.**
`approve_high_confidence` (api.py:226-246) mirrors `review_draft`'s approve branch: per selected target sets `draft.status = DraftStatus.APPROVED` + best-effort `_write_library_on_approve`, persists once (`store.save` only `if approved`), returns `{"approved": [...], "count": len(...)}`. Grepped the method and the whole `src/` tree: `append_audit` is called ONLY from `apply.py:151` (the apply write-path) — the approve method writes NO `audit_log` row, matching `review_draft` (which also never calls `append_audit`). Route (api.py:607-615) uses `_actor()` (returns `"app-service-principal"`, the SP — api.py:464-467), NOT `_identity()`/OBO. Matches MED1+MED2 exactly.

**3. Bulk-approve ≠ apply (D7/D48) — PASS.**
The method only mutates `draft.status`; it never touches `apply_status` (state.py defaults `ApplyStatus.NOT_APPLIED`, untouched here), never invokes `self.applier`, and writes nothing to UC. The route is separate from `POST /api/sessions/{id}/apply` (api.py:617-625), which is the only UC write-path and is the only route using OBO identity. Bulk-APPROVE only stages drafts for a later explicit, human/OBO apply (D48/D7).

**4. confidence_summary (§2) — PASS.**
`_confidence_summary` (api.py:101-126): `overall = 0.5*table_conf + 0.5*col_mean` when both present; null column confidences excluded from the mean (`col_confs` comprehension filters `is not None`, line 108); all-null → `overall=None` (line 113 one-sided fallback yields `None` when both null — test line 435 confirms); one-sided handled (table-only or cols-only → that side). `review_ready` counts drafts in `{DRAFT, APPROVED, EDITED}` with `confidence >= thr` (lines 115-117); `needs_input`/`low` come straight from status; `weakest` = `min(scored, key=conf)` (may be a flagged draft — test line 427 confirms weakest is `b` @ 0.5, a NEEDS_INPUT draft). Arithmetic verified: `_conf_state` → `0.5*0.9 + 0.5*mean(0.95,0.5,0.6,0.8) = 0.45 + 0.5*0.7125 = 0.45 + 0.35625 = 0.80625` (test line 424). `get_schema_run` attaches it per session via the documented per-session `store.load` (perf caveat acknowledged in-code — see LOW finding).

**5. Tests + no regression — PASS.**
`PYTHONPATH=src .venv/bin/python -m pytest -q` → **392 passed, 1 skipped, 1 warning** (the warning is a pre-existing Starlette/httpx deprecation, unrelated). Matches the claim. The 4 new tests assert non-trivially: (a) selection set `{__table__,a,d}` for the helper fixture (d=0.8≥0.75); (b) exact rollup 0.80625 + buckets `review_ready=3, needs_input=1, low=1` + weakest `b@0.5`; (c) all-null→`None`; (d) endpoint approves only `{__table__,a}` (count=2), leaves `b=needs_input` and `d=draft` (d=0.6<0.75). The two fixtures differ deliberately (helper fixture d=0.8 eligible vs endpoint fixture d=0.6 below-threshold) and are each internally consistent — not a contradiction. Mutation-checked both selection and endpoint tests in-head (see point 1): both fail if flagged drafts were approved or the threshold check were dropped.

**6. Cross-refs — PASS.**
U155 ledger row (LEDGER.md:204) annotates supersession of U148 (api.py — endpoint + helpers + get_schema_run summary) and U149 (test_api.py); both supersessions are stated in the Output column. D59 exists (DECISIONS.md:695-705, Status: locked) and its three points match the claim (overall confidence meter, bulk approve-N-high-confidence, extend to hands-off schema view). LLD-amend-007.md exists with §2/§3/§4 as cited. The 2026-06-23 Recent-updates entry records the binding deltas (MED1/MED2/LOW1/LOW2) the build honored.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 2 · UNVERIFIED: 0
Verdict: PASS-FINDINGS   (HIGH ⇒ FAIL; MEDIUM/LOW-only ⇒ PASS-FINDINGS; clean ⇒ PASS)
