# Audit report — design (LLD/amendment) — 2026-06-12

## Target audited
File: `docs/design/LLD-nfr-plan.md`
Unit: U24 — NFR plan: (A) `app.yaml` config surface · (B) per-loop token budgets + summarization · (C) pluggable MCP context providers · (D) git→customer deploy via DAB + shell

Oracle: `.gotm/DECISIONS.md` (D32–D35, D4, D5, D12, D18, D20, D30) · `docs/design/LLD-agent-core.md` (U4) · `docs/design/LLD-packaging-deployment.md` (U8) · `docs/design/LLD-data-layer.md` (U2) · `docs/design/LLD-app-backend.md` (U5) · `docs/design/LLD-frontend.md` (U23) · `docs/design/LLD-amend-001.md` (U10) · `docs/design/LLD-amend-002.md` (U11) · `migrations/APPLY.md`

---

## Findings

### HIGH severity
- None.

### MEDIUM severity
- None.

### LOW severity

- **"D23 Principle 5" is a label-level mis-citation** (`LLD-nfr-plan.md:50`). NFR-B item 4 attributes the honest-surfacing requirement to "D23 Principle 5." D23 (`DECISIONS.md:315–325`) *does* record the honesty guardrail ("narration and explainability reflect real backend events … never fabricated polish"), but it is not structured as numbered principles. The numbered "Principle 5" actually lives in U9 (`LLD-ux-delight.md:26` "Honest theater" principle 5, and §11 "Honesty guardrails (Principle 5, expanded)"). The substance the doc cites (compaction must be surfaced in the UI, never silent) is correct and traceable; only the citation label conflates the decision with U9's principle number. Suggested correction (in a follow-on, not an edit to this frozen unit): cite "U9 Principle 5 / D23 honesty guardrail."

- **`GENIEFY_SAMPLE_MODE` cites "U3 §4.1" but the live profiling provider selection is D25/RunConfig, and sampling is RunConfig-driven** (`LLD-nfr-plan.md:29`). The config-schema row maps `GENIEFY_SAMPLE_MODE` to "default sampling intent (U3 §4.1)." U3 §4.1 is the `profile_table` Request (which carries a `sample` arg), so the citation resolves and is defensible. Minor imprecision only: the *agent-side* sampling policy is a `RunConfig` knob threaded to U3 (U4 §7 / D3 "App passes sample_size; Job passes none"), so the more precise anchor is RunConfig→U3 §4.1 rather than U3 §4.1 alone. Non-blocking; the implementer will not be misled.

### UNVERIFIED

- **`databricks postgres …` exact subcommands for "create project/branch/endpoint + geniefy DB" in `deploy.sh` (NFR-D step 3)** (`LLD-nfr-plan.md:95,99`). The plan correctly aligns with the Autoscaling-Lakebase reality in `APPLY.md` (uses `databricks postgres`, the Project→Branch→Endpoint model, CLI ≥ v0.285, explicitly *not* the legacy `database` group) — this is consistent and the key check passes. What an audit cannot verify from the design alone is that a single idempotent `databricks postgres` create-project/branch/endpoint + `CREATE DATABASE geniefy` sequence is fully expressible/idempotent via CLI (APPLY.md created the DB via a direct `psql`/driver autocommit connection, noting `databricks psql` does not work for Autoscaling projects). This is a *design plan* (the contract is "deploy.sh ensures these, idempotently"); the concrete command sequence is correctly deferred to the implementing deploy unit. Flagged for the practitioner to validate at build time, not a defect in the plan.

---

## Cross-reference & §5 amendment-register check (the key check)

Every `D#` / `U#` / `§` cited resolves and says what is claimed. The §5 amendment register is **accurate**:

| Register row | Claim | Verified against oracle |
|---|---|---|
| A (config) → **U8 §6, U5** | adds `app.yaml` config schema + startup validation | U8 §6 = "App config & resource wiring" ✓ (`LLD-packaging-deployment.md:74`). U5 = `LLD-app-backend.md`; has provider/config endpoints (§6 `/providers` CRUD, `LLD-app-backend.md:87`) and AppConfig-via-env model (§2, `:21`) ✓ |
| B (tokens) → **U4 §4/§5/§7** | per-call budgets, TokenCounter/PromptBudgeter/compaction, RunConfig fields, observability | U4 §4 = "Orchestration (the loop)" ✓; §5 = "Model integration (D4)" w/ `LLMClient` ✓; §7 = "Configuration (RunConfig)" ✓; §6 = "Observability" (cited separately at `:52`) ✓; §3.3 `context_token_budget` trim (cited at `:46`) ✓ |
| C (MCP) → **U2, U4 §3.3, D5** | registration schema + MCP invocation contract (data model already supports it) | U2 has `context_providers` with `kind` ∈ `mcp` and `config jsonb` ✓ (`LLD-data-layer.md:75–89`, enum `LLD-data-layer.md:238`); U4 §3.3 = `ContextGatherer` with uniform built-in + MCP provider interface ✓; D5 = built-in + pluggable MCP providers ✓ |
| D (deploy) → **U8 §1/§3/§7/§11** | DAB-vs-CLI split + `deploy.sh` | U8 §1 = "Purpose & distribution model" ✓; §3 = "The DAB" ✓; §7 = "Lakebase provisioning + migrations" ✓; §11 = "CI/CD (deferred)" ✓ — all four sections exist and are on-topic |

Additional spot-checks that passed:
- The §4/§5 NFR-D claim "amends U8 §1/§3/§7/§11" matches **D35** verbatim (`DECISIONS.md:478` "amends U8 §1/§3/§7/§11") — no drift.
- The §5 closing precedent "exactly as the U10/U11 amendments related to U2/U4/U6": U10 amends U2/U4/U5/U6/U8/U9 (`LLD-amend-001.md:4`) and U11 amends U6/U10 (`LLD-amend-002.md:4`) — U2, U4, and U6 are all genuinely touched, so the precedent is accurate.
- NFR-A §29 `GENIEFY_PG_HOST/DATABASE/SCHEMA → D6/D30`: D30 (`DECISIONS.md:416`) fixes database = `geniefy`, schema `geniefy` ✓; APPLY.md confirms the live DB/schema names ✓.
- `vite build … see U24 §4` referenced from U23 (`LLD-frontend.md:61,89`) and back-referenced by U24 NFR-D step 2 + D31 — consistent.

---

## Spec match (one section per NFR + amendment register)

- **Existence:** doc exists at the ledger's stated path (`docs/design/LLD-nfr-plan.md`); ledger row U24 (`LEDGER.md:45`) names this output. ✓
- **One section per NFR + register:** §1 NFR-A, §2 NFR-B, §3 NFR-C, §4 NFR-D, §5 amendment register, §6 sequencing — present and each is a concrete, buildable plan:
  - **(A)** real env→config table (`GENIEFY_*` → source → drives), layering chain (bundle var → per-target → `--var` → `app.yaml` env → `AppConfig`), fail-fast load, and the Lakebase-vs-`app.yaml` split for redeploy-free config. ✓
  - **(B)** per-call input-token budgets per call-type (`reason`/`judge`/`summarize`), a TokenCounter, an ordered 4-step compaction sequence with a defined stop rule + truncation fallback, explicit RunConfig additions, and observability (budget vs. estimated vs. actual + running cost). ✓
  - **(C)** a registration schema (jsonc with `server_url`/`transport`/`auth` scope-ref/`tool_allowlist`/`query_template`/`max_snippets`/`token_budget`) and a 4-step invocation/normalization contract (connect+discover → invoke → normalize to `ContextSnippet` → auth), with the degrade-not-fail rule (U4 §8). ✓
  - **(D)** an explicit DAB-can / DAB-can't split and a 7-step idempotent `deploy.sh` (preflight → vite build → ensure Lakebase → bundle deploy → migrate → grants → print URL). ✓

---

## Internal consistency & decision fidelity

- **No contradiction with the frozen design.** B is consistent with U4's loop (compaction's "shrink the batch" = fewer columns per `reason` call, matching U4 §3.4 `reason_batch_size` batching; budget tie to D20 per-iteration bound is coherent with U4 §4 phase persistence). C is **additive**, not a redesign: it explicitly builds on the *existing* `context_providers.kind='mcp'` + `config jsonb` (U2) and the uniform `ContextGatherer` (U4 §3.3) — "adding a provider is a config row, not code." D is consistent with APPLY.md's Autoscaling-Lakebase reality (`databricks postgres`, Project→Branch→Endpoint, not the legacy `database` group).
- **Decision fidelity:** D32 (per-call budgets + compaction pipeline trim→summarize→reduce→shrink + the three named RunConfig fields) — matches §2 exactly. D33 (`app.yaml` single surface, `GENIEFY_*`, fail-fast, redeploy-free config in Lakebase) — matches §1 exactly. D34 (registration schema fields + connect→list_tools∩allowlist→invoke→normalize→degrade) — matches §3 exactly. D35 (DAB spine + idempotent `deploy.sh`, the named steps) — matches §4 exactly.
- **D4 secrets:** `auth` is a `{secret_scope, key}` reference; the schema comment and §3 step 4 both state "ref only, never a raw secret" / "never logged" — honors D4 (sanitized/secret-refs). ✓
- **D12 parameterization:** the layering chain keeps prod fully overridable (`--var`, per-target); no hardcoded values introduced. ✓
- **D20 tie-in:** §2 item 5 explicitly maps the per-batch budget to the D20 crash/cost bound — coherent. ✓
- **Freeze-rule compliance:** the doc carries every delta itself (env schema, token-budget plan, MCP contract, deploy split) and states the frozen U4/U8/U2/D5 are unedited; the amendment register names what each NFR supersedes. No part of the plan requires editing a frozen doc — it follows the U10/U11 amendment pattern correctly. ✓

---

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 2 · UNVERIFIED: 1
Verdict: **PASS-FINDINGS**   (HIGH ⇒ FAIL; MEDIUM/LOW-only ⇒ PASS-FINDINGS; clean ⇒ PASS)

The §5 amendment-register citations all check out — every cited U4/U8/U5/U2/D# section resolves and says what is claimed, the U8-section list matches D35 verbatim, and the U10/U11 precedent is accurate. The plan is concrete and buildable for all four NFRs, is additive to (not in conflict with) the frozen design, and complies with the GOTM freeze rule. The two LOW findings are citation-label imprecisions; the one UNVERIFIED item is a build-time validation correctly deferred to the implementing deploy unit.
