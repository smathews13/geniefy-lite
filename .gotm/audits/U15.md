# Audit report — code/spec (DAB sync-exclude + host-var fix; re-audit of U13 FAIL) — 2026-06-12

## Target audited
File: `databricks.yml`
Unit: U15 — "Fix (U13 FAIL): add `sync.exclude` for `.gotm/` + dev `.claude/` (D25) + align `prod` host to U8 shape"

Oracle:
- `.gotm/audits/U13.md` (the original FAIL — its HIGH `sync.exclude`/D25 finding + the prod-host MEDIUM)
- `docs/design/LLD-packaging-deployment.md` (U8 — §3 illustrative DAB, §4 parameterization, §9 ships vs. dev-only)
- `.gotm/DECISIONS.md` — D25 (`.gotm/`+`.claude/` excluded from `bundle sync`), D12 (fully parameterized; prod overridable), D28 (dev FE-VM defaults)
- `.gotm/LEDGER.md` (U13/U15 rows; ownership of `databricks.yml` moved U13→U15)

Checklist run: 5-point (existence · spec-match · cross-reference integrity · internal consistency · decision fidelity) + DAB-specific + the named CRITICAL re-audit check (is the prior HIGH genuinely closed).

---

## Findings

### HIGH severity

None. **The original U13 HIGH (the D25 `sync.exclude` regression) is genuinely closed.** A top-level `sync.exclude` block is present (`databricks.yml:12-15`) listing both `.gotm/` and `.claude/`, with a comment that correctly cites D25 / U8 §9 (`databricks.yml:9-11`). I confirmed by direct grep — `grep -niE "exclude|\.gotm|\.claude|sync:" databricks.yml` → matches at lines 12-15 (the original audit's identical grep returned *no* match). Both directories still exist on disk and are populated (`.claude/settings.json` + `.claude/settings.local.json`; `.gotm/{audits,hooks,prompts,DECISIONS.md,LEDGER.md,PROTOCOL.md,QUESTIONS.md}`), so the leak the exclude prevents was a real risk — and is now fixed at the source, not merely relabeled. The fix landed as a follow-on unit's output (per the no-edit-done rule), with ownership of `databricks.yml` recorded as moving U13→U15 in the ledger (`.gotm/LEDGER.md:34,36`).

### MEDIUM severity

None. **The original U13 prod-host MEDIUM is also resolved.** A `host` bundle variable is now declared (`databricks.yml:18-20`) and **both** targets reference it: `dev` (`databricks.yml:45`) and `prod` (`databricks.yml:50`) each use `workspace.host: ${var.host}` — exactly the U8 §3 illustrative shape (`docs/design/LLD-packaging-deployment.md:50-51`, both targets `host: "${var.host}"`) and the U8 §4 "`host` as a first-class bundle variable" requirement. The previous scaffold's hardcoded dev host + comment-only prod has been replaced; `prod` stays customer-overridable via `--var host=…` (comment at line 50), satisfying D12.

### LOW severity

- **`prod` resolves to the dev FE-VM host unless overridden** (`databricks.yml:18-20` + `:50`). Because `host` has a single `default` (`https://fevm-rd-classic.cloud.databricks.com`, the D28 dev value) and `prod` does not pin its own `host`, a bare `databricks bundle validate -t prod` (no `--var`/profile) resolves the prod host to the FE-VM workspace. This is the U8 §3 illustrative shape exactly (which likewise gives both targets `${var.host}` and no separate prod default) and is explicitly documented in-file ("customer overrides: bundle deploy -t prod --var host=…", line 50), so it honors D12's "prod stays overridable" — it is a defaults-ergonomics note, not a deviation. A later deploy/README unit (U16) should ensure the customer-facing prerequisites make the prod `--var host` step explicit so no one deploys "prod" to the FE-VM by omission. Non-blocking.

### UNVERIFIED

- **Whether `databricks bundle validate` actually executes cleanly** could not be run (no Databricks CLI / workspace auth in this audit context; authoring is intentionally infra-free per D27). Static read indicates it would still validate for `dev`: `bundle.name` present (`databricks.yml:6-7`); a well-formed `sync.exclude` list (`:12-15`); a complete `variables` block (`:17-37`) including the newly-added `host`; `targets.dev`/`targets.prod` both well-formed (`:40-51`); and `resources.apps.geniefy` intact with `source_code_path: ./app` (`:53-61`). `${var.host}` resolves against the declared `host` variable, so no dangling reference is introduced. Resource bindings (warehouse, endpoint, Lakebase, secret scope) remain intentionally deferred per the file's own comment (`:58-61`) and U8 (§6, §13) — consistent, not a gap. Adding `sync.exclude` is a deploy-artifact concern and does not affect validate-time success. Flag the actual `bundle validate -t dev -p fe-vm-classic` run as the deploy unit's gate (carried over from U13's UNVERIFIED).

---

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 1 · UNVERIFIED: 1
Verdict: PASS-FINDINGS   (HIGH ⇒ FAIL; MEDIUM/LOW-only ⇒ PASS-FINDINGS; clean ⇒ PASS)

**The fix landed.** U13's original HIGH — the D25 `sync.exclude` regression that would upload `.gotm/` + the dev `.claude/` immutability hook into the deployed workspace — is genuinely resolved: a top-level `sync.exclude` block now lists `.gotm/` and `.claude/` (`databricks.yml:12-15`), confirmed by grep where the original audit's identical grep found nothing. U13's prod-host MEDIUM is also resolved: a `host` variable is declared (`:18-20`) and both `dev` (`:45`) and `prod` (`:50`) use `host: ${var.host}`, matching U8 §3/§4. Decision fidelity holds — D25 satisfied (the named CRITICAL check), D12 parameterization preserved (dev keeps the D28 FE-VM defaults exactly: host `fevm-rd-classic`, catalog `rd_classic_catalog`, schema `geniefy`, warehouse `0336d1a2b47936b4`, endpoint `databricks-claude-sonnet-4-6`; prod overridable), and all D#/U# citations in the file's comments resolve and say what is claimed. The bundle is internally consistent: `${var.host}` references the declared variable, both targets are well-formed, and the app resource is intact with bindings intentionally deferred. The single LOW is a defaults-ergonomics note (bare `-t prod` resolves to the dev host unless overridden — matches the U8 illustrative shape and is documented in-file); the UNVERIFIED is the still-unrun `bundle validate`, carried over and gated to the deploy unit. No blocking findings — U15 is consumable.
