# Audit report — wiring / structure + test-meaningfulness — 2026-06-12

## Target audited
File: `src/geniefy_app/api.py` (the `/apply` route delegation + the `build_service` `Applier(sql, store)` wiring) · `tests/test_apply_wiring.py`
Unit: U54 — wire the Applier into `build_service` so `/api/sessions/{id}/apply` is live (was a 501 stub)

## Oracle
- `src/geniefy_app/apply.py` (U53 — `Applier.apply(session_id, *, created_by, confirm=False, targets=None)`)
- `docs/design/LLD-review-apply.md` (U6 — apply endpoint semantics)
- `.gotm/DECISIONS.md` — D7 (apply is a separate, diff-first action), D21 (re-check live comment, conflict-aware, idempotent, per-item), D36 (hermetic backend pkg; `build_service`/`_lakebase_connection` integration-verified at deploy, not unit-tested)
- `.gotm/LEDGER.md` — U54 row (stated paths confirmed)

## What was checked (and how)

### 1. Wiring correct — PASS
- `build_service` constructs the Applier exactly once: `Applier(sql, store)` (api.py:307), passed as `SessionService(make_orchestrator=…, store=store, config=rc, applier=Applier(sql, store))` (api.py:306–307).
- `sql` is the **same** `WarehouseSqlRunner` built at api.py:286 and shared with the profiler/lineage/query-history wiring — the same warehouse runner the apply DDL path needs (D7/D21). `store` is the **same** `SessionStore` (api.py:304) the service reads/writes. No second runner/store is created for the Applier.
- Positional `(sql, store)` maps correctly onto `Applier.__init__(self, run_sql, store)` (apply.py:55). ✓
- `/apply` route: returns **501** (`JSONResponse`) when `service.applier is None` (api.py:236–238); delegates `service.applier.apply(session_id, created_by=_actor())` when set (api.py:239). ✓
- Delegation args match `Applier.apply`'s signature: `session_id` positional, `created_by` keyword-only; `confirm`/`targets` correctly left to their defaults (route does not yet expose re-confirm/scoped apply — consistent with U54's minimal scope and LLD-review-apply §6, which lists those as separate routes). ✓

### 2. Test meaningfulness — PASS
RUN: `PYTHONPATH=src /tmp/geniefy_pgvenv/bin/python -m pytest tests/test_apply_wiring.py -q` → **2 passed**.
- `test_apply_501_when_not_wired` — `applier=None` ⇒ asserts 501 (unwired branch). ✓
- `test_apply_delegates_when_wired` — wires a `FakeApplier` whose `apply(self, session_id, *, created_by)` mirrors the real signature; asserts status 200, return-value passthrough (`applied == 2`), **and** `applier.calls == [("sid1", "app-service-principal")]` — i.e. the route delegates with the *right* args (session_id + the actor identity as `created_by`). ✓
- Both branches (501 unwired / delegate-when-wired with correct args) are covered. The `FakeApplier` keyword-only `created_by` means a positional-vs-keyword mismatch in the route would fail the test — the delegation contract is genuinely exercised, not just smoke-tested.
- Per the unit note (D36), the test does **not** exercise the real `Applier(sql, store)` construction in `build_service` (integration-verified at deploy) — only the delegate/wiring, which is correct scope. Not a finding.
- Full suite regression check: `PYTHONPATH=src … pytest -q` → **262 passed** (grown from the ledger's 255; no regression from the wiring).

### 3. Hermetic (SDK-free import) — PASS
RUN: `PYTHONPATH=src python3 -c "import geniefy_app.api; print('ok')"` → **ok**.
Stronger verification: on this machine the system `python3` *has* `databricks.sdk` but *lacks* `fastapi`/`pydantic`; the import still succeeds, and an `sys.modules` probe after import shows **zero** leaked infra imports — `fastapi`, `pydantic`, `databricks.sdk`, `psycopg2`, `geniefy_app.apply`, `…store`, `…providers` are all absent at module-import time. The `Applier` import is genuinely lazy (inside `build_service`); `Applier` is not bound at module scope. ✓

### 4. Consistency — PASS
- Exactly one `Applier(` construction (api.py:307) — no double-construction.
- Exactly one 501 path, gated solely on `applier is None` — no leftover stub fires when the applier is wired.
- Actor identity passed (`_actor()` → `"app-service-principal"`, api.py:186) is the same actor used by every other write route (run/answers/review) — sane and consistent. The LLD's on-behalf-of-user identity (D11/LLD §5) is a documented future refinement, not a U54 obligation.

## Findings

### HIGH severity
- none.

### MEDIUM severity
- none.

### LOW severity
- **Stale `# pragma: no cover` on the delegation line.** api.py:239 carries `# pragma: no cover`, but `test_apply_delegates_when_wired` now exercises exactly that line (it asserts the FakeApplier was called and its return value passed through). The pragma was correct under U52 (when `/apply` was a pure 501 stub and the real Applier couldn't be invoked hermetically); U54's wiring test makes it inaccurate. Cosmetic — it would only mask a future coverage gap on that line. Suggested follow-on: drop the `# pragma: no cover` on api.py:239.
- **Slightly stale module docstring.** The api.py module docstring (lines 16–18) still describes `/apply` as "501 until that unit lands" / "optional injected `applier` (501 until that unit lands)." Post-U54 the production path always wires an Applier; the 501 now only occurs for a service constructed without one (e.g. the unwired test). Living-doc text, non-blocking; tidy if convenient.

### UNVERIFIED
- none. (Per D36, the real `Applier(sql, store)` construction inside `build_service` is integration-verified at deploy, explicitly out of unit-test scope — not treated as unverifiable; the unit note sanctions it.)

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 2 · UNVERIFIED: 0
pytest: `tests/test_apply_wiring.py` → 2 passed; full suite → 262 passed
Wiring-correct: yes
Verdict: **PASS-FINDINGS**   (HIGH ⇒ FAIL; MEDIUM/LOW-only ⇒ PASS-FINDINGS; clean ⇒ PASS)
