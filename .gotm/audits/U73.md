# Audit report — content-claim + code (fix verification) — 2026-06-12

## Target audited
File: `tests/test_providers.py` (the added `test_lazy_factories_import_sdk_only_when_called`, lines 112–126)
Unit: U73 — Fix (U50 audit LOW): a named test proving the `*_from_databricks` factories lazy-import the SDK

Independent auditor — did not author this unit. Mechanical only; nothing fixed, no
ledger/source edits.

## Oracle
- `.gotm/audits/U50.md` — the LOW finding: the "lazy factories raise only when called"
  half of the dispatch contract had no named test (it was verified by the audit, not by
  the suite).
- `src/geniefy_app/providers.py` — `databricks_chat_query` (line 79) and
  `databricks_sql_execute` (line 94), each with `from databricks.sdk import
  WorkspaceClient` *inside* the function body (lines 82, 97).

## Commands run
- `PYTHONPATH=src /tmp/geniefy_pgvenv/bin/python -m pytest tests/test_providers.py -q`
  → **9 passed in 0.02s**.
- `find_spec('databricks')` in `/tmp/geniefy_pgvenv` → **`None`** (SDK genuinely absent),
  so the test's skip guard does **not** trigger.
- `pytest -v` confirms `test_lazy_factories_import_sdk_only_when_called` → **PASSED**
  (RAN, not skipped).
- **Mutation probe** (throwaway copy of `src/`, real source untouched): hoisted
  `from databricks.sdk import WorkspaceClient` to module top in `providers.py` (eager
  import). Result: module import → `ModuleNotFoundError` at collection → the whole
  `test_providers.py` suite errors out (1 collection error). Confirms the lazy-import
  contract is genuinely pinned by the suite — an eager regression is caught.

## Checks performed

**1. Test is meaningful + correct.** PASS.
- *Import-SDK-free half* — the module-level `from geniefy_app.providers import ...`
  (line 16) already succeeds in the SDK-less venv (precondition of the whole suite); the
  test re-imports the module (line 121) to make that explicit.
- *Raise-only-when-called half* — the test asserts `pytest.raises(ModuleNotFoundError)`
  for both `P.databricks_chat_query()` (line 124) and
  `P.databricks_sql_execute(warehouse_id="wh")` (line 126). In an SDK-less interpreter
  this proves the `from databricks.sdk import WorkspaceClient` is deferred to call time
  (providers.py:82, 97), not eager. This is exactly the half the U50 LOW said was untested.
- *Skip guard correct + portable* — `importlib.util.find_spec("databricks") is not None →
  pytest.skip(...)` (lines 119–120). On an SDK-equipped machine the factories would NOT
  raise `ModuleNotFoundError`, so the test skips cleanly rather than producing a false
  failure. Guard is correct.
- *Factory names + call signatures match providers.py* — `databricks_chat_query()`
  (keyword-only `profile=None`, callable with no args ✓) and
  `databricks_sql_execute(warehouse_id="wh")` (keyword-only required `warehouse_id` ✓)
  match the source signatures verbatim. The SDK import sits before the required-arg use in
  both, so `ModuleNotFoundError` is raised at the import line regardless of arg binding.
- Mutation probe confirms non-vacuity (eager import → suite errors).

**2. RUN.** PASS — `tests/test_providers.py` → **9 passed** in 0.02s. The new test
**RAN** (not skipped — `find_spec('databricks')` is `None` in the hermetic venv) and
passed.

**Decision fidelity.** Honors D1/D17 (hermetic core; no SDK in the unit suite) — the test
is portable across SDK-present and SDK-absent machines via the skip guard. Consistent with
the U49/U50 audit conclusions; no contradiction.

## Does it close the U50 LOW?
**Yes.** The U50 LOW was: the "raise only when called" half of the lazy-factory contract
had no named test. `test_lazy_factories_import_sdk_only_when_called` is that named test —
it calls each factory in an SDK-less env and asserts `ModuleNotFoundError`, making the
deferred-import guarantee self-documenting in the suite. The U50 LOW is resolved.

## Findings

### HIGH severity
- None.

### MEDIUM severity
- None.

### LOW severity
- None.

### UNVERIFIED
- Live behavior of the factories against a real Databricks serving endpoint + warehouse
  (the `model_dump()` response shape, manifest `schema.columns` + `data_array`
  extraction). By design these are deploy/integration-verified, not part of the hermetic
  suite — not a defect of U73, and out of this fix-unit's scope. Flagged for the deploy
  step (consistent with U47/U49/U50 precedent).

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 0 · UNVERIFIED: 1
Verdict: PASS   (HIGH ⇒ FAIL; MEDIUM/LOW-only ⇒ PASS-FINDINGS; clean ⇒ PASS — the lone
UNVERIFIED is an out-of-scope deploy-time item, not a finding against U73)
