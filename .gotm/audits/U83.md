# Audit report — code (regenerate: orchestrator + API) — 2026-06-12

## Target audited
Files:
- `src/geniefy_core/orchestrator.py` — `regenerate(state, targets)` + `_targets_to_affected`
- `src/geniefy_app/api.py` — `SessionService.start_regenerate` / `execute_regenerate` + `POST /api/sessions/{id}/regenerate` + `RegenerateRequest`
- `tests/test_orchestrator.py` — regenerate scope/no-reprofile tests
- `tests/test_api.py` — regenerate route tests
Unit: U83 (E3/D45 — re-generate table or specific columns)

## Oracle
- `docs/design/LLD-amend-003.md` §E3
- `.gotm/DECISIONS.md` D45 (regenerate named targets only, reuse profile/context, preserve the rest, async+poll), D48 (apply uses the user; everything else — incl. regenerate — runs as the SP), D17 (no re-profiling on resume-like passes), D18 (async background + poll)
- `.gotm/LEDGER.md` (U83 row + supersession annotations)

## Findings

### HIGH severity
- None.

### MEDIUM severity
- None.

### LOW severity
- **No regenerate-specific 409 concurrency test.** `tests/test_api.py` has `test_regenerate_unknown_session_404` (404) and `test_regenerate_route_redrafts_then_polls_ready` (poll-liveness), but no test that POSTing `/regenerate` on an already-in-flight session returns 409. The 409 guard in `SessionService.start_regenerate` (api.py:124-125) is byte-for-byte identical to `submit_answers` (api.py:102-103, tested by `test_answers_rejected_while_in_flight_409`), so the behavior is correct; this is a coverage gap, not a defect. Suggested fix unit: add `test_regenerate_rejected_while_in_flight_409`.
- **`test_regenerate_all_forwards_none_targets` only asserts the 202.** It does not assert the route actually forwarded `None` to the orchestrator (the `FakeOrch.regen_targets` recorder is set but never read). The route logic `targets = None if req.all else req.targets` (api.py:267) is plainly correct and the orchestrator scope test `test_regenerate_all_redrafts_table_and_every_column` covers the `None ⇒ everything` semantics directly, so the assertion gap is cosmetic. Suggested: assert `service`'s last orchestrator saw `regen_targets is None`.

### UNVERIFIED
- None.

## Detailed check results

1. **named-targets-only — PASS.** `_targets_to_affected` (orchestrator.py:125-134): `not targets or "__all__" in targets → {None, *cols}` (table + every profiled column); otherwise each `t` maps to `None if t in ("__table__", "", None) else t`. `_redraft` (orchestrator.py:174-195) builds a sub-state carrying the full table block + only the affected columns + existing context, re-drafts/judges on it, replaces `state.table_draft` only when `None in affected`, and merges columns via `fresh.get(c.column_name, c)` — untouched drafts keep their prior object (and review state). All three scope tests confirm: `test_regenerate_named_column_only_redrafts_that_target` (column "x" regenerated to "def x"; o_orderkey + table stay "STALE"), `test_regenerate_table_only_keeps_columns` (table → "Order headers."; columns stay "STALE"), `test_regenerate_all_redrafts_table_and_every_column` (table + all columns refreshed). Matches §E3 + D45.

2. **no re-profiling (D17) — PASS.** `regenerate` (orchestrator.py:113-123) calls only `_targets_to_affected`, `_redraft`, and `_gate` — never `self.profiler`/`self.gatherer`. `_redraft` reads `state.profile`/`state.context` and threads them into the sub-state. `test_regenerate_reuses_profile_no_reprofile` asserts `st.profile is prof` (the same object after regenerate). Matches D17 / "like resume".

3. **re-gate happens — PASS.** `regenerate` ends with `return self._gate(state)` (orchestrator.py:123); `_gate` recomputes `_signals` + `gate.apply` and, in INTERACTIVE mode, returns `needs_input` (Phase.AWAITING_INPUT) when a regenerated draft trips, else `ready_for_review`. Questions/phase recompute as required.

4. **SP not user (D48) — PASS.** The `/regenerate` route (api.py:263-273) takes no identity dependency and uses `actor = _actor()` (the app SP) for both `start_regenerate` and the `execute_regenerate` background task. Only `/apply` (api.py:284-292) injects `identity: RequestIdentity = Depends(_identity)` and forwards `identity.actor` / `identity.access_token`. Regenerate runs as the SP, as D48 requires.

5. **poll-liveness + concurrency — PASS.** `start_regenerate` (api.py:120-129): 404 on unknown session; 409 if `state.session_status.value in _IN_FLIGHT`; sets `state.phase = Phase.REASONING` and saves BEFORE the background task. `Phase.REASONING.to_session_status()` = `SessionStatus.REASONING` ("reasoning"), which is in `_IN_FLIGHT = {"profiling","gathering_context","reasoning","applying"}` (api.py:62) — so the poll client sees an in-flight status immediately and a second concurrent regenerate/answers call 409s. `execute_regenerate` flips it terminal via `orch.regenerate` + final save. 404 and poll-liveness are tested; the 409 path shares identical code with the tested `submit_answers` guard (see LOW).

6. **tests pass — PASS.** `tests/test_orchestrator.py tests/test_api.py` → **29 passed** (1 deprecation warning, unrelated). Full suite `tests/` → **304 passed** (matches the expected 304). Command: `PYTHONPATH=src /tmp/geniefy_pgvenv/bin/python -m pytest`.

7. **hermetic — PASS.** `PYTHONPATH=src python3 -c "import geniefy_core.orchestrator; ..."` → **leaked: none** (no databricks/openai/psycopg2/fastapi modules imported by the core orchestrator).

8. **ledger hygiene — PASS.** U83's Output cell lists all 4 files (orchestrator.py, api.py, test_orchestrator.py, test_api.py). Supersession annotations verified in the correct rows on the correct files: U67 `src/geniefy_core/orchestrator.py (superseded by U83)`, U85 `src/geniefy_app/api.py (…; superseded by U83)`, U42 `tests/test_orchestrator.py (superseded by U83)`, U64 `tests/test_api.py (superseded by U83)`.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 2 · UNVERIFIED: 0
Verdict: PASS-FINDINGS   (HIGH ⇒ FAIL; MEDIUM/LOW-only ⇒ PASS-FINDINGS; clean ⇒ PASS)

Both findings are LOW test-coverage gaps with no impact on correctness; the regenerate behavior matches §E3/D45/D48/D17/D18 and the full suite is green at 304.
