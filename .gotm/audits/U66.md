# Audit report — fix-verification (content-claim + render/lint + regression) — 2026-06-12

## Target audited
File: `src/geniefy_app/profiling.py` (+ `tests/test_profiling.py`)
Unit: U66 — Fix: emit an empty-table (0-row) warning (U51 audit MED, U3 §9) + drop the unused `Sequence` import (LOW)

Oracle: `.gotm/audits/U51.md` (MED: empty-table `warnings` entry per U3 §9 not emitted; LOW: unused `Sequence` import) · `docs/design/LLD-profiling-tool.md` (U3 §9 — warnings on degenerate inputs, referenced via U51).

## Method
Independent auditor (did not author U66). Read target + oracle. Verified the fix closes the U51 MED and LOW. Ran the prescribed suite. Mutation-tested the new bite test (removed the fix, confirmed the test fails, restored). Grepped the whole file for `Sequence`. Verified SDK-free import.

`PYTHONPATH=src /tmp/geniefy_pgvenv/bin/python -m pytest tests/test_profiling.py -q` → **23 passed in 0.02s** (U51 was 22; +1 for the new test). `import geniefy_app.profiling` loads **zero** `databricks*` modules (SDK-free).

## Findings

### HIGH severity
*(none)*

### MEDIUM severity
*(none)*

### LOW severity
*(none)*

The three prescribed checks pass:

1. **fix correct — confirmed.**
   - **Empty-table warning (closes U51 MED).** `profile_table` at profiling.py:114-115 appends, when `row_count == 0`:
     `"table is empty (0 rows): distributions and cardinality are not meaningful (U3 §9)"` to the table block's `warnings`. The message names the empty/0-row condition and cites §9. This is the exact signal U51 flagged as missing (LLD §9, audits/U51.md:29).
   - **No crash on the empty path.** Per-column aggregates over 0 rows stay sane: `null_fraction = round(1 - non_null/total, 6) if total else None` (line 157) → `None` (no zero-division); `cardinality_ratio = … if (ndv is not None and non_null) else None` (line 160) → `None`; `is_enum_candidate = bool(ndv is not None and 0 < ndv <= max_card)` (line 163) → `False` for `ndv=0`. The 0-row pytest exercises this end-to-end and passes.
   - **`Sequence` removed (closes U51 LOW).** Import at line 24 is now `from typing import Any, Mapping` — `Sequence` dropped. `grep -n "Sequence"` over the whole file returns zero hits: genuinely no longer imported AND not referenced anywhere.

2. **test bites — confirmed.** `test_empty_table_emits_warning` (test_profiling.py:146-164) drives a 0-row fake warehouse: `information_schema.columns` → the 3 COLS; row-count `COUNT(*) AS n` → `{"n": 0}`; `approx_count_distinct` aggregate → `{"total": 0, "non_null": 0, "ndv": 0}`; top-K → `[]`. It asserts `row_count == 0` **and** `any("empty" in w for w in warnings)`. Mutation probe: deleting the two `if row_count == 0:` append lines makes the test FAIL (`assert any("empty" in w …)` → `assert False` at test:164); restoring the lines makes it pass. The test genuinely bites the fix.

3. **no regression — confirmed.** Full suite: **23 passed** (the +1 over U51's 22 is exactly this new test; all prior assertions — type classification, enum gating, PII mask/no-mask, sampling, column subset, complex-skip, end-to-end core wiring — still green). `import geniefy_app.profiling` is SDK-free (no `databricks*` module loaded on import). `test_table_block` still asserts `warnings == []` on the populated 100-row table, so the new branch did not leak a spurious warning into the non-empty path.

### UNVERIFIED
- **Live-warehouse SQL correctness** (carried forward from U51, unchanged by this fix): the generated `information_schema` / aggregate / top-K / `TABLESAMPLE` SQL is exercised only against the fake warehouse. This is the explicitly deferred integration step, not an audit gap, and U66 did not touch the SQL generation.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 0 · UNVERIFIED: 1
Verdict: PASS   (no findings above the trivial bar; the lone UNVERIFIED is the pre-existing, out-of-scope live-warehouse integration deferral)

Fix closes U51 MED: **yes.** Fix closes U51 LOW (`Sequence`): **yes.**
