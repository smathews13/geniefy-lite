# Audit report — existence · content-claim/source-fidelity · regression — 2026-06-14

## Target audited
Files:
- `app/frontend/src/App.tsx` (header `<h1>`, tagline `<p>`, `NAV` array)
- `app/frontend/index.html` (`<title>`)
- `src/geniefy_app/api.py` (`FastAPI(title=...)`)

Unit: U128

## Oracle
- **D55** (`.gotm/DECISIONS.md:648-652`) — "geniefy-lite" shown in UI header + browser/page title + FastAPI title; tagline "Make your lakehouse AI ready"; nav tabs Table · Schema · History · Library (rename "Document"→"Table"); **Databricks app resource stays `geniefy-dev`** (renaming out of scope).
- **U128 ledger row** (`.gotm/LEDGER.md:154`) — visible-string/title changes only; no behavior/route/test change; outputs = App.tsx (h1 + tagline + nav label), index.html (`<title>`), `src/geniefy_app/api.py` (FastAPI/OpenAPI title string only).

## What was checked

### existence — all three files + the required strings present
PASS.
- `app/frontend/src/App.tsx:54` — `<h1 …>geniefy-lite</h1>` present.
- `app/frontend/src/App.tsx:55-57` — tagline `<p>` reads `Make your lakehouse AI ready.` present.
- `app/frontend/src/App.tsx:18-23` — `NAV` array present; first entry `{ id: 'run', label: 'Table', … }`.
- `app/frontend/index.html:6` — `<title>geniefy-lite — Make your lakehouse AI ready</title>` contains "geniefy-lite".
- `src/geniefy_app/api.py:354` — `app = FastAPI(title="geniefy-lite", …)`.

### content-claim / source-fidelity — strings match D55; no contradicting remnants
PASS.
- Header reads exactly **geniefy-lite** (matches D55 §1). `api.py:355` description also updated to "…— make your lakehouse AI ready." (consistent, non-blocking bonus).
- Tagline reads **Make your lakehouse AI ready.** (D55 §2 wording; trailing period is the explicitly-allowed variant per the audit spec).
- Nav tab labels, in order: **Table · Schema · History · Library** (`App.tsx:19-22`) — matches D55 §3 exactly; the ambiguous "Document" tab label is gone.
- `grep -rn "geniefy-v3\|geniefy<\|>geniefy<" app src` over the **named U128 targets** finds no old-name remnant in `app/frontend/src/App.tsx`, `app/frontend/index.html`, or `src/geniefy_app/api.py`.
- No `label: 'Document'`, no `>Document<` tab, and no "Turn a Unity Catalog table…" old-tagline remnant anywhere in the targets. (The two surviving "Document" tokens in App.tsx — the run-button verb at `:120` and the word inside the `run` tab's `hint` tooltip at `:19` — are legitimate verbs, not the nav label, and do not contradict the rebrand.)
- The live Databricks app resource remaining `geniefy-dev` is per D55 (out of scope) — correctly NOT touched; not a finding.

### regression — string-only; routing/ids/handlers unchanged
PASS.
- Nav `id` values preserved: `run · schema · history · library` (`App.tsx:19-22`). First tab `id: 'run'` intact, so existing routing works (label rename is decoupled from id).
- `View` type unchanged: `'run' | 'schema' | 'history' | 'library'` (`:17`); `useState<View>('run')` default (`:28`); all view dispatch (`view === 'run'/'history'/'library'/'schema'`, `:91-94`), `setView(n.id)` (`:66`), and `reset/open` → `setView('run')` (`:41,46`) key off **id**, never **label** — the label change has zero behavioral effect.
- `api.py` change is the `FastAPI(title=…)` (+ description) string only; no route, handler, signature, or logic touched.

## Findings

### HIGH severity
- none

### MEDIUM severity
- none

### LOW severity
- **Stale gitignored deploy-staging copy still reads "geniefy-v3"** (`app/geniefy_app/api.py:354` → `FastAPI(title="geniefy-v3", …)`, description without the tagline). This is NOT a U128 target and NOT a contradiction of the unit: `app/geniefy_app/` and `app/geniefy_core/` are **gitignored build artifacts** (`.gitignore:15-16`) that `deploy.sh:104-106` deletes (`rm -rf app/geniefy_core app/geniefy_app`) and re-copies fresh from `src/` (`cp -R src/geniefy_core src/geniefy_app app/`) on every deploy. The stale copy is a leftover from a pre-U128 deploy; because the App's `source_code_path` is `./app` and `app/main.py` imports `geniefy_app.api`, the *currently-staged* tree would serve the old FastAPI title — but the next `deploy.sh` run overwrites it with the correct `geniefy-lite` source. Informational only: re-run `deploy.sh` (or `rm -rf app/geniefy_app app/geniefy_core`) before relying on the live OpenAPI/FastAPI title. No source fix needed; the authoritative `src/geniefy_app/api.py` is correct.

### UNVERIFIED
- none

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 1 · UNVERIFIED: 0
Verdict: PASS-FINDINGS   (LOW-only; the LOW is an informational note on a regenerated build artifact, not a source defect — downstream may consume)
