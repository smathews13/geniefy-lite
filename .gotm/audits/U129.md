# Audit report — existence + structure/render + content/spec-fidelity + a11y/D23 + regression — 2026-06-14

## Target audited
File: `app/frontend/src/screens/HowItWorks.tsx` (new)
File: `app/frontend/src/App.tsx` (mounts `<HowItWorks/>` on the `!sessionId` empty state of the Table/'run' view; input example hint; nav `title` tooltips)
Unit: U129

## Oracle
- `.gotm/DECISIONS.md` → D55 (R4 rebrand + first-run onboarding; "fun" animated architecture diagram with flowing connections; restrained-but-delightful per D23)
- `.gotm/DECISIONS.md` → D23 (UX-delight direction; honesty guardrail; restrained-but-delightful; color paired with icon/label, never color alone)
- `.gotm/LEDGER.md` → U129 row

## Method
- Read both targets in full.
- Ran the SPA build: `cd app/frontend && npm run build` (`tsc --noEmit && vite build`) — clean.
- Ran the Python suite: `PYTHONPATH=src .venv/bin/python -m pytest -q` — 375 passed, 1 skipped.
- Cross-checked the five pipeline stages, SMIL animation, a11y attributes, the 4-step explainer, and the inline help hints against the U129 spec and D55/D23.
- Regression: no VCS in this tree (`git` reports "not a git repository"), so regression is assessed by direct inspection of App.tsx — confirming the review/run/questions/apply flow and other tabs are untouched.

## Findings

### HIGH severity
- None.

### MEDIUM severity
- None.

### LOW severity
- Decorative emoji glyphs used as in-SVG icons (`HowItWorks.tsx:17-21` node `icon` field: `▦ 🔍 ✨ ✓ 🚀`) are rendered as `<text>` inside an `aria-hidden="true"` group, which is correct for a11y, but emoji rendering inside SVG `<text>` is platform/font-dependent and may render inconsistently across OSes. Cosmetic only; the accessible name is carried entirely by the `<title>`, so no information is lost if a glyph fails to render. Fix if convenient (e.g., swap to inline SVG path icons), not required.
- The diagram caption "you approve every change" (`HowItWorks.tsx:111`) and the title/step-4 "nothing is written to Unity Catalog until you approve" assert the human-approval guardrail, which is honest and consistent with D23; no issue. Noted only as a reminder that this copy is load-bearing for the honesty guardrail and should track any future change to the apply flow.

### UNVERIFIED
- In-browser visual/animation rendering (does the SMIL flow actually animate, do traveling dots travel, does the layout look right at real widths, is reduced-motion respected by the platform) is NOT verified here — per the audit instructions, frontend verification is build + a separate web-devloop browser step. Flagged UNVERIFIED; expected to be closed by the dedicated web-devloop step. Note: the component does not implement a `prefers-reduced-motion` guard for the SMIL animation; SMIL does not auto-respect that media query, so motion plays regardless. The motion is small/looping and correctly marked decorative (`aria-hidden`), so this is within "restrained-but-delightful" and not raised as a finding, but the web-devloop reviewer may want to confirm it is acceptable.

## Detailed check results

### 1. Existence (PASS)
- `HowItWorks.tsx` exists at the ledger's stated path and exports `export function HowItWorks()` (`HowItWorks.tsx:105`).
- `App.tsx` imports it (`App.tsx:10`) and renders `<HowItWorks />` (`App.tsx:139`) inside the `view === 'run'` block, gated by `!sessionId` (`App.tsx:94, 96, 139-141`) — i.e., the Table-tab empty state only. Correct mount point.

### 2. Structure / render (PASS)
- `npm run build` is clean: `tsc --noEmit` passes (no type errors) and `vite build` succeeds (1047 modules, built in ~1.3s). No warnings attributable to the new code.

### 3. Content / spec-fidelity (PASS)
- Five pipeline stages, in spec order (`HowItWorks.tsx:16-22`):
  1. `Table / Schema` (Unity Catalog) — matches "Table/Schema"
  2. `Profile + context` (sample · lineage · queries) — matches "Profile+Context"
  3. `Reason` (Claude drafts) — matches "Reason · Claude"
  4. `Judge + you` (score · clarify · approve) — matches "Judge+You"
  5. `AI / Genie-ready` (documented + tagged), highlighted — matches "AI/Genie-ready"
- SMIL flowing connectors + traveling dots present: dashed connector `<line>` with animated `stroke-dashoffset` (`:55-58`), traveling `<circle>` dot with animated `cx` + `opacity`, staggered per segment via `begin={i*0.4s}` (`:59-64`). Connector lives in each gap between consecutive nodes (`:25`).
- SVG carries `role="img"` (`:32`) and a `<title id="flow-title">` describing the full pipeline (`:36-40`), referenced via `aria-labelledby="flow-title"` (`:33`). The spec's "role=img + title for a11y" is satisfied via the correct accessible-name pattern.
- 4-step explainer present (`STEPS`, `:94-103`; rendered as an ordered list `:118-131`): "Point it at your data" → "It gathers grounding" → "Claude drafts the docs" → "You review & apply".
- Help hints present in App.tsx:
  - Clickable `samples.tpch.orders` example — a `<button>` that sets the table input (`App.tsx:124-132`).
  - Schema-tab pointer — "To document an entire schema at once, use the **Schema** tab." (`App.tsx:132`); the explainer also points to the Schema tab (`HowItWorks.tsx:96`).
  - Nav `title` tooltips — `title={n.hint}` on each nav button (`App.tsx:67`), with per-tab hints defined in `NAV` (`App.tsx:18-23`).

### 4. a11y / D23 (PASS)
- All decorative motion/visuals marked `aria-hidden="true"`: the connector groups (`:54`), the node groups (`:72`), and the per-step emoji (`App.tsx:125` / `HowItWorks.tsx:125`). The accessible name is carried once by the SVG `<title>`.
- Color is always paired with an icon and/or label/text, never color-alone:
  - The highlighted final node (gradient fill, `:79-81`) is paired with the 🚀 icon + "AI / Genie-ready" label (`:82-84`).
  - The step number badges (indigo) are paired with the numeral + step icon + title text (`App.tsx:122-127`).
- Honest copy: states nothing is written to Unity Catalog until the user approves — in the SVG `<title>` ("Nothing is written to Unity Catalog until you approve it.", `:39`) and in step 4 ("Nothing is written to Unity Catalog until you say so — you are always in control.", `:102`), plus the caption "you approve every change" (`:111`). Consistent with the D23 honesty guardrail and human-approved apply flow.
- Restrained-but-delightful: a single small looping pipeline animation + static explainer cards; no gratuitous motion. Within D23/D55 intent.

### 5. Regression (PASS — empty-state only)
- All five screen imports remain in App.tsx (`:5-9`): `RunView`, `ReviewView`, `HistoryView`, `LibraryView`, `SchemaRunView` — none removed.
- The review/run/questions/apply flow logic is intact: `IN_FLIGHT`/`DONE` status sets (`:13-14`), `hasDrafts` (`:33`), `RunView` render (`:144`), failed-state card (`:145-149`), `ReviewView` render (`:150-152`), and the catch-all status card (`:153-158`) are all present and unchanged in shape.
- Other tabs untouched: `history`, `library`, `schema` renders (`:91-93`) unchanged.
- `<HowItWorks/>` is strictly additive and scoped to `view === 'run'` && `!sessionId` (`:94-141`); it does not appear once a session exists. No logic was removed from App.tsx — the unit only adds the import, the empty-state mount, the input example hint, and the nav `title` tooltips, consistent with the ledger ("supersedes U128 for App.tsx") and the U128 rebrand (header/tagline/tabs) it builds on.
- Python suite unaffected: 375 passed, 1 skipped (matches the expected 375 passed).

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 2 · UNVERIFIED: 1
Verdict: PASS-FINDINGS   (in-browser visual is UNVERIFIED by design, to be closed by the web-devloop step; the two findings are LOW/cosmetic and non-blocking)
