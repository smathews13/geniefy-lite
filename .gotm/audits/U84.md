# Audit report — content-claim / SQL-correctness / hermetic — 2026-06-13

## Target audited
Files:
- `src/geniefy_app/store.py` — `list_sessions`, `upsert_library_entry`, `list_library`, `_iso`
- `src/geniefy_app/apply.py` — `Applier._write_library` (called after a successful UC write)
- `src/geniefy_app/api.py` — `Store` protocol, `SessionService.list_sessions/list_library`, `get()` profile, routes `GET /api/sessions` + `GET /api/library`
- `tests/test_store.py`, `tests/test_apply.py`, `tests/test_api.py`
Unit: U84 — history + comment-library backend (E6/E7/E4/D46, D48 refinement)

## Oracle
`docs/design/LLD-amend-003.md` §E4/§E6/§E7 · `.gotm/DECISIONS.md` D46 + D48 (incl. refinement) · `migrations/001_init.sql` (schema ground-truth)

## Findings

### HIGH severity
- None.

### MEDIUM severity
- None.

### LOW severity
- None.

### UNVERIFIED
- None.

## Detailed check results

**1. SQL correctness vs schema — PASS.** Every column/table referenced exists in `001_init.sql`:
  - `list_sessions` (store.py:193-207) selects `s.id, s.uc_catalog, s.uc_schema, s.uc_table, s.status, s.created_by, s.created_at, s.updated_at` from `geniefy.sessions` — all present (migration lines 99-114). The count subqueries hit `geniefy.column_drafts (session_id, apply_status)` (lines 156, 170). The applied-count subquery uses `c.apply_status = 'applied'` (store.py:199) — `'applied'` is a valid `apply_status` enum value (migration line 38). The `table` filter composes `s.uc_catalog||'.'||s.uc_schema||'.'||s.uc_table` from real columns.
  - `upsert_library_entry` (store.py:217-256) targets `geniefy.comment_library` columns `scope, match_key, canonical_comment, conditional_fields, tags, source_session_id, source_table_ref, usage_count, approved_by, approved_at, updated_at` — all present (migration lines 183-197); the INSERT seeds `usage_count` to literal `1` and the UPDATE bumps `usage_count = usage_count + 1`.
  - `list_library` (store.py:258-288) selects `id, scope, match_key, canonical_comment, tags, usage_count, source_table_ref, approved_by, updated_at` from `geniefy.comment_library` — all present. No non-existent column/table found.

**2. Injection-safe — PASS.** All user-supplied values are bound `%s` params:
  - `list_sessions`: `status`, `table`, `limit`, `offset` all bound as `(status, status, table, table, limit, offset)` (store.py:206); the only interpolated tokens are `{s}` (the `_qualify`-validated schema, store.py:40-43) and the `||'.'||` literal composition of trusted column identifiers. `limit` clamped to `[1,200]`, `offset` floored at 0 (store.py:189-190).
  - `list_library`: `scope`, `limit`, `offset` bound (store.py:275); `limit` clamped to `[1,500]`, `offset` floored (store.py:262-263).
  - `upsert_library_entry`: `scope`, `match_key`, `canonical_comment`, `conditional_fields`, `tags`, `source_session_id`, `source_table_ref`, `approved_by` all bound in both the SELECT-existing, UPDATE, and INSERT branches (store.py:230, 241-242, 251-252). No f-string interpolation of any user value. `test_list_sessions_maps_rows_and_binds_filters` and `test_list_sessions_clamps_limit_and_offset` assert the exact bound-param tuple and the clamp, so the property is test-enforced.

**3. Library write is best-effort + SP — PASS.** `Applier._write_library` (apply.py:160-173): guards with `getattr(self._store, "upsert_library_entry", None)` → returns silently if the store lacks the method (older fakes); wraps the call in `try/except Exception: pass` so a raising upsert never fails the apply. It writes via `self._store` (the SP Lakebase connection injected at construction) — **not** the OBO user runner (`sql`/`self._make_user_sql` are used only for the UC read+write DDL, never for the library). It is called only at apply.py:157 — *after* `draft.apply_status = ApplyStatus.APPLIED` (line 145) and the audit append (line 151), on the genuine-write path only; conflict (line 120), empty-over-existing (line 129), empty-over-empty noop (line 127), idempotent noop (line 135), and failed-write (line 142) branches all `return` before reaching it. Independently probed: a raising `upsert_library_entry` and a store lacking the method both leave `applied == 1`. `test_apply_records_applied_comment_in_library`, `test_apply_noop_or_conflict_does_not_write_library`, and the OBO tests (`test_obo_apply_uses_user_token_runner_not_the_sp` asserts `store.audits[-1]["actor"] == "user"` while the library still goes through the SP store) all pass. Conforms to D48 refinement (library write = SP).

**4. get() exposes profile — PASS.** `SessionService.get` returns `"profile": state.profile or {}` (api.py:177). `test_session_view_exposes_per_column_profile` asserts `g["profile"]["table"]["row_count"] == 1`. Satisfies E4.

**5. Routes — PASS.** `GET /api/sessions` (api.py:258-262; `status/table/limit/offset` → `service.list_sessions`) is registered *before* `GET /api/sessions/{session_id}` (api.py:269-274), so the static path takes precedence — no collision. `GET /api/library` (api.py:264-267; `scope/limit/offset` → `service.list_library`). The `Store` protocol (api.py:53-58) declares both `list_sessions` and `list_library`. `test_list_sessions_endpoint_returns_history` asserts `store.list_args == {"status": "ready_for_review", "table": "samples.tpch.orders", "limit": 10, "offset": 0}` — confirming query params flow to the store; `test_list_library_endpoint` covers the library route.

**6. Hermetic — PASS.** `PYTHONPATH=src python3 -c "import sys, geniefy_app.store, geniefy_app.apply; ..."` → `leaked: none` (no `databricks`/`openai`/`psycopg2` in `sys.modules`).

**7. Tests bite + pass — PASS.** `tests/test_store.py tests/test_apply.py tests/test_api.py` → **60 passed**. Full suite `tests/` → **314 passed** (matches expected). The store tests assert bound params not interpolation (`test_list_sessions_maps_rows_and_binds_filters`), the clamp (`test_list_sessions_clamps_limit_and_offset`), and both upsert branches: insert-when-absent (`test_upsert_library_inserts_when_absent`, fetch_queue `[None]`) and bump-when-present (`test_upsert_library_bumps_usage_when_present`, fetch_queue `[("lib-1",)]`, asserts `usage_count = usage_count + 1` and `WHERE id = "lib-1"`). The apply→library write is covered by `test_apply_records_applied_comment_in_library` and `test_apply_noop_or_conflict_does_not_write_library`.

**8. Ledger hygiene — PASS.** U84 (LEDGER row 110) lists its 6 files (`store.py + api.py + apply.py + test_store.py + test_apply.py + test_api.py`). U55 carries `store.py (superseded by U84)` + `test_store.py (superseded by U84)`; U85 carries `apply.py (superseded by U84)` + `test_apply.py (superseded by U84)`; U83 carries `api.py (superseded by U84)` + `test_api.py (superseded by U84)` — exactly 6 `(superseded by U84)` annotations on the right rows/files.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 0 · UNVERIFIED: 0
Verdict: PASS

sql-matches-schema: y · injection-safe: y · library-best-effort-SP: y · full-suite: 314 passed
