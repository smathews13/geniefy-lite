# Audit report — content-claim + cross-reference + mutation-check — 2026-06-23

## Target audited
Files:
- `src/geniefy_app/api.py` (`get_schema_run`, `_CONFIDENCE_REVIEWABLE`)
- `src/geniefy_app/schema_run.py` (`_SP_ACTOR`, `run_schema`)
- `src/geniefy_core/reasoner.py` (`_GROUNDING_RULES` + both call sites)
- `src/geniefy_app/store.py` (`update_schema_run` SET-clause)
- `migrations/004_schema_runs.sql` (confirm untouched)
- `tests/test_api.py::test_get_schema_run_summarizes_only_reviewable_sessions`
Unit: U159

## Findings

### HIGH severity
- (none)

### MEDIUM severity
- **Supersession cross-ref mis-attributed for `schema_run.py`** (`.gotm/LEDGER.md` U159 row): the row reads
  ``src/geniefy_app/schema_run.py` (supersedes U119)``, but **U119 never authored `schema_run.py`**.
  U119's outputs are `databricks.yml` + `jobs-bundle/databricks.yml` + `deploy_jobs.sh` +
  `jobs/schema_run_entry.py` + `api.py` (job-name trigger only) — it "revises U111" at the Job-bundle
  level. The only unit that authored `src/geniefy_app/schema_run.py` (the driver file U159 actually edits
  for the `_SP_ACTOR` change) is **U111** (U111 row Outputs: ``src/geniefy_app/schema_run.py` (driver,
  new)``). So the note should read `(supersedes U111)`. The code change itself is correct; this is a
  provenance/cross-ref-integrity defect in the ledger row that would mislead a future reader tracing
  schema_run.py history. The other three supersession notes are well-formed: api.py←U157 (U157 is the last
  api.py author; its own audit even flagged the get_schema_run O(n) LOW that U159 closes), reasoner.py←U116
  (U116 is the last reasoner.py author of `_GROUNDING_RULES`), store.py←U155 (U155 is the last store-adjacent
  api.py author; store.py's update_schema_run last lived under U109 — see note below).
  - Suggested fix unit: append a 1-line ledger correction changing U159's `schema_run.py (supersedes U119)`
    to `(supersedes U111)` (append-only erratum row; do not rewrite history beyond the supersession token).

### LOW severity
- **U109-L2 description is slightly loose** (spec text vs. `store.py:438-456`): the spec says the SET-clause
  "was `;`-joined." The builder produces a comma-joined clause (`', '.join(sets)`) — the only valid form for
  a multi-column SQL `UPDATE`; a literally `;`-joined SET clause would have been invalid SQL. The realized
  change is one-`sets.append()`-per-line clarity with byte-identical emitted SQL (`status = %s, total_tables
  = %s, job_run_id = %s`). Behavior unchanged; the spec wording is the only imprecision. No action needed.

### UNVERIFIED
- **U116 prompt reword — live generation behavior**: `_GROUNDING_RULES` now reads "Return ONLY the JSON
  object, with no extra prose outside it (the comment fields themselves are prose — that is expected)"
  (`reasoner.py:46-47`). The JSON-only contract is preserved verbatim ("Return ONLY the JSON object"); the
  added clause only clarifies that prose *inside* the comment fields is expected — it REDUCES the prior
  tension with the prose-comment requirement and does not loosen the JSON requirement. Both call sites (table
  prompt `reasoner.py:181`, column prompt `reasoner.py:214`) interpolate the shared `_GROUNDING_RULES`, so
  both get the reword. This ships committed-but-undeployed; the actual model output under the new wording can
  only be confirmed at the next live run. Flag for re-verification at next deploy (consistent with U116's own
  UNVERIFIED-live disposition).

## Verification evidence
1. **Perf filter correctness (main change):** `_CONFIDENCE_REVIEWABLE = {"ready_for_review",
   "awaiting_input"}` (api.py:81). Cross-checked against `SessionStatus` (state.py:36-49): the full set is
   created/profiling/gathering_context/reasoning/awaiting_input/ready_for_review/applying/applied/paused/
   failed/cancelled. The two reviewable states with settled, stable drafts are exactly ready_for_review and
   awaiting_input; in-progress (profiling/gathering_context/reasoning/applying) and failed/cancelled/applied
   are correctly excluded. `get_schema_run` (api.py:398-401) loads + summarizes ONLY when
   `s.get("status") in _CONFIDENCE_REVIEWABLE`, else `confidence_summary=None` and no `.load()` call. Correct.
2. **Mutation-check (test is a real guard):** replaced the filtered load with an unconditional
   `self.store.load(sid) if sid else None` and ran the new test → FAILED on
   `assert loaded == ["rev"]` (`AssertionError: ['rev','inprog'] == ['rev']`). Restored; test passes again.
   The test asserts (a) reviewable → confidence_summary present with approvable==2, (b) in-progress → None,
   (c) `loaded == ["rev"]` (in-progress NOT loaded). It would fail if the filter were removed. Non-tautological.
3. **No semantic change to confidence_summary:** `_confidence_summary` (api.py:104-130) untouched —
   rollup (0.5·table + 0.5·col mean), buckets, approvable=len(_high_confidence_targets), weakest unchanged.
   Only WHICH sessions are summarized changed.
4. **U111 / U109-L2:** `_SP_ACTOR == "app-service-principal"` confirmed at runtime (value unchanged; same as
   `api.py:472` `_actor()`); `run_schema` default `created_by=_SP_ACTOR` (schema_run.py:23,28). SET-clause
   emits identical SQL (verified by reconstruction).
5. **Migration untouched (U109-L1):** `migrations/004_schema_runs.sql` comment still references "The Job
   (U111)"; the U109 audit's "no action needed" on the migration comment is correctly honored — U159 left it
   alone. `git log -- migrations/004_schema_runs.sql` shows only the initial commit.
6. **Suite:** `PYTHONPATH=src .venv/bin/python -m pytest -q` → **393 passed, 1 skipped, 1 warning** (matches
   the claim). The single deprecation warning is the pre-existing Starlette/httpx testclient notice,
   unrelated to U159.

## Summary
HIGH: 0 · MEDIUM: 1 · LOW: 1 · UNVERIFIED: 1
Verdict: PASS-FINDINGS   (HIGH ⇒ FAIL; MEDIUM/LOW-only ⇒ PASS-FINDINGS; clean ⇒ PASS)
