# Audit report — code (negative test for optional token env vars) — 2026-06-12

## Target audited
File: `tests/test_app_config.py` — the added `test_invalid_optional_token_var_reported`
(parametrized over `GENIEFY_MAX_INPUT_TOKENS_PER_CALL` and `GENIEFY_SUMMARY_TARGET_TOKENS`).
Unit: U72 (fix the U44-audit LOW: no bad-value negative for the optional token env vars).

## Method
Independent, mechanical per-unit audit (auditor did not author U72). No fixes, no
ledger/source edits. Read target (`tests/test_app_config.py:104-110`) + oracle
(`.gotm/audits/U44.md` LOW finding §70-75; `src/geniefy_app/config.py` `from_env`/`num()`).
Ran the suite and mutation-tested the new test against a `/tmp` scratch copy of `config.py`.

RUN: `cd … && PYTHONPATH=src /tmp/geniefy_pgvenv/bin/python -m pytest tests/test_app_config.py -q`
→ **13 passed in 0.02s** (was 11 pre-U72; the new parametrized test contributes 2).

## Checks

### 1. Test bites — PASS
The new `test_invalid_optional_token_var_reported` (`tests/test_app_config.py:104-110`):
- **Parametrized over BOTH vars** — `@pytest.mark.parametrize("var", ["GENIEFY_MAX_INPUT_TOKENS_PER_CALL",
  "GENIEFY_SUMMARY_TARGET_TOKENS"])` (line 104), so both LOW-flagged vars are covered.
- **Routes through `num(..., int, ...)`** — sets each var to `"lots"` (a truthy non-int). In
  `config.py:66-67`, `max_in`/`summ_target` invoke `num(key, int, None)` only when `env.get(key)`
  is truthy; `"lots"` is truthy, so the `num()` path fires. Inside `num()` (`config.py:52-60`),
  `int("lots")` raises `ValueError` → line 59 appends `f"{key}={raw!r} is not a valid {cast.__name__}"`
  = `"GENIEFY_…='lots' is not a valid int"` → line 88-89 raises `ConfigError`.
- **Asserts both the var name AND "not a valid int"** — line 110:
  `assert var in str(ei.value) and "not a valid int" in str(ei.value)`. Both substrings are present
  in the error message produced by `num()`.

**Mutation probe (neuter the error path, then revert):** on a `/tmp` scratch copy, replaced the
`errors.append(f"{key}={raw!r} is not a valid {cast.__name__}")` in `num()` with a bare `pass`.
Result: **2 failed** — both parametrized cases failed with `DID NOT RAISE <ConfigError>`. So the
test genuinely depends on the `num()` bad-value error path; it is not vacuous. Scratch copy deleted.

### 2. RUN — PASS
`PYTHONPATH=src /tmp/geniefy_pgvenv/bin/python -m pytest tests/test_app_config.py -q` →
**13 passed in 0.02s**.

## Findings

### HIGH severity
- none.

### MEDIUM severity
- none.

### LOW severity
- none.

### UNVERIFIED
- none. (Oracle + infra-free test path fully available; mutation probe ran locally.)

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 0 · UNVERIFIED: 0
pytest: 13 passed
test-bites: yes (mutation: 2 failed when num() error path neutered)
closes-U44-LOW: yes (both `GENIEFY_MAX_INPUT_TOKENS_PER_CALL` and `GENIEFY_SUMMARY_TARGET_TOKENS`
now have a bad-value negative asserting var name + "not a valid int")
Verdict: PASS   (no findings)
