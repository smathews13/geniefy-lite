# Audit report — content-claim / decision-fidelity — 2026-06-12

## Target audited
Files:
- `src/geniefy_app/apply.py` (Applier OBO runner selection + fail-fast; `sql` threaded)
- `src/geniefy_app/providers.py` (`make_obo_sql_execute` + extracted `_rows_from_statement_response`)
- `src/geniefy_app/api.py` (`build_service` wires `make_user_sql`; `/apply` forwards `identity.access_token`)
- `tests/test_apply.py`, `tests/test_apply_wiring.py`
Unit: U85

Oracle: `.gotm/DECISIONS.md` → D48 + Refinement; `docs/design/LLD-amend-004.md` §A5; `docs/design/LLD-amend-003.md` §E9; `.gotm/LEDGER.md` (U85/U55/U91/U77 rows).

## Findings

### HIGH severity
- None.

### MEDIUM severity
- None.

### LOW severity
- None.

### UNVERIFIED
- **Live OBO write on a writable target** is not exercised by the hermetic suite (by design — `make_obo_sql_execute` lazy-imports the real SDK and is integration-verified, consistent with D36 and the other `databricks_*` factories). LLD-amend-004 §A5 and U78/U89 already route the live re-verification (R2 redeploy + the `sql` user-authorization operator step), so this is sanctioned, not a gap. No `make_obo_sql_execute` unit test exists in `tests/test_providers.py`, matching the existing non-hermetic-factory convention (no finding raised).

## Detailed check results

1. **OBO write path correct (y).** `apply.py:81-87` defaults `sql = self._sql` (SP), and when `make_user_sql` is set AND a token is passed builds `sql = self._make_user_sql(access_token)` (user runner); this `sql` is threaded into `_apply_one`→`_read_live`/`_write` (lines 106/115/140). `test_obo_apply_uses_user_token_runner_not_the_sp` confirms `seen==["user-tok"]`, the user runner wrote `ALTER COLUMN`, `sp_wh.ddl==[]`, audit actor == `user@x.com`.

2. **No silent SP fallback (y).** `make_user_sql` set + no token → `obo_missing=True` (apply.py:83-87); each applyable item (after the `_APPLYABLE` filter at line 100) is marked `ApplyStatus.FAILED` with `_OBO_MISSING` ("on-behalf-of not enabled… add the `sql` user-authorization scope") and NO `_write` runs on any runner (apply.py:102-104). `test_obo_missing_token_fails_each_item_without_writing` confirms `applied==0`, 2 failed items containing "on-behalf-of", `sp_wh.ddl==[] and user_wh.ddl==[]`, draft FAILED, zero "applied" audit rows. Honors D48 / LLD-004 §A5 fallback #1.

3. **Back-compat / hermetic preserved (y).** No `make_user_sql` → `obo_missing` stays False, SP `sql` used regardless of token (`test_no_factory_uses_injected_runner_regardless_of_token`). `_read_live`/`_write`/`_apply_one` take `sql` as a parameter; `grep` shows `self._sql` only at the `__init__` assignment (line 67) and line 81 — no stale reference inside `_apply_one`. All pre-existing apply tests (conflict/noop/escaping/per-item-failure/checkpoint/audit/targets) pass unchanged.

4. **providers OBO factory (y).** `make_obo_sql_execute` (providers.py:160-188) resolves `resolved_host` ONCE outside the `make` closure (lines 174-175 — closes the U90 LOW), builds a token-authed `WorkspaceClient(host=resolved_host, token=access_token)` per call (line 178), reuses `_rows_from_statement_response` (lines 181-184). Token is passed to the client only — not stored on any attribute, not logged. SP `databricks_sql_execute` (lines 143-157) is unchanged. SDK import is lazy (line 172).

5. **Route wiring (y).** `/apply` (api.py:247-255) depends on `_identity` and forwards BOTH `created_by=identity.actor` AND `access_token=identity.access_token`. `build_service` (api.py:336-339) wires `make_user_sql=lambda tok: WarehouseSqlRunner(obo(tok))` with the SP `sql` runner as the default. `test_apply_wiring` asserts `[("sid1","user@x.com","tok-xyz")]` with headers and `[("sid1","anonymous",None)]` without.

6. **Hermetic (y).** `PYTHONPATH=src python3 -c "import geniefy_app.providers, geniefy_app.apply…"` → **leaked: none** (no databricks/openai/psycopg2 in sys.modules).

7. **Tests pass.** `tests/test_apply.py tests/test_apply_wiring.py` → **21 passed**. Full suite `tests/` → **295 passed** (matches the expected 295).

8. **Ledger hygiene (y).** U85 row (LEDGER line 111) lists all 5 files + `supersedes U55 apply.py/test_apply.py, U91 api.py/test_apply_wiring.py, U77 providers.py`. Matching supersede annotations present on U55 (line 81: `apply.py … superseded by U85`, `test_apply.py (superseded by U85)`), U91 (line 117: `api.py … superseded by U85`, `test_apply_wiring.py (superseded by U85)`), U77 (line 103: `providers.py (superseded by U85)`).

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 0 · UNVERIFIED: 1 (sanctioned — live OBO re-verify routed to U78/U89)
Verdict: **PASS**   (no HIGH/MED/LOW; the single UNVERIFIED is an explicitly-deferred live-infra check, already tracked)
