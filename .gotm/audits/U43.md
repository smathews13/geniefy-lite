# Audit report — content+code (config loader) — 2026-06-12

## Target audited
File: `src/geniefy_app/config.py`
File: `tests/test_app_config.py`
Unit: U43 (tests authored under U44)

## Method
Independent mechanical audit (auditor did not author the unit). Read target + oracle
(`docs/design/LLD-nfr-plan.md` U24 §1; `DECISIONS.md` D33/D34/D36/D12; `geniefy_core/state.py`
`RunConfig`). Ran the unit tests, probed edge cases directly, and **mutation-tested the
test suite** (injected 5 defects to confirm they're caught — meaningfulness, not ceremony).

RUN: `PYTHONPATH=src pytest tests/test_app_config.py -q` → **11 passed in 0.01s**.

## 5-point + code checklist

1. **existence** — PASS. `src/geniefy_app/config.py`, `tests/test_app_config.py`, and the
   package `src/geniefy_app/__init__.py` (named in the ledger row) all exist at the stated paths.
2. **spec match** — PASS. `from_env(mapping)` loads the documented `GENIEFY_*` schema (U24 §1)
   into infra coords (`model_endpoint`, `warehouse_id`, `pg_host/database/schema`, `secret_scope`)
   + a `RunConfig` for the core + MCP-provider registrations, and **fails fast collecting ALL
   problems at once**. Every env var in the U24 §1 table is handled.
3. **cross-reference integrity** — PASS. D33 (app.yaml single config surface / fail-fast),
   D34 (MCP providers via config JSON, array), D36 (`src/geniefy_app/` package), D12 (no
   hardcoded infra — all knobs parameterized) are honored and accurately reflected. The
   docstring's U24 §1 / D33 / D34 citations trace to real, matching oracle entries.
4. **internal consistency** — PASS. `cfg.model_endpoint == cfg.run_config.model_endpoint`
   (single source); defaults in `config.py` match `RunConfig`'s defaults in `state.py`
   (keep 0.75, profile 50, reason 25, ctx 4000, summarize True); `pg_schema`/`secret_scope`
   default `"geniefy"` consistent with D30.
5. **decision fidelity** — PASS. Fail-fast aggregates rather than raising on first error
   (D33 "no limping along"); MCP must be a JSON **array** (D34); hermetic, no I/O, takes a
   mapping (D36 unit-testable / D12).

**Code-specific checks (RUN + probe):**
- **fail-fast collects ALL problems** — verified: missing-required + bad float + bad int +
  bad MCP JSON + bad mode all surface in one `ConfigError`. The `num()` helper records the
  error AND returns a default so collection continues (confirmed by probe + mutation).
- **type coercion** — int/float via `num()`, bool via `_bool` (1/true/yes/on, case-insensitive),
  csv via `_csv` (trim + drop-empty), mode via `SessionMode(...)`, MCP via `json.loads`. All
  correct, incl. blank-required-treated-as-missing (`.strip()`).
- **defaults** — all match the U24 §1 / `RunConfig` contract; verified with a minimal env.
- **hermetic** — verified `from_env` works against a plain dict with `os.environ` emptied; no I/O.
- **non-list MCP rejected** — verified for object, number, string, bool, and `null` (all rejected).
- **meaningfulness (mutation test)** — all 5 injected defects were **CAUGHT** by the suite:
  swallowed missing-required (4 fail), wrong `keep_threshold` default (1 fail), wrong
  `profile_batch` default (1 fail), non-list MCP accepted (1 fail), num-errors not collected
  (2 fail). A swallowed missing-required, a wrong default, and a non-collected error are all caught.

## Findings

### HIGH severity
- none.

### MEDIUM severity
- none.

### LOW severity
- **Untested invalid-value path for `GENIEFY_MAX_INPUT_TOKENS_PER_CALL` / `GENIEFY_SUMMARY_TARGET_TOKENS`**
  (`config.py:66-67`). The suite tests their *valid* parse (`test_full_env_parses_all_types`) and
  the invalid path for the other numerics, but never feeds these two a non-int. I confirmed by
  direct probe that a bad value **is** collected into `ConfigError` correctly — so this is a test-
  coverage gap, not a defect. Add two one-line negatives in the test-hardening sweep.
- **Redundant double-guard on the same two fields** (`config.py:66-67`):
  `num(key, int, None) if env.get(key) else None`. `num()` already returns its `default` (`None`)
  when the key is absent/empty, so the outer ternary is dead code. Harmless; simplify for clarity.
- **`GENIEFY_MODE`, `GENIEFY_TEMPLATE_ID`, `GENIEFY_ENABLED_PROVIDERS` loaded but absent from the
  U24 §1 schema table.** They are legitimate `RunConfig` fields and the loader is a correct
  superset of the documented table; the doc table is the lagging artifact, not the code. Optional:
  note these in U24 §1 (a living-doc touch-up) for schema completeness. Not blocking.

### UNVERIFIED
- none. (Oracle and infra-free test path were both fully available; no live infra needed.)

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 3 · UNVERIFIED: 0
pytest: 11 passed
Verdict: PASS-FINDINGS   (no HIGH/MEDIUM; 3 LOW-only ⇒ PASS-FINDINGS)
