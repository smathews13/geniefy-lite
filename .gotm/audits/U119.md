# Audit report — build / structure + decision-fidelity + render — 2026-06-14

## Target audited
Files:
- `databricks.yml` (app bundle — `geniefy_setup` reverted to migrate-only)
- `jobs-bundle/databricks.yml` (new — `geniefy-jobs` bundle / `geniefy_schema_run`)
- `deploy_jobs.sh` (new — stages core+entrypoint, deploys the jobs bundle)
- `jobs/schema_run_entry.py` (`here`-first sys.path)
- `src/geniefy_app/api.py` (`_make_run_schema_job` resolves `geniefy_schema_run`)
Unit: U119
Oracle: `.gotm/DECISIONS.md` D54 / D51 §6 / D48·U77·U78 (grant-safety) / D37 (dev-branch endpoint) / U117 ; `deploy.sh` (app `--code-only` path + staging pattern) ; `.gotm/LEDGER.md`

## Findings

### HIGH severity
- None.

### MEDIUM severity
- **Stale module docstring in `jobs/schema_run_entry.py` (lines 1–8) contradicts the unit it implements.** The header still reads "(U111, D51 / option-b)", "The `schema_run` task of the EXISTING `geniefy_setup` Job (no new bundle resource — grant-safe)", "It runs AFTER the `migrate` task" and "the plain post-deploy `bundle run geniefy_setup` migrate-only case is unaffected." Under U119/D54 this entrypoint now lives in its OWN `geniefy-jobs` bundle as the standalone `geniefy_schema_run` job — there is no co-located `migrate` task, and the no-op case is now a plain `geniefy_schema_run` run with an empty `schema_run_id`, not `bundle run geniefy_setup`. The runtime sys.path block (lines 30–36) WAS correctly updated to the D54/U119 `here`-first layout, so this is documentation drift only — no behavior bug — but a reader of the docstring is actively misled about where this runs. The inline comment on line 670 of `api.py` (`…no-ops when schema_run_id is empty… migrate-only `bundle run` is unaffected (U111)`) carries the same stale framing.
  - Suggested fix unit: append a doc-only follow-on that rewrites the `schema_run_entry.py` module docstring (and the api.py:670 comment) to describe the standalone `geniefy_schema_run` job per D54/U119.

### LOW severity
- **`.gitignore` does not cover the jobs-bundle staged paths.** `.gitignore:15–16` ignores the app-staging dirs `app/geniefy_core/` + `app/geniefy_app/`, but `deploy_jobs.sh` (lines 28–30) stages `jobs-bundle/geniefy_core/`, `jobs-bundle/geniefy_app/`, and `jobs-bundle/schema_run_entry.py`, none of which are gitignored. The CHECK item asserts `jobs-bundle/databricks.yml` `sync.include` "force-includes the gitignored staged dirs" — the `sync.include` (lines 16–19) is present and correctly names all three paths, but the premise that they are gitignored is false. Functional impact is nil (the `sync.include` makes them upload regardless; and since they're not ignored, DABs would sync them anyway), but the staged build artifacts can be accidentally committed. Mirror the app pattern by adding `jobs-bundle/geniefy_core/`, `jobs-bundle/geniefy_app/`, `jobs-bundle/schema_run_entry.py` to `.gitignore`.
  - Suggested fix unit: append the three jobs-bundle staged paths to `.gitignore` (one-line hygiene follow-on).

### UNVERIFIED
- **Live `bundle deploy` of `geniefy-jobs` is out of scope.** That the separate bundle actually deploys cleanly (CLI resolves `${var.*}` defaults, the job is created as `geniefy-schema-run-dev`, and a real `run_now` reaches the entrypoint) is integration-only and routed to U113 per the unit spec. Statically the bundle is well-formed; live deploy/run remains unverified here.

## Verification performed
- **`geniefy_setup` reverted cleanly (PASS).** `databricks.yml` parses; `resources.jobs` = `{geniefy_setup}` only; `geniefy_setup.tasks` = `[migrate]`, `environments` = `[migrate]`, **no `parameters` key**, no `schema_run` task/env. `resources.apps.geniefy` (with the `sql-warehouse` resource) + `variables` + `targets` unchanged. No dangling references.
- **Separate-bundle correctness (PASS).** `jobs-bundle/databricks.yml` parses; `bundle.name: geniefy-jobs`; `resources` = `{jobs}` only, `jobs` = `{geniefy_schema_run}` — **no `apps` resource** → a `bundle deploy` of it cannot reconcile/affect the app (grant-safe per D54/U77/U78/D48). Task `parameters` order == job-level `parameters` order == entrypoint argv unpack order: `schema_run_id, catalog, schema, filters, model_endpoint, warehouse_id, pg_host, pg_database`. `schema_run` environment deps = `[psycopg2-binary, databricks-sdk, openai, pyyaml]` (all four present).
- **pg_host defaults to the DEV branch, NOT production (PASS — the named HIGH risk is absent).** `jobs-bundle` `pg_host` default = `ep-curly-sunset-d2sz2asi.database.us-east-1.cloud.databricks.com` = `projects/geniefy/branches/dev/endpoints/primary` (per `.gotm/DECISIONS.md` D37, `migrations/DEV_VERIFY.md:11–12`, and the U107 live finding that the dev app binds `branches/dev`). The production endpoint `ep-blue-smoke-d2yetcmv…` is correctly NOT used. Matches the app binding (U117) as required.
- **Staging script (PASS).** `deploy_jobs.sh` has `set -euo pipefail`; `bash -n` clean. Stages `src/geniefy_core` + `src/geniefy_app` + `jobs/schema_run_entry.py` into `jobs-bundle/` (rm -rf then cp -R, prunes `__pycache__`), then `( cd "$JB" && databricks bundle deploy -t "$TARGET" )`. `sync.include` force-includes exactly the three staged paths so the bundle is self-contained (entrypoint `./schema_run_entry.py` + `import geniefy_app` resolve on the cluster). `migrations/` is correctly NOT staged — the entrypoint references migrations only in prose, never imports them.
- **Entrypoint sys.path (PASS).** `here` candidate is checked FIRST (`for cand in (here, os.path.join(here,"..","app"), os.path.join(here,".."))`), with the older `../app` / `..` paths as fallback; no change to the no-op guard (`if not schema_run_id: return 0`) or the 8-arg unpack.
- **Trigger resolution (PASS).** `_make_run_schema_job` resolves by `getattr(config,"schema_run_job_id",None)` (safely None — not an AppConfig field) then by name `"schema-run" in name or "schema_run" in name` (lower-cased). Verified: matches `geniefy-schema-run-dev/-prod` and `[dev rohit] geniefy-schema-run-dev`; does NOT match `geniefy-setup-dev/-prod`. The 4 run-specific `job_parameters` (`schema_run_id, catalog, schema, filters`) are the only overrides; config params fall to bundle-var defaults. `build_service` wires `run_schema_job=_make_run_schema_job(config)`.
- **No grant-safety regression (PASS).** `deploy.sh` `bash -n` clean; the `--code-only` path (150–160) + grant short-circuit (190–191) untouched. The app bundle no longer carries the job, so a future app `bundle deploy` neither adds nor removes it.
- **Tests/compile (PASS).** `PYTHONPATH=src .venv/bin/python -m pytest -q` → **365 passed, 1 skipped** (matches the expected count; the trigger change is integration/fakes). `py_compile` of `api.py` + `jobs/schema_run_entry.py` clean. `bash -n` clean for both shell scripts. Both YAMLs parse.

## Summary
HIGH: 0 · MEDIUM: 1 · LOW: 1 · UNVERIFIED: 1
Verdict: PASS-FINDINGS   (no HIGH — grant-safety is genuinely achieved: the app bundle is reverted to migrate-only with no job, and the separate `geniefy-jobs` bundle declares ONLY the job/no app resource; param order matches the entrypoint argv; pg_host correctly defaults to the dev branch; YAML/bash/py all clean; 365 passed/1 skipped. One MEDIUM = stale `schema_run_entry.py` module docstring (+ api.py:670 comment) still describing the U111 option-b "task of geniefy_setup" framing; one LOW = jobs-bundle staged paths not added to `.gitignore`. Live `bundle deploy` of the jobs bundle → UNVERIFIED, routed to U113.)
