# Audit report — code (tests) — 2026-06-12

## Target audited
File: `tests/test_gate.py` (U34 — Gate-test hardening; extends the original U31 suite)
Unit: U34
Focus: `test_ambiguous_unused_but_not_high_null_does_not_trip`, `test_high_null_unused_but_not_ambiguous_does_not_trip`, `test_apply_trips_the_table_draft`

Oracle: `.gotm/audits/U30.md` (the MED + LOW being closed) · `src/geniefy_core/gate.py` (module under test — `route_item`, `hard_trips`, `apply`) · `src/geniefy_core/state.py` (U27 spine types).

## Method
Read target + oracle. Verified the three new tests against `gate.py`'s actual predicates and the `state.py` spine types they reference. **Ran** `PYTHONPATH=src /tmp/geniefy_pgvenv/bin/python -m pytest tests/test_gate.py -q` → **18 passed in 0.02s** (15 original U31 + 3 U34 additions). Mutation-tested the suite on a scratch copy to confirm the additions are non-vacuous and actually close the U30 findings; the real `gate.py` was never modified (mutations applied only to `/tmp/u34_mut`, which was deleted afterward).

## Findings

### HIGH severity
- None.

### MEDIUM severity
- None.

### LOW severity
- None.

### UNVERIFIED
- None. All oracle files present and readable; the test venv (`/tmp/geniefy_pgvenv/bin/python`) exists and ran the suite.

## Detailed check results

**1. Negative cases are right (closes the U30 MED) — PASS.**
The trip predicate in `gate.py:78` is `if signals.ambiguous_name and not signals.has_usage_evidence and signals.high_null_fraction:` — a three-way conjunction, so each sub-condition is independently load-bearing. The U30 MED was that only one breaking negative (usage evidence, `test_ambiguous_but_used_does_not_trip`) was tested; the `high_null_fraction` and `ambiguous_name` axes were unguarded (Mutations 3 and 9 survived in the U30 audit).

- `test_ambiguous_unused_but_not_high_null_does_not_trip` (`test_gate.py:95-98`) — `HardSignals(ambiguous_name=True, has_usage_evidence=False, high_null_fraction=False)` asserts `.keep`. This is exactly the missing `high_null_fraction=False` axis. **Mutation A** (drop `signals.high_null_fraction` from line 78) → this test now FAILS at `test_gate.py:98`, where the U30 audit's Mutation 3 had survived 15/15. Closed.
- `test_high_null_unused_but_not_ambiguous_does_not_trip` (`test_gate.py:101-104`) — `HardSignals(ambiguous_name=False, has_usage_evidence=False, high_null_fraction=True)` asserts `.keep`. The missing `ambiguous_name=False` axis. **Mutation B** (drop `signals.ambiguous_name` from line 78) → this test now FAILS at `test_gate.py:104`, where the U30 audit's Mutation 9 had survived. Closed.

Both negative cases are routed at confidence 0.99, so the only thing that can keep them is the *absence* of the trip — they genuinely exercise the trip predicate, not the threshold. Confirmed jointly required: all three sub-conditions are now pinned (positive all-true `test_ambiguous_unused_high_null_forces_low` + three independent breaking negatives).

**2. Table-draft trip is real (closes the U30 LOW) — PASS.**
`test_apply_trips_the_table_draft` (`test_gate.py:162-174`) builds a `SessionState` whose `TableDraft(confidence=0.4)` is below the 0.75 `keep_threshold`, plus one confident column (`o_orderkey`, 0.95). It asserts: exactly one `Question` with `target_name is None`, that question's `target_kind == DraftKind.TABLE`, the table draft is marked `DraftStatus.NEEDS_INPUT`, and the confident column stays `DraftStatus.DRAFT` (untouched). This matches `apply()` exactly: the table draft is enqueued as `(None, DraftKind.TABLE, state.table_draft)` (`gate.py:114-115`), routed via `route_item` (`gate.py:122`), and in interactive mode set to `NEEDS_INPUT` with a `Question(target_kind=DraftKind.TABLE, target_name=None, …)` (`gate.py:126-135`). The U30 LOW was that the `None`-keyed table force-low/needs_input branch in `apply()` was never exercised end-to-end (the table draft in the shared `_state()` fixture was built to `keep`). This test exercises it directly. **Mutation C** (skip appending the table item in `apply()`) → this test FAILS at `test_gate.py:172` (`assert 0 == 1`). The column-kept assertion also guards that the table trip doesn't bleed into confident columns. Closed.

**3. RUN — PASS.** `PYTHONPATH=src /tmp/geniefy_pgvenv/bin/python -m pytest tests/test_gate.py -q` → **18 passed in 0.02s**. The three U34 tests also pass in isolation (3 passed). All spine types the tests reference resolve in `state.py` (U27): `DraftStatus.NEEDS_INPUT`/`DRAFT` (`state.py:82,80`), `DraftKind.TABLE`/`COLUMN` (`state.py:104-105`), `Question.target_kind`/`target_name` (`state.py:269-270`), `TableDraft.confidence`/`proposed_comment` (`state.py:170-172`).

## Tests: pass count + meaningfulness
- **Pass count: 18 passed** (`pytest tests/test_gate.py -q`, 0.02s).
- **Meaningful: YES.** Mutation testing confirms each of the three U34 additions catches the specific defect it targets: dropping `high_null_fraction` (A), dropping `ambiguous_name` (B), and skipping the table draft in `apply()` (C) each now turn the suite red, where the corresponding mutations survived 15/15 in the U30 audit. The hardening is non-vacuous and directly traceable to the U30 findings.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 0 · UNVERIFIED: 0
Verdict: **PASS**   (clean — no findings. U34 closes the U30 audit MED (both untested sub-condition axes now pinned) and the U30 LOW (table-draft `apply()` path now exercised). The module `gate.py` was already correct; U34 is the test-coverage hardening that makes that correctness regression-safe.)
