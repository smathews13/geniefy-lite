# Audit report — deploy-orchestration (deploy.sh + run_migrations.py + databricks.yml) — 2026-06-12

## Target audited
Files:
- `/Users/rohit.dashora/fe-vibe/geniefy-v3/deploy.sh`
- `/Users/rohit.dashora/fe-vibe/geniefy-v3/migrations/run_migrations.py`
- `/Users/rohit.dashora/fe-vibe/geniefy-v3/databricks.yml`

Unit: U62 (deploy orchestration, NFR-D / D35)
Auditor: independent subagent (did not author U62). Mechanical only — no fixes, no ledger edits.

## Oracle
- `docs/design/LLD-nfr-plan.md` §4 (7-step deploy.sh spec + DAB-vs-CLI split)
- `docs/design/LLD-packaging-deployment.md` (U8 §3/§6/§7 — bundle layout, geniefy_setup runs migrations/)
- `migrations/APPLY.md` (validated migration mechanism)
- `.gotm/DECISIONS.md` D35 (DAB+shell split), D30 (geniefy DB), D12 (parameterized), D39 (app.yaml config surface), D40 (token_budget subsumed)
- `app/app.yaml` (`valueFrom: sql-warehouse` / `valueFrom: lakebase` bindings)

## Mechanical command results
- `bash -n deploy.sh` → **OK** (exit 0).
- `python3 -m py_compile migrations/run_migrations.py` → **OK** (exit 0).
- `databricks bundle validate -t dev` → **Validation OK!** (exit 0, no error/no warning). CLI v0.298.0.
- `databricks bundle validate -t prod` → **Validation OK!** (exit 0, no error/no warning). Rendered prod `root_path` = `/Workspace/Users/<user>/.bundle/geniefy-v3/prod` (principal-scoped, not `/Workspace/Shared`).

## Findings

### HIGH severity
- None.

### MEDIUM severity
- None.

### LOW severity
- **Missing-flag-value emits a raw bash error, not a friendly usage message** (`deploy.sh:40` and the other `shift 2` arms). Invoking `./deploy.sh -t` with no following value derefs `$2` under `set -u`, printing `deploy.sh: line 40: $2: unbound variable` instead of `usage 1`. The script still aborts **non-zero (exit 1) before any side effect**, so it fails safely — purely a UX nit on a malformed-invocation edge case. The `--help` (exit 0) and unknown-arg (exit 1, usage printed) paths are correct.

### UNVERIFIED
- **End-to-end live deploy not exercised.** A real run needs a live workspace + Lakebase project/endpoint + warehouse; per the audit prompt and D36, this is integration-verified at deploy and out of hermetic scope. Static + parse + validate checks all pass. Not counted against the verdict.

## Detailed check results

**Check 1 — deploy.sh covers the 7 spec steps, idempotently: PASS.**
All 7 step headers present and correctly ordered (`step "N/7 …"`, lines 69/82/92/110/116/130/142): (1) Preflight, (2) Build FE→app/static, (3) Ensure Lakebase, (4) bundle deploy, (5) Migrate, (6) Grants, (7) URL.
- Preflight: `command -v` for databricks/jq, npm (guarded behind `--skip-build`), python3 (local-migrate path); **real version compare** via `sort -V` against `MIN_CLI=0.285.0` (lines 72–75); auth via `current-user me` (line 78).
- Build: `npm ci || npm install` + `npm run build`, then a **guard** `[[ -f app/static/index.html ]] || die` (line 89).
- Lakebase: get-or-create project (`postgres get-project` then `create-project`), **wait-for-ACTIVE** loop (60×5s, line 101–107) with a final `${STATE:-}` guard.
- Deploy: `bundle validate` then `bundle deploy` (idempotent).
- Migrate: local runner default; `--use-job` path runs `bundle run geniefy_setup`; psycopg2 presence checked before the local path.
- Grants: prints SELECT/MODIFY guidance (cites D7/U6) with SP resolved if available.
- URL: prints `apps get … .url`, with a fallback hint.
- Idempotency: every step is get-or-create / re-runnable (project get-then-create, DuplicateDatabase catch in the runner, idempotent SQL, bundle deploy inherently idempotent). `bash -n` clean.
- `set -euo pipefail` safety: `DBX` array always seeded with `databricks` (never empty-expanded); `STATE` guarded with `${STATE:-}` (line 107); `SP` uses `${SP:+ …}` and `[[ -n "$SP" ]]`; `URL` uses `[[ -n "$URL" ]]`; `PROFILE` initialized to `""` (line 25) so `DATABRICKS_CONFIG_PROFILE="$PROFILE"` is never unset. No unguarded empty-array expansion found.

**Check 2 — run_migrations.py matches APPLY.md: PASS.**
- `CREATE DATABASE "{DB}"` on an **autocommit** connection (line 78), catching `psycopg2.errors.DuplicateDatabase` (line 84) — matches APPLY.md step 1.
- Applies `migrations/[0-9]*.sql` in **sorted** order (line 90; glob matches `001_init.sql`), one `cursor.execute(f.read_text())` + `conn.commit()` per file (lines 95–99) — the documented simple-query-protocol whole-file mechanism.
- Coords from env (`GENIEFY_PG_HOST/USER/TOKEN`, `GENIEFY_LAKEBASE_PROJECT/BRANCH/ENDPOINT`, `DATABRICKS_CONFIG_PROFILE`) with a **CLI fallback** (`_coords`/`_cli`): `postgres list-endpoints …status.hosts.host`, `current-user me .userName`, `postgres generate-database-credential …token` — byte-for-byte the APPLY.md commands.
- `psycopg2` imported at **run time only** (inside `_connect` and `main`, lines 64/75), never at module import.
- deploy.sh↔runner env-var names agree; empty `PROFILE` is falsy in Python so no stray `-p ""` is passed. `py_compile` clean.

**Check 3 — databricks.yml correct + validates: PASS.**
- App `resources` bindings named **`sql-warehouse`** (line 74) and **`lakebase`** (line 79) — exactly match `app.yaml`'s `valueFrom: sql-warehouse` / `valueFrom: lakebase` (app.yaml lines 24/26).
- `geniefy_setup` job runs `./migrations/run_migrations.py` via `spark_python_task` (lines 91–98), with a `psycopg2-binary` dependency in the environment spec.
- **No `${var}` on auth fields** — the earlier U15 bug (`workspace: { host: "${var.host}" }`) is gone; `host` is only a documentation-only `variables:` entry, never assigned to `workspace.host`. Auth comes from profile/DATABRICKS_HOST.
- Prod has an explicit, principal-scoped `root_path` (line 59) rendering to `/Workspace/Users/<user>/.bundle/geniefy-v3/prod` — not world-writable; no warning emitted.
- `bundle validate` reports `Validation OK!` on **both** dev and prod.

**Check 4 — parameterization (D12): PASS.**
- `deploy.sh` and `run_migrations.py` contain **zero** hardcoded workspace URLs / catalogs / warehouse IDs. `LAKEBASE_INSTANCE` is env-overridable (`${GENIEFY_LAKEBASE_INSTANCE:-geniefy}`); `TARGET`/`PROFILE`/`HOST` are CLI flags.
- All dev-specific values in `databricks.yml` (`fevm-rd-classic`, `rd_classic_catalog`, warehouse `0336…`, model endpoint, `samples`, `geniefy`) are `variables:` with `default:` — overridable per-target or via `--var`. The `host` default is explicitly informational-only.
- Observation (not a finding): `app.yaml` carries the model endpoint + MCP-providers JSON as literals rather than via bundle `${var}` (cf. D39's "var feeding app.yaml"). This is mechanically forced — Databricks App `env` literals do not interpolate bundle vars; only the two `valueFrom` resource bindings can be wired, and both are present and correct. Consistent with D33/D39 (app.yaml is the config surface). Not a U62 defect.

Note: the README being stale post-U62 is tracked as **U74** (not a U62 defect, per the audit prompt).

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 1 · UNVERIFIED: 1
Verdict: **PASS-FINDINGS** (one LOW UX nit on malformed-arg handling; one UNVERIFIED live-deploy item explicitly out of hermetic scope)
