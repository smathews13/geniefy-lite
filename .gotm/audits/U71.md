# Audit report — code (test hardening) — 2026-06-12

## Target audited
File: `tests/test_state.py` — the two tests added by U71:
`test_draft_from_dict_applies_defaults_when_fields_omitted` (:195-202) and
`test_draft_kind_matches_migration_enum` (:189-192).
Unit: **U71** (fix unit closing the two residuals of the U29 audit — its MED and its LOW)

Auditor: independent subagent; did **not** author U71 (or U27/U28/U29). Mechanical audit
only — read target + oracle, RAN the suite, and mutation-probed both new assertions on a
**scratch copy** under `/tmp/u71_probe` (never the real tree). No source/ledger/migration
edits; after probing, the scratch tree was diffed against the live tree (`state.py`,
`001_init.sql`, `test_state.py` all reported IDENTICAL) and the scratch dir removed.

## Oracle
- `.gotm/audits/U29.md` — the two residuals U71 must close:
  - **MED:** U29 tested only the dataclass *constructor* default (`TableDraft(...)`), not
    the `from_dict({})` omitting-dict path that `_draft_common_from_dict` handles
    independently. U29 mutation-confirmed that flipping `_draft_common_from_dict`'s
    `apply_status` default `not_applied`→`applied` left all 31 tests green.
  - **LOW:** the `draft_kind` check pinned member *names* (`{k.name} == {"TABLE","COLUMN"}`),
    not enum *values* against the live schema, so a value drift (`TABLE = "tbl"`) would slip past.
- `src/geniefy_core/state.py` — `_draft_common_from_dict` defaults (:254-255: `status`
  defaults `"draft"`, `apply_status` defaults `"not_applied"`); `DraftKind` (:103-105,
  values `"table"`/`"column"`).
- `migrations/001_init.sql:55` — `create type geniefy.draft_kind as enum ('table','column')`.

## Checks run (commands + results)

**1. The from_dict-defaults test bites the named U29 MED regression.**
`test_draft_from_dict_applies_defaults_when_fields_omitted` (:195-202) builds
`TableDraft.from_dict({"proposed_comment": "x"})` and
`ColumnDraft.from_dict({"column_name": "o_orderkey"})` — both omitting `status`/`apply_status`
— and asserts `status == DraftStatus.DRAFT and apply_status == ApplyStatus.NOT_APPLIED`. This
routes through `_draft_common_from_dict` (state.py:254-255), the exact independent default
path the U29 MED named (distinct from the constructor-default path covered by
`test_table_draft_defaults` at :177). Mutation-confirmed on the scratch copy:

| Mutation (scratch `state.py`, then discarded) | Result |
|---|---|
| `_draft_common_from_dict` `apply_status` default `"not_applied"`→`"applied"` (the U29 MED mutation, :255) | **FAILS at :200** — `applied != not_applied` (was green under U29) |
| `_draft_common_from_dict` `status` default `"draft"`→`"reviewed"` (:254) | **FAILS at :200** — `reviewed != draft` |

The U29 audit recorded that the first mutation left **all 31 tests green**; under U71 it now
**fails the new test**. The omitting-dict default path is no longer a regression-net gap. **MED closed.**

**2. draft_kind parity is real (closes U29 LOW).**
`test_draft_kind_matches_migration_enum` (:189-192) asserts
`{k.value for k in DraftKind} == _schema_enum("draft_kind")` — i.e. enum **values**
(`{"table","column"}`) against the live migration, via the *same* `_schema_enum` regex
mechanism (:40-44) used by `test_enums_match_migration` for the other four enums. This is the
value↔schema parity the U29 LOW found unguarded (the old `test_draft_kind_has_table_and_column`
at :184 pins only member names and still stands — names + values are now both pinned).
Mutation-confirmed on the scratch copy, both drift directions:

| Mutation (scratch tree, then discarded) | Result |
|---|---|
| Migration `draft_kind` value `'table'`→`'tbl'` (member name `TABLE` intact) | **FAILS at :192** — `{column,table} != {column,tbl}` |
| `DraftKind.TABLE` value `"table"`→`"tbl"` in `state.py` | **FAILS at :192** — `{column,tbl} != {column,table}` |

A value drift on either side — Python or schema — is caught. `_schema_enum("draft_kind")`
matched the migration cleanly (no "enum not found" assert), confirming the regex resolves the
`draft_kind` enum at `001_init.sql:55`. **LOW closed.**

**3. RUN.** `cd /Users/rohit.dashora/fe-vibe/geniefy-v3 && PYTHONPATH=src
/tmp/geniefy_pgvenv/bin/python -m pytest tests/test_state.py -q` → **33 passed in 0.03s**
(was 31 under U29; +2 net: the two U71 tests). Post-probe re-run on the restored scratch tree:
33 passed.

## Findings

### HIGH severity
- None. The suite runs green (33/33), and both new tests are mutation-confirmed non-vacuous:
  each fails under exactly the regression its U29 residual named. Nothing blocks downstream
  consumption.

### MEDIUM severity
- None. The U29 MED (the `from_dict` omitting-dict default path) is closed — Check 1.

### LOW severity
- None. The U29 LOW (`draft_kind` value↔schema parity unguarded) is closed — Check 2.

### UNVERIFIED
- None. All claims were checkable by running the suite and mutating a scratch copy of the source/migration.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 0 · UNVERIFIED: 0
Verdict: **PASS**   (HIGH ⇒ FAIL; MEDIUM/LOW-only ⇒ PASS-FINDINGS; clean ⇒ PASS)

**U71 closes both U29 residuals.** The MED is closed: a new test exercises the
`_draft_common_from_dict` omitting-dict default path independently of the dataclass
constructor, and mutation-confirms it now fails under the precise mutation U29 found
slipped through green. The LOW is closed: `draft_kind` is now pinned by enum **value**
against the live migration through the same `_schema_enum` parity mechanism as the other
four enums, catching a value drift on either the Python or the schema side. Suite: 33/33 green.
