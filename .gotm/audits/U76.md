# Audit report — verification-evidence (existence + consistency + truthfulness vs live workspace) — 2026-06-12

## Target audited
File: `/Users/rohit.dashora/fe-vibe/geniefy-v3/DEPLOY_VERIFY.md`
Unit: U76

## Method
Independent auditor (did not author U76). Mechanical only — no fixes, no ledger/source edits.
Oracle: live workspace (CLI profile `fe-vm-classic`), `deploy.sh`, `databricks.yml`, `.gotm/DECISIONS.md` (D36),
and the live `samples.tpch.orders` schema.

## Checks run

### Check 1 — app is real + healthy (LIVE)
`databricks apps get geniefy-dev -p fe-vm-classic` returned:
- name: `geniefy-dev` ✅
- url: `https://geniefy-dev-7474653107059373.aws.databricksapps.com` — matches DEPLOY_VERIFY.md line 13 ✅
- compute_status.state: **ACTIVE** ✅
- app_status.state: **RUNNING** ✅
- resources: `["sql-warehouse", "geniefy-db", "fmapi-endpoint"]` — exactly the three the doc claims (operator steps §34-36) ✅

### Check 2 — health endpoint (LIVE)
`curl -H "Authorization: Bearer <token>" .../health` → `{"status":"ok"}` (exit 0) ✅

### Check 3 — doc internally consistent + not fabricated (cross-checked 6 of 10 findings vs source)
- **Finding 2** (`sync.include` force-uploads gitignored artifacts): `databricks.yml` lines 16-19 — includes `app/geniefy_core/**`, `app/geniefy_app/**`, `app/static/**`. Matches ✅
- **Finding 5** (no `get_open_ai_client`): `src/geniefy_app/providers.py` line 84 documents "SDK exposes no OpenAI-client helper"; line 95 builds `OpenAI(base_url=f"{host}/serving-endpoints", api_key=oauth_token)` directly. Matches ✅
- **Finding 9** (fence-strip): `src/geniefy_core/llm.py` lines 73-90 — `json()` strips leading/trailing ``` fences (lines 79-83) and falls back to outermost `{…}` via `text[start:end+1]` (line 90). Matches ✅
- **Finding 8** (FMAPI rejects `response_format=json_object`): `src/geniefy_app/providers.py` lines 59-63 — accepts `response_format` for protocol compat but "intentionally not sent", with the exact INVALID_PARAMETER_VALUE rationale. Matches ✅
- **Finding 10** (max-tokens bump): `src/geniefy_core/llm.py` line 27 — `DEFAULT_MAX_TOKENS = 4096` with comment citing 1024 truncation. Matches ✅
- **Finding 3** (runtime deps): `app/requirements.txt` lines 6-8 — `openai`, `psycopg2-binary`, `pyyaml` present. Matches ✅
- **Finding 4** (deploy.sh starts the app): `deploy.sh` line 126 — `bundle run geniefy`. Matches ✅

Run-evidence consistency vs **live** `samples.tpch.orders` schema (DESCRIBE via warehouse `0336d1a2b47936b4`):
the table has exactly 9 columns; DEPLOY_VERIFY.md (lines 48-50) accounts for all 9 — 6 commented
(`o_orderkey` bigint/PK, `o_custkey` bigint/FK, `o_totalprice` decimal, `o_orderdate` date, `o_clerk` string,
`o_comment` string) + 3 needs_input (`o_orderstatus` string-enum, `o_orderpriority` string-enum,
`o_shippriority` int "always 0"). Column types align with each stated description; no fabricated or
omitted columns. The `o_orderdate` range 1992-01-01→1998-08-02 is the canonical TPC-H orders range. Consistent ✅

D36 (`.gotm/DECISIONS.md` line 486) exists and is the App-backend-packaging decision that a live deploy
validates — consistent with the doc's framing of D36 as the "integration-verified at deploy" boundary ✅

### Check 4 — deploy.sh parses
`bash -n deploy.sh` → PARSE_OK ✅

## Findings

### HIGH severity
- none.

### MEDIUM severity
- none.

### LOW severity
- **Findings 1, 6, 7 not independently re-derived against source.** Of the 10 findings, 7 were cross-checked
  against files; findings 1 (DAB `database` binding can't target Autoscaling Lakebase), 6 (PGHOST/PGUSER
  binding contract in `_lakebase_connection`), and 7 (`SessionStore` rollback self-heal) were corroborated
  by surrounding evidence (the live app's `geniefy-db` resource exists; `databricks.yml` lines 84-87 document
  the same Autoscaling-vs-classic limitation as finding 1) but not each verified line-by-line. The audit spec
  asked for 2-3 cross-checks; 7 were done, so this is below-bar coverage residue, not a defect. Cosmetic.

### UNVERIFIED
- **Live state is a snapshot, not a replay of the original deploy.** Per the unit's own note, a full deploy
  cannot be re-run; the audit verifies present live state + doc-to-source consistency, which it does. The
  exact end-to-end run on 2026-06-12 (state-machine transitions `created→…→awaiting_input`) was not re-executed —
  but its output (the 9-column comment set) is fully consistent with the live table schema, which is the
  strongest available corroboration.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 1 · UNVERIFIED: 1
Verdict: **PASS-FINDINGS**   (no HIGH/MEDIUM; one LOW coverage note + one inherent UNVERIFIED replay limit)

Doc matches reality: **YES.** The app is live (compute ACTIVE / app RUNNING), health returns
`{"status":"ok"}`, all three claimed resources are bound, every cross-checked finding (7/10) maps to a real
source change, and the run evidence is consistent with the live `samples.tpch.orders` schema. No fabrication
detected.
