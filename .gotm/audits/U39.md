# Audit report — code (module + tests) — 2026-06-12

## Target audited
File: `src/geniefy_core/context.py` (U39 — `ContextGatherer` + built-in providers + `McpContextProvider`)
File: `tests/test_context.py` (U40 — the context-layer tests; assessed + run)
Unit: U39 (with U40 tests)

## Method
Read target + oracle (LLD-agent-core §3.3/§8, LLD-nfr-plan §3 / D34, D5/D4/D1/D17, state.py U27, llm.py U32). Ran the import smoke-test and the U40 suite. Grepped imports for real-I/O dependencies. Mutation-tested 9 spec-critical behaviors in a scratch copy to gauge whether the tests are meaningful. Probed edge cases (GROUP-BY tail parse, empty providers, nested normalize, mid-loop max_snippets cap).

## Findings

### HIGH severity
- None.

### MEDIUM severity
- None.

### LOW severity
- **Built-in lineage/query-history providers have no direct unit test for SQL *escaping under adversarial input*.** `LineageProvider.queries` uses `_lit()` (doubles single quotes) and `test_lineage_queries_target_system_tables_and_escape` asserts the literal wraps `'samples.tpch.orders'`, but no test feeds a table name containing a quote (e.g. `c.s.o'rders`) to confirm the escape actually neutralizes injection. The code is correct (`value.replace("'", "''")`); the coverage is just thin. Cosmetic — fix if convenient. (`context.py:65-67`, `tests/test_context.py:48-52`)
- **`McpContextProvider` builds one query and sends the *same* `{"query": query}` arg to every allowed tool.** This matches the D34 contract as written (query_template → invoke allowed tool(s)), and is fine for the Glean/Confluence `search`-style tools the spec names. No issue against spec; noting only that a tool expecting a differently-named arg would need future config — out of v1 scope. (`context.py:201-208`)

### UNVERIFIED
- **Live system-table SQL shape.** The `system.access.table_lineage` / `system.access.column_lineage` / `system.query.history` queries are validated structurally (string assertions + injected fake runner) but not executed against a real warehouse. This is intentional and consistent with the hermetic design (D1/D17) and the U17-style "queries validated live later" note in the ledger — flagged, not a defect.

## Detailed check results

1. **Existence** — both files exist at the ledger's stated paths. PASS.

2. **Spec match** — PASS.
   - `ContextSnippet` carries `text` / `source` / `informs` / `relevance` (+`raw`) with round-tripping `to_dict`/`from_dict`; `ContextProvider` is a `runtime_checkable` Protocol with `name` + `gather(table, columns)`. (U4 §3.3)
   - `LineageProvider` queries `system.access.table_lineage` (upstream via target match, downstream via source match) and `system.access.column_lineage`, emitting upstream-sources, downstream-consumers, and column-derivation snippets (the last tagged `informs=[target_col]`, relevance 0.8). Dispatch across the three queries is unambiguous (verified). (U4 §3.3, D5)
   - `QueryHistoryProvider` queries `system.query.history` with an `ILIKE` table filter + `ORDER BY start_time DESC LIMIT`, and parses alias-tolerant `JOIN ... ON <a>=<b>` + `GROUP BY` (tail-anchored before ORDER/LIMIT/EOL) into join-key and grain hints. (U4 §3.3, D5)
   - `McpContextProvider` follows the D34 contract: injected `session_factory(config)` → `list_tools()` ∩ `tool_allowlist` (empty allowlist = allow-all) → build query from `query_template` → `call_tool` → `_normalize_mcp_result` → cap at `max_snippets` (cap honored mid-loop across multiple tools, verified). (U24 §3, D34)
   - `ContextGatherer` merges providers → ranks by `relevance` desc → `_trim` to `context_token_budget` keeping ≥1 (top) → degrades on provider exception with a warning, never aborting. `gather_into(state)` sets `state.phase = GATHERING_CONTEXT` and writes `state.context` as the dict form the Reasoner reads, deriving columns from `state.profile`. (U4 §3.3/§8)

3. **Cross-reference integrity** — PASS. Citations resolve: U4 §3.3 (ContextGatherer interface + budgeting) and §8 ("Context provider down (MCP) → skip + warning, degraded not failed") both present in LLD-agent-core.md; U24 §3 + D34 specify the exact connect→list∩allowlist→query_template→normalize→max_snippets contract the code implements; D5 (built-in + pluggable MCP); D4 (secret-scope ref, never raw); D1/D17 (hermetic, injected I/O); U27 `SessionState.context`/`phase` and `Phase.GATHERING_CONTEXT` exist in state.py; U32 `TokenCounter`/`HeuristicTokenCounter` exist in llm.py and are reused for budgeting.

4. **Internal consistency** — PASS. Ranking is relevance-desc (`reverse=True`); `_trim` guards `... > budget and kept` so the top snippet is never dropped; `gather_into` sets both `state.context` (list of dicts via `to_dict`) and `phase`; `_normalize_mcp_result` handles None / str / list / tuple / Mapping (with `results`/`content`/`items`/`data` recursion and `text`/`snippet`/`summary` single-key) and stringifies unknown shapes. No contradictions found.

5. **Decision fidelity (CRITICAL)** — PASS.
   - **Hermetic (D1/D17):** imports are stdlib (`re`, `dataclasses`, `typing`) + intra-package (`geniefy_core.llm`, `geniefy_core.state`) only. The forbidden-I/O grep (requests/httpx/urllib/http/socket/databricks/sqlalchemy/psycopg/pyodbc/sqlite3/aiohttp) returned **nothing**. The `SqlRunner` and `McpSession` are injected Callables/Protocols. **Module confirmed hermetic.**
   - **D4 (secrets):** the module never reads, stores, or logs a raw secret — auth/transport (the secret-scope token) lives entirely inside the injected `session_factory`; the docstrings explicitly state "secret-scope reference, never a raw secret."
   - **Degrade-not-fail (U4 §8):** a provider raising is caught, recorded as a warning, and the run continues with the surviving providers' snippets.

## Test assessment (U40)

- **Run:** `PYTHONPATH=src /tmp/geniefy_pgvenv/bin/python -m pytest tests/test_context.py -q` → **21 passed** (0.02s). Import smoke-test (`import geniefy_core.context`) → `ok`, no heavy deps.
- **Meaningful?** YES. 9 spec-critical mutations were each caught by the suite:
  - M1 ranking not applied (reverse=True→False) → 2 failed
  - M2 budget trim drops everything (remove `and kept` guard) → 1 failed
  - M3 allowlist not enforced (allow-all) → 2 failed
  - M4 degrade swallows a real result (`extend`→discard) → 5 failed
  - M5 `max_snippets` cap removed → 1 failed
  - M6 normalize single-key `text`/`snippet`/`summary` broken → 1 failed
  - M7 `query_template` ignored → 1 failed
  - M8 degrade swallows the warning → 1 failed
  - M9 `gather_into` doesn't set phase → 1 failed

  The five behaviors the audit brief asked about (ranking, budget-drops-all, allowlist, degrade-swallow, normalize shapes) are all covered and all kill their mutant.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 2 · UNVERIFIED: 1
Verdict: **PASS-FINDINGS**   (LOW/UNVERIFIED only — no blocking findings)

U40 tests: 21 passed, MEANINGFUL (9/9 mutations caught). Module confirmed HERMETIC (stdlib + intra-package only; injected SQL runner + MCP session; no raw-secret handling).
