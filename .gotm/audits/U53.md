# Audit report — code (5-point + critical-path correctness) — 2026-06-12

## Target audited
File: `src/geniefy_app/apply.py`
File: `tests/test_apply.py`
Unit: U53 — UC apply write-path `Applier` (D7/D21)

## Method
Read target + oracle (LLD-review-apply.md §4/§5/§8, D7, D21, D20, D11, `state.py`).
Ran the suite hermetically and mutation-tested each critical guard to confirm the
tests are *meaningful* (would catch the named failure modes), not just green.

RUN: `PYTHONPATH=src /tmp/geniefy_pgvenv/bin/python -m pytest tests/test_apply.py -q`
Result: **13 passed in 0.02s**.

Mutation test (disable a guard in the source, confirm a test goes red):

| Regression injected | Outcome |
|---|---|
| `_lit` stops doubling `'` (raw interpolation) | CAUGHT (1 failed) — `test_escaping_doubles_single_quotes` |
| conflict check disabled | CAUGHT (1 failed) — `test_conflict_when_live_changed_since_profiling` |
| empty-clobber guard disabled | CAUGHT (1 failed) — `test_empty_over_existing_refused` |
| idempotent no-op disabled | CAUGHT (1 failed) — `test_noop_when_proposed_equals_live` |
| per-item checkpoint removed | CAUGHT (5 failed) — checkpoint + reload assertions |
| non-approved drafts written | CAUGHT (1 failed) — `test_only_approved_or_edited_are_applied` |

Every load-bearing guard fails the suite when broken. Source restored verbatim after.

## 5-point + critical checks

1. **Existence** — both files exist at the ledger's stated paths. PASS.
2. **Spec match** — `Applier.apply` implements LLD §4's per-item ordered pipeline
   (re-read live → conflict → empty-clobber → idempotent → escaped write → per-item
   persist) and §8's edge-case table. PASS.
3. **Diff-first** — `_apply_one` re-reads the *live* comment (`_read_live`, querying
   `information_schema.tables`/`.columns` at apply time, apply.py:88,126-136) and
   compares to the profiled `draft.current_comment` (apply.py:92), not the snapshot. PASS.
4. **Conflict** — when `_norm(live) != _norm(current_comment)` and `not confirm` →
   `ApplyStatus.CONFLICT`, returns both `live`/`expected`, no write (apply.py:92-95).
   `confirm=True` overrides (test_conflict_overridden_by_confirm). PASS.
5. **Idempotent** — `_norm(proposed) == _norm(live)` → `SKIPPED_NOOP`, no DDL
   (apply.py:107-110). PASS.
6. **Never clobbers w/ empty** — empty/whitespace proposed over a non-empty live →
   `FAILED` with a clear message, no write (apply.py:98-104). Empty-over-empty → noop.
   Correctly *precedes* the idempotent check and sits *after* the conflict check, so a
   `confirm` override still cannot push an empty over a good comment. PASS.
7. **Escaped DDL** — identifiers backtick-quoted with backtick-doubling (`_ident`,
   apply.py:37-38); comment literal single-quote-doubled (`_lit`, apply.py:41-42).
   `test_escaping_doubles_single_quotes` asserts a real doubled quote
   (`COMMENT 'it''s the key'`). The read path's WHERE values are also `_lit`-escaped
   (apply.py:130,134-135), so reads are injection-safe too. No raw interpolation of
   user text anywhere. PASS.
8. **Per-item `apply_status` + checkpoint** — each item flips its own
   `apply_status`/`applied_*`/`status`, and `self._store.save(...)` runs after each
   processed item (apply.py:80); `test_per_item_checkpoint_saves_each` asserts 2 saves
   for 2 items. PASS (D20/D21).
9. **Failure isolation** — a write exception (e.g. missing MODIFY) marks that item
   `FAILED` and the loop continues; `test_failed_item_isolated_others_proceed` proves
   `{a: failed, b: applied}`. PASS (LLD §8 / D20).
10. **Only approved/edited written** — `_APPLYABLE = (APPROVED, EDITED)` gate
    (apply.py:30,77); draft-status items are skipped. PASS.
11. **Hermetic** — warehouse runner + `SessionStore` injected via `__init__`; tests use
    `FakeWarehouse`/`FakeStore`, no real UC/Lakebase (D36). PASS.
12. **Internal consistency / decision fidelity** — uses the frozen `ApplyStatus`/
    `DraftStatus` enums from `state.py` verbatim; honors D7 (separate, diff-first,
    never-clobber), D21 (re-read + conflict + idempotent + per-item checkpoint + escaped
    DDL), D20 (per-iteration bound). PASS.

## Findings

### HIGH severity
- none.

### MEDIUM severity
- none.

### LOW severity
- **Unused import** (`tests/test_apply.py:23` imports `SessionState`, used only as a
  type hint in `FakeStore.save`; `apply.py:27` imports `SessionState` but the module
  never references it — only `ApplyStatus`/`DraftStatus` are used). Cosmetic; a linter
  would flag the `apply.py` one. No functional impact.

### Notes (not findings — sanctioned deferrals, per the audit brief)
- The D21 `audit_log` row is **deferred to U54** (needs a `SessionStore.append_audit`
  the frozen store doesn't expose); the per-item before/after is returned in `results`
  meanwhile. Documented in the module docstring (apply.py:19-22) and the U54 ledger row.
  Not a defect of U53.
- Live UC DDL behaviour (actual `COMMENT ON`/`ALTER COLUMN` execution, MODIFY grant
  errors) is **integration-verified later** (U54 wiring + deploy). The unit is hermetic
  by design; the fake warehouse models the missing-MODIFY path. Not a defect.
- `/api/sessions/{id}/apply` (U52) returns 501 until U54 wires the `Applier` into
  `build_service`. Tracked. Not a defect of U53.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 1 · UNVERIFIED: 0
pytest: 13 passed
DDL escaped + conflict-aware: confirmed (y)
Verdict: **PASS-FINDINGS** (one LOW cosmetic finding — unused imports; output is
consumable downstream)
