# Audit report — content-claim / behavior + tests — 2026-06-12

## Target audited
File: `src/geniefy_core/reasoner.py` (two-pass threading) + `tests/test_reasoner.py` (two new tests)
Unit: U82 — two-pass generation (E2/D44)

## Oracle
- `docs/design/LLD-amend-003.md` §E2 — keep the `draft_table`→`draft_columns` split; feed the generated table comment into the column pass as an explicit labeled context block.
- `.gotm/DECISIONS.md` D44 — table comment first, then columns grounded in it (purpose/grain/keys); no re-profiling; reuses the existing split.

## Findings

### HIGH severity
- none.

### MEDIUM severity
- none.

### LOW severity
- none.

### UNVERIFIED
- none.

## Check-by-check

1. **Threading correct — PASS.** `draft()` (reasoner.py:84-95) calls `_draft_table` first, then reads `state.table_draft.proposed_comment` (line 91) and passes the resulting `table_comment` into **every** column batch via `_draft_columns(state, batch, table_comment)` (line 95). The column instruction prepends a labeled block only under `if table_comment:` (line 139), so the block is added solely when a non-empty table comment exists.

2. **Degrade — PASS.** `table_comment` is initialized `None` (line 89) and set only when `state.table_draft is not None and status != DraftStatus.ERROR` (line 90), and then only if `(proposed_comment or "").strip()` is truthy (line 91 — empty/whitespace comment collapses to `None`). With `table_comment` falsy the `if table_comment:` guard (line 139) is skipped → no `"TABLE COMMENT"` label, no stale/empty block, no crash; columns still draft from their own profile + `schema_meta`. Confirmed by `test_two_pass_omits_block_when_table_draft_failed` (test_reasoner.py:272-279), which asserts `status == ERROR`, no `"TABLE COMMENT"` substring in any column instruction, and that all column drafts are non-ERROR.

3. **Per-phase budgets still honored (U81 not regressed) — PASS.** `_draft_columns` still threads `max_tokens=self.column_max_tokens` to `_complete` (line 144); the table call still passes `table_max_tokens` (line 113). `test_table_and_column_phases_use_their_own_max_tokens` and `test_per_phase_budgets_fall_back_to_client_default_when_unset` pass.

4. **No fabrication / batching intact — PASS.** Column batching via `_chunks(columns, reason_batch_size)` (line 94), dropped-column flagging → `DraftStatus.ERROR` with "model did not return a draft" (lines 158-161), and per-batch `LLMError` isolation (lines 145-151) are unchanged from U35. `test_column_batching_makes_multiple_calls`, `test_dropped_column_is_flagged_not_fabricated`, `test_failing_batch_isolated`, `test_table_failure_isolated` all pass.

5. **Tests bite + pass — PASS.**
   - `PYTHONPATH=src /tmp/geniefy_pgvenv/bin/python -m pytest tests/test_reasoner.py -q` → **14 passed**.
   - `test_two_pass_threads_table_comment_into_column_prompt` (test_reasoner.py:263-269) asserts, for every column-batch instruction, BOTH the label `"TABLE COMMENT (just generated"` AND the actual generated table comment text `"Order headers."` appear. Both label and content checked — bites.
   - Full suite `PYTHONPATH=src /tmp/geniefy_pgvenv/bin/python -m pytest tests/ -q` → **297 passed**, 1 (unrelated httpx/starlette deprecation) warning.
   - **Orchestrator tests pass (7 passed).** The prior-version regression risk is closed: `tests/test_orchestrator.py:86` extracts column names with `re.findall(r"'([^']+)'", instr)`, relying on the single-quoted Python-list repr in `"Draft comments for columns: ['c0', ...]"`. The shipped two-pass block (reasoner.py:140-141) contains **no stray single-quote** — it uses an em-dash and the apostrophe-free phrase "purpose, grain, and keys" (the inline comment at line 139 documents this intent). The original `f"Draft comments for columns: {names}."` text is preserved after the prepended block, so the quoted names the orchestrator consumer parses are intact. No scrambling.

6. **Hermetic — PASS.** `cd … && PYTHONPATH=src python3 -c "import sys, geniefy_core.reasoner; print('leaked:', …)"` → **leaked: none** (no databricks/openai/psycopg2/fastapi modules imported).

7. **Ledger hygiene — PASS.** U82 (LEDGER.md:108) lists `src/geniefy_core/reasoner.py` + `tests/test_reasoner.py` (supersedes U81 for both). U81 (LEDGER.md:107) carries the supersession annotation on both files: `reasoner.py` (superseded by U82) and `test_reasoner.py` superseded by U82.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 0 · UNVERIFIED: 0
Verdict: **PASS**
- threading-correct: y
- degrades-safely: y
- full-suite pass count: 297
- no-stray-quote-in-prompt: y
