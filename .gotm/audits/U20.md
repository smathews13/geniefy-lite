# Audit report — code (Python module, rec-applying fix unit; first dedicated audit per D41) — 2026-06-12

## Target audited
File: `src/geniefy_core/profiler.py`
Unit: U20 — Close the U19-audit MED: guard the `_introspect` (schema_meta) path so a column with no `name` → `ProfileError` (the 2nd call site)

Oracle: `.gotm/audits/U19.md` (the residual MEDIUM: the first missing-`name` site was guarded in `_merge`, but the introspector/`schema_meta` path had a 2nd unguarded site at `SchemaMeta.from_dict` → raw `KeyError`) · `docs/design/LLD-profiling-tool.md` (U3 §9 errors & edge cases / §10 conformance — return structured errors, never a fabricated/partial profile).
Method: read target + oracle + ledger (U20 owns `profiler.py` via the U19→U20 transfer); located **both** missing-`name` call sites; exercised each directly (malformed provider response *and* malformed introspector result); static hermeticity scan for I/O/SQL-exec/connection primitives; ran the hermetic import and the repo test suite.

Note: the prompt's oracle path `docs/design/LLD-profiling.md` does not exist; the actual design doc is `docs/design/LLD-profiling-tool.md` (its §9 is the cited "Errors & edge cases" table — "agent surfaces it, does not fabricate a profile"). Audited against that. Filename-only mismatch in the dispatch, not a target defect.

## Findings

### HIGH severity
- *(none)*

### MEDIUM severity
- *(none)*

### LOW severity
- *(none)*

### UNVERIFIED
- **`information_schema` SQL correctness against live UC.** Carried over from U14/U19 and explicitly out of U20's scope: `information_schema_queries` (profiler.py:384–429) is reference-shaped and slated for live validation at the deploy/infra unit. Identifier/literal escaping (`_ident`/`_literal`, profiler.py:432–439) is sound and unit-tested. Not a U20 regression; noted, not failed.

## Verification of the U19 MEDIUM (this audit's primary job)

The U19 audit (`.gotm/audits/U19.md`:17–18) found U19 guarded the provider/profile path but **not** the introspector path, which it named precisely: `SchemaMeta.from_dict` indexing `c["name"]` (profiler.py:328) reached via `_introspect`, where `SchemaMeta.from_dict(...)` sat *outside* the `try` block → a `name`-less introspector column raised a raw `KeyError`.

**Both call sites located and confirmed guarded:**

- **1st site (provider/profile path) — guarded (pre-existing, U19).** `_merge` (profiler.py:581–589) raises `ProfileError("provider_error", "profile response contains a column with no 'name'", detail=col)` before `TableProfile.from_dict` runs. **Exercise:** provider returns `columns:[{ordinal:1}]` (no `name`) → `ProfileError(code='provider_error')`, no `KeyError`. PASS.
- **2nd site (`_introspect`/schema_meta path) — guarded by U20.** `SchemaMeta.from_dict(ref.full_name, raw or {})` is now **inside** `_introspect`'s `try` block (profiler.py:530–533), with `except ProfileError: raise` (profiler.py:534–535, so a structured error from the introspector passes through un-rewrapped) and a catch-all `except Exception` mapping to `ProfileError("provider_error", "schema introspection failed for …", detail=str(exc))` (profiler.py:536–539). A `name`-less column dict therefore raises `KeyError('name')` inside the guard → caught → surfaced as `ProfileError`. The inline comment (profiler.py:531–532) documents exactly this intent. **Exercise:** introspector returns `columns:[{ordinal:1,data_type:'int'}]` (no `name`) → `ProfileError(code='provider_error')`, no raw `KeyError`. PASS.

Both sites now behave identically and honor U3 §9 / §10 (structured `ProfileError`, never a fabricated/partial profile / raw exception). The U19 MEDIUM is **resolved**.

## No regression
- **Hermetic import (RUN):** `cd … && PYTHONPATH=src python3 -c "import geniefy_core.profiler; print('ok')"` → `ok`. Connection-free.
- **Hermeticity static scan:** `grep -E 'socket|http|requests|urllib|sqlalchemy|psycopg|databricks|pyspark|.connect(|.execute(|subprocess|spark.'` over the source → **no matches** (the only `execute`/`connect` tokens are in docstrings describing the *injected* runner). Module imports only `dataclasses` + `typing`; holds query strings, executes nothing — D1/D17 honored.
- **Suite (RUN):** `PYTHONPATH=src /tmp/geniefy_pgvenv/bin/python -m pytest tests/test_profiler.py -q` → **40 passed** (in 0.03s). (Up from U19's 33 — U21 added the min-version-floor + U20-guard tests.)
- **Provider-agnostic (D15):** `ProfileProvider` / `SchemaIntrospector` remain `runtime_checkable` Protocols; Profiler injected, cannot tell providers apart. Honored.
- **Internal consistency:** the U20 change is a 2-line scope move (pulling `SchemaMeta.from_dict` into the existing `try`); the `except ProfileError: raise` ordering correctly preserves structured introspector errors while wrapping malformed-result `KeyError`s. No dead code introduced; no other call site touched.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 0 · UNVERIFIED: 1
Verdict: PASS-FINDINGS   (HIGH ⇒ FAIL; MEDIUM/LOW-only ⇒ PASS-FINDINGS; clean ⇒ PASS)

The U19-audit MEDIUM is resolved: both missing-`name` call sites (`_merge` provider path and `_introspect`/`SchemaMeta.from_dict` schema_meta path) now raise a structured `ProfileError` rather than a raw `KeyError`, matching U3 §9. Hermetic import → ok; suite → 40 passed; no regression (D1/D17/D15 honored). The lone UNVERIFIED (`information_schema` SQL vs. live UC) is a pre-existing, out-of-scope carry-over slated for the deploy unit — not a U20 defect — so the verdict is PASS-FINDINGS on the strength of that single non-blocking carry-over (no new MED/LOW). If the carry-over is judged non-applicable to U20's narrow scope, this is effectively a clean PASS.
