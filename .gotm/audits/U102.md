# Audit report — structure + content-claim / source-fidelity — 2026-06-13

## Target audited
File: `docs/design/LLD-amend-006.md`
Unit: U102 — Design: LLD-amend-006 (comment enrichment + library lifecycle + reuse + steward-first hero)

## Method
Read the target end-to-end, then checked it against the oracle: `.gotm/DECISIONS.md`
D52/D53 (and cited D9, D43, D23, D48, D5/D34), the live `comment_library` schema in
`migrations/001_init.sql` (= LLD-data-layer U2 §4.7), the sibling `docs/design/LLD-amend-005.md`
(migration numbering), and the U102 ledger row. Applied three audit kinds: existence,
structure, and content-claim / source-fidelity. Each of the prompt's nine targeted checks
was verified individually.

## Findings

### HIGH severity
- None. No claim contradicts D52/D53 or a frozen prior. Reuse is suggestion-only (not
  auto-copy), sunset is soft (no hard delete), the upsert key is unchanged, and the migration
  references only columns that exist. None of the FAIL triggers named in the spec are present.

### MEDIUM severity
- **Revive-to-`applied` references an "apply marker" the migration never creates.** A2
  (`LLD-amend-006.md:39`) says: *"Revive restores to `applied` if `applied_at`/an apply marker
  exists, else `approved`."* But migration `002` (A1, `:16-25`) adds only `status`, `sunset_at`,
  and `sunset_by` to `comment_library` — there is **no `applied_at` column** on the table (the
  live schema, `migrations/001_init.sql:183-197`, has `approved_at`/`approved_by` but no
  `applied_at`). So the revive logic depends on a persisted marker the design does not define.
  This is not a D52 contradiction (D52 only says revive goes "back to `approved`/`applied`"
  without prescribing the mechanism), but a build unit would hit an undefined column. The
  design should either add an `applied_at`/`last_applied_at` column (or an `applied`-state
  audit lookup) to `002`, or simplify revive to always restore to `approved` (apply re-upgrades
  on the next successful write anyway, per the A1 table). Citeable downstream, but the data-model
  unit needs this resolved.
  - Suggested fix unit: follow-on design delta — define the persisted "was-applied" marker
    (add column to migration `002`) OR change A2 revive to restore to `approved` and let apply
    re-upgrade.

### LOW severity
- **Migration SQL style diverges from `001_init.sql` conventions.** A1's SQL (`:16-25`) uses
  **unqualified** `comment_library` and **uppercase** `ALTER TABLE` / `TIMESTAMPTZ` / `CREATE
  INDEX`, whereas `001_init.sql` uses `geniefy.`-schema-qualified names, lowercase DDL, and
  `set search_path to geniefy;` at the top. The unqualified names resolve correctly *only if*
  the migration sets `search_path` (as `001` does), but the snippet omits that line. Also `status`
  is modeled as `TEXT` rather than a `geniefy.*` enum (001 uses enums for all status-like fields,
  e.g. `library_scope`, `draft_status`) — though this matches the sibling U101's `status TEXT`
  choice, so it is a consistent house-divergence, not a contradiction. Cosmetic; the build unit
  should align the snippet to the 001 house style.
  - Suggested fix unit: none required; note for the data build unit (C.1) to schema-qualify +
    set search_path and decide enum-vs-text consistently.
- **Migration numbering is coordinated but not cross-referenced.** U102 claims
  `002_library_status.sql`; the sibling U101 (`LLD-amend-005.md:64`) claims `003_schema_runs.sql`.
  The numbers do **not** collide, and the ledger pins both (U102 row → "migration 002", U101 row →
  "migration 003"), so this is coherent as designed. The only residual risk: neither doc
  cross-references the other's number, so if the two R3 build streams land out of the ledger's
  assumed order a human could misnumber. Low risk given the ledger is authoritative.
  - Suggested fix unit: none; the ledger already disambiguates. Optionally add a one-line
    cross-note when the data build unit is registered.

### UNVERIFIED
- None. All nine targeted checks were verifiable against on-disk oracle (DECISIONS.md, the live
  `001_init.sql` schema, the sibling LLD-amend-005, and the ledger row).

## Check-by-check result (prompt's targeted checks)
1. **Existence** — `docs/design/LLD-amend-006.md` exists, 115 lines, non-empty. PASS.
2. **Structure** — both halves present (PART A library lifecycle/reuse A1–A6; PART B
   enrichment/tags/hero B1–B5). Contains data-model SQL (A1), lifecycle transition table (A1),
   reuse-provider design (A4 `LibraryProvider`), draft-field additions (B2), UI changes
   (A6/B3/B4), and a build-unit sketch (PART C). PASS.
3. **Q4 lifecycle** — transition table coherent: approve/edit → `approved` (explicitly *net-new*
   library write via `SessionService.review_draft`, A3); successful apply → `applied`; failed
   apply stays `approved`/absent ("no upgrade on failure"); manual → `sunset`. A1's closing
   paragraph is explicit that displayed status mirrors apply success ("applied once written to UC,
   approved if only approved"). A3 explicitly states approve (not just apply) now writes the
   library. PASS.
4. **Q5** — upsert key stays `(scope, match_key)` = one canonical entry (A2); churn trade-off
   explicitly acknowledged and *not* hand-waved into a different key. PASS.
5. **Q6** — sunset is SOFT ("Soft only — no hard delete", A5), revivable, excluded from reuse +
   default UI, `usage_count` preserved. PASS.
6. **Q1** — reuse via a `LibraryProvider` *in the ContextGatherer* (A4), explicitly *not* a
   Reasoner side-channel; exact `match_key`, `status ∈ {approved, applied}`, `sunset` excluded,
   ordered by `usage_count DESC`; suggestion-only with stated guardrail ("the Reasoner still
   grounds primarily in this table's profile … never a blind copy"); read-as-SP (D48). PASS.
7. **Q2** — tags FREE-FORM, seeded not constrained, on BOTH `TableDraft.tags` and
   `ColumnDraft.tags`, persisted via `to_dict/from_dict`, flow into `comment_library.tags`
   (which already exists as `jsonb` per `001_init.sql:189`). PASS.
8. **Q3** — field set includes the human's explicit set (purpose, business definition,
   freshness/SLA, known business rules, known data-quality issues, technical/data owner) plus
   proposed additions; word-cap change framed as template `max_words` guidance (~500→~900),
   explicitly distinguished from and not contradicting `GENIEFY_REASON_TABLE_MAX_TOKENS=20000`
   (D43). Deterministic/config-consistent (NFR-A). PASS.
9. **#4 hero** — steward-first hierarchy concrete: hero header band (FQN + trust/confidence badge
   + table tags) + steward facts row (owner · freshness · grain · keys · sensitivity) + secondary
   denser column list; honesty guardrail (D23) explicitly stated ("confidence/verdict shown
   truthfully; low-confidence fields visibly flagged, not hidden"). PASS.

Schema-fidelity spot-check: the ALTER/CREATE INDEX in A1 references only `scope`, `match_key`,
`status` (status added by the same migration) — all present in the live `comment_library`
(`001_init.sql:183-198`). The new index `comment_library_reuse_idx(scope, match_key, status)`
is a coherent superset of the existing `idx_comment_library_lookup(scope, match_key)`; different
name, no collision. `comment_library.tags` already exists (`:189`), so B2's "already exists"
claim is correct.

## Summary
HIGH: 0 · MEDIUM: 1 · LOW: 2 · UNVERIFIED: 0
Verdict: PASS-FINDINGS
