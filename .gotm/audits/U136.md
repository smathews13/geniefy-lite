# Audit report — content-claim + live-verification — 2026-06-22

## Target audited
File: `jobs-bundle/databricks.yml`
Unit: U136
Live: deployed Job `388957213112033` (`geniefy-schema-run-dev`) + fix run `403025164429105` (task run `683688403903318`) vs prior failed run `754088150578631` (task run `29686894801434`), profile `fe-vm-classic`

## Findings

### HIGH severity
- (none)

### MEDIUM severity
- (none)

### LOW severity
- Staged-artifact stale dev string (not in target output): `jobs-bundle/geniefy_app/providers.py:117` contains the literal `projects/geniefy/branches/dev/endpoints/primary` — but it is a docstring example inside an error message, NOT a config default, and the file is a `.gitignored` build artifact that `deploy_jobs.sh` copies from `src/geniefy_app/providers.py` at deploy time (not part of U136's single-file output `jobs-bundle/databricks.yml`). No runtime effect: the Job's effective endpoint comes from the `lakebase_endpoint` var default (`…/branches/production/…`), confirmed live. Cosmetic only; fix if/when the src docstring is touched.

### UNVERIFIED
- (none)

## Detail (checks performed)

1. **existence — PASS.** `jobs-bundle/databricks.yml` declares `pg_host` default `ep-blue-smoke-d2yetcmv.database.us-east-1.cloud.databricks.com` (line 45), `lakebase_endpoint` default `projects/geniefy/branches/production/endpoints/primary` (line 50), `pg_database` default `geniefy` (line 47). The job `parameters` block wires each via `${var.pg_host}` / `geniefy` / `${var.lakebase_endpoint}` (lines 109-114). Matches the claim exactly.

2. **spec match — PASS.** Production host + production endpoint path + `pg_database=geniefy`, matching D57/U136. Var descriptions are accurate and cite D57/U136/U123 (lines 42-49). No `ep-curly-sunset` and no `branches/dev` references anywhere in the audited file (grep exit 1 = no match).

3. **cross-reference integrity — PASS.** D57 exists (`DECISIONS.md:671`, Status: locked) and supports the production-branch direction. U121 (`LEDGER.md:152`) and U123 (`LEDGER.md:154`) are both annotated `jobs-bundle/databricks.yml … superseded by U136`. The fix is real, not cosmetic: `_make_run_schema_job` (`src/geniefy_app/api.py:657`) passes ONLY run-specific params (`schema_run_id/catalog/schema/filters`) to `run_now` (lines 680-682) and explicitly relies on the job's declared bundle-var defaults for `pg_host/pg_database/lakebase_endpoint`; `schema_run_entry.py` (lines 56-72) consumes argv → `GENIEFY_PG_HOST` / `GENIEFY_LAKEBASE_ENDPOINT` env, which feed `AppConfig.from_env()` and the cluster cred mint. Editing the bundle var defaults IS the operative fix.

4. **internal consistency — PASS.** `databricks bundle validate -t dev -p fe-vm-classic` succeeds (1 advisory recommendation re: deploy-identity in `permissions`, 1 benign workspace-folder-permissions warning — no errors). `pg_host`'s branch (production `ep-blue-smoke-d2yetcmv`) matches `lakebase_endpoint`'s branch (`…/branches/production/…`). Consistent with the app binding (U135): `app/app.yaml:26` pins the same production host `ep-blue-smoke-d2yetcmv` — Job and app both target production.

5. **decision fidelity — PASS.** Honors D57: the Job uses the stable production branch for sustained persistence.

6. **LIVE verification — PASS.**
   - Deployed job params now production: `databricks jobs get 388957213112033` → `pg_host=ep-blue-smoke-d2yetcmv.database.us-east-1.cloud.databricks.com`, `pg_database=geniefy`, `lakebase_endpoint=projects/geniefy/branches/production/endpoints/primary`.
   - Fix run succeeded: `get-run 403025164429105` → `result_state=SUCCESS` (life_cycle TERMINATED, task `683688403903318` SUCCESS). Task output: `[schema_run] 471f9b22-a57e-48dc-88c6-8d5a5251bfe8 complete: {'total': 0, 'counts': {}}` — NO `password authentication failed`, no connection error. `total=0` is legitimate (the schema had no undocumented tables under skip_documented; the prod-Lakebase connect + lifecycle write is what's verified).
   - Contrast confirmed: prior run `754088150578631` → `result_state=FAILED`; task output `683688…`→ task run `29686894801434` shows `OperationalError: connection to server at "ep-curly-sunset-d2sz2asi.database.us-east-1.cloud.databricks.com" … password authentication failed for user 'bcc7089c-deac-4f42-b125-4e5681c7d8c1'` — the exact original symptom on the dead dev endpoint.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 1 · UNVERIFIED: 0
Verdict: PASS-FINDINGS   (HIGH ⇒ FAIL; MEDIUM/LOW-only ⇒ PASS-FINDINGS; clean ⇒ PASS)
