# Audit report — content-claim / structure — 2026-06-13

## Target audited
File: `deploy.sh` + `docs/DEPLOY.md`
Unit: U78

## Findings

### HIGH severity
- (none)

### MEDIUM severity
- (none)

### LOW severity
- (none)

### UNVERIFIED
- (none)

## Detail (per check)

### Check 1 — flag is wired + correct (PASS)
- **Arg parse:** `CODE_ONLY=0` initialized (deploy.sh:30); `--code-only) CODE_ONLY=1; shift;;` parsed (deploy.sh:51).
- **Usage:** `--code-only` listed in the flags line (deploy.sh:37) with a multi-line explanation (deploy.sh:38–40).
- **APP_NAME defined before step 4:** `APP_NAME="geniefy-${TARGET}"` at deploy.sh:62, well before step 4 (line 128) consumes it at lines 137/139.
- **Step 4 code-only branch (deploy.sh:130–140):** when `CODE_ONLY=1` it runs `bundle validate` (read-only, line 129 — runs unconditionally, harmless), then `bundle sync` (line 136), resolves `SRC_PATH` from `apps get "$APP_NAME" … .default_source_code_path` (line 137), and runs `apps deploy "$APP_NAME" --source-code-path "$SRC_PATH"` (line 139). It does **NOT** run `bundle deploy` or `bundle run`. The else-branch (lines 141–147) keeps the original `bundle deploy` (line 142) + `bundle run geniefy` (line 145). No path under `--code-only` reaches `bundle deploy`/`bundle run`.
- **Step 6 role re-provision (deploy.sh:170–179):** an explicit `if [[ "$CODE_ONLY" -eq 1 ]]` guard (line 170) prints "SP Lakebase role + grants preserved (not re-provisioned)" and skips; only the `elif [[ -n "$SP" ]]` branch (line 172) runs `postgres create-role` (line 173). No path under `--code-only` reaches `create-role`. The grant-guidance heredoc (lines 180–185) only prints `GRANT` SQL text — no execution.
- Verdict: flag is wired and correct.

### Check 2 — syntax (PASS)
- RAN `cd /Users/rohit.dashora/fe-vibe/geniefy-v3 && bash -n deploy.sh && echo SYNTAX_OK` → **SYNTAX_OK**.
- Step-4 header uses a real conditional: `step "4/7 Deploy app -t ${TARGET}$([[ "$CODE_ONLY" -eq 1 ]] && echo ' (code-only)')"` (deploy.sh:128) — command substitution with a numeric `-eq 1` test, NOT `${CODE_ONLY:+…}`. So "0" does not expand the `(code-only)` tag.

### Check 3 — doc accuracy (PASS)
- `docs/DEPLOY.md` describes first-deploy via `bundle deploy` (lines 6–12) vs `--code-only` redeploy via `bundle sync` + `apps deploy --source-code-path` (lines 27–42), matching deploy.sh exactly.
- One-time operator steps listed (lines 14–25): Lakebase `postgres`/`geniefy-db` binding, `fmapi-endpoint` serving binding, the `sql` OBO user-authorization scope (D48/U85), SP `SELECT` + user `MODIFY` + warehouse `CAN_USE` grants. Matches the U78 spec.
- Doc states `--code-only` "also skips the SP role re-provision in step 6" (line 38) and "Requires the app to already exist … `apps deploy` resolves the workspace source path from `apps get … .default_source_code_path`" (lines 41–42) — both match deploy.sh:137–138 and the step-6 guard. No command contradicts the script; no overclaims found.

### Check 4 — ledger (PASS)
- `.gotm/LEDGER.md` U78 row (line 104): Files cell = `deploy.sh (\`--code-only\` grant-safe redeploy) + docs/DEPLOY.md (operator runbook) (supersedes U77 deploy.sh)` — both target files listed.
- U77 row (line 103): deploy.sh carries `(superseded by U78)` in its Files cell.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 0 · UNVERIFIED: 0
Verdict: PASS   (HIGH ⇒ FAIL; MEDIUM/LOW-only ⇒ PASS-FINDINGS; clean ⇒ PASS)
