# Audit report — render / source-fidelity (frontend) — 2026-06-13

## Target audited
Files: `app/frontend/src/screens/ProfileStrip.tsx` (NEW), `app/frontend/src/screens/DraftCard.tsx`, `app/frontend/src/screens/ReviewView.tsx`, `app/frontend/src/api/types.ts`
Unit: U86 — per-column profile visual cues in DraftCard (E4)

## Oracle
- `docs/design/LLD-amend-003.md` §E4 (render per-column profile in DraftCard with existing `viz/` primitives, read-only "evidence at a glance")
- `src/geniefy_app/profiling.py` (ground-truth backend column-profile field names)
- `src/geniefy_app/api.py` `get()` (session view exposes `profile`, U84)

## Findings

### HIGH severity
- None.

### MEDIUM severity
- None.

### LOW severity
- **top_k rendered as text chips, not Sparkline/bars** — LLD §E4 suggests rendering `top_k` "as small bars/`Sparkline`" (`docs/design/LLD-amend-003.md:39`). `ProfileStrip` instead renders top-3 values as monospace text chips with `×count` (`ProfileStrip.tsx:45-55`). This honors the spec's intent ("evidence at a glance", read-only) and the value/count counts are shown, but is a presentational deviation from the literal suggestion. Non-blocking; fix only if a visual bar is desired.
- **`min`/`max`/`top_k.value` typed `unknown`** — `ColumnProfile.min`/`max` and `ColumnTopK.value` are `unknown` (`api/types.ts:56, 67-68`). This is the *safe* choice (not `any`), and the renderer coerces with `String(...)` (`ProfileStrip.tsx:34, 50`), so it does not defeat type-checking. Noted only because the backend stringifies temporals/Decimals (`profiling.py:_s`) and emits raw scalars otherwise — `unknown` correctly models that heterogeneity. No action needed.

### UNVERIFIED
- None. All six checks were mechanically verifiable from source + build.

## Detailed check results

1. **Field-name fidelity — PASS.** `ProfileStrip` destructures the exact backend keys `null_fraction, distinct_count, cardinality_ratio, min, max, is_enum_candidate, top_k, pii` (`ProfileStrip.tsx:9`); reads `top_k[].value` / `top_k[].count` (`:50-51`) and `pii.detected` / `pii.classes` (`:40, :42`). These match the column dict emitted by `InAppProfileProvider._profile_column` / `_sanitize` (`profiling.py:143-170, 230-239`: `null_fraction`, `distinct_count`, `cardinality_ratio`, `min`, `max`, `is_enum_candidate`, `top_k=[{value,count}]`, `pii={detected,classes,action}`). No mismatched key — viz will render live. Field-names-correct: **y**.

2. **Degrade — PASS.** `if (!profile) return null` guards absent profile (`:8`). An `anything` predicate (`:12-14`) returns null when no usable signal (empty table → all-null fields), satisfying "renders nothing when no signal". Every field is individually guarded: `null_fraction != null`, `distinct_count != null`, `hasRange = min != null && max != null`, `hasTop = Array.isArray(top_k) && top_k.length > 0`, `pii?.detected`, `cardinality_ratio != null` (`:11-13, 18, 26, 29, 32, 40, 45`). Any subset of fields missing renders without crashing. Degrades-safely: **y**.

3. **Wiring — PASS.** `ReviewView` builds `profileByCol` from `session.profile?.columns ?? []` keyed by `p.name` (`ReviewView.tsx:20-22`); the table card spec carries no `profile` key (`:25-27`) while column cards set `profile: profileByCol.get(c.column_name)` (`:28-34`); `<DraftCard profile={c.profile} />` is threaded (`:74`) and `DraftCard` renders `<ProfileStrip profile={profile} />` (`DraftCard.tsx:115`). Read-only: `ProfileStrip` performs no mutations, no review/apply coupling.

4. **Types — PASS.** `ColumnProfile`, `ColumnTopK`, `ProfileView` (`api/types.ts:55-76`) mirror the backend shape; `SessionView.profile?` is optional (`:85`). No `any` leak — scalars that vary by type are `unknown` (safe), not `any`.

5. **Reuses viz, no new heavy deps — PASS.** Uses existing `NullFractionBar` from `../viz` (`ProfileStrip.tsx:2`); no other viz/chart primitive imported into the main path. `package.json` adds no dependency (deps: @tanstack/react-query, @visx/{group,scale,shape}, clsx, react, react-dom, recharts, tailwind-merge — all pre-existing). JudgeRadar remains `lazy(() => import('../viz/JudgeRadar'))` (`DraftCard.tsx:9`) and builds as a separate chunk (see below) — bundle not regressed.

6. **Build gate — PASS.** `node_modules/.bin/tsc --noEmit` exited 0 (clean). `node_modules/.bin/vite build`: ✓ built, 1040 modules. Main bundle `index-CZWlQTM9.js` = **220.88 kB** (gzip 69.67 kB, matches expected ~220 kB); `JudgeRadar-8MXI1pAx.js` = 352.52 kB is a **separate lazy chunk** (recharts isolated, as intended). Typecheck-clean: **y**. Vite-build-clean: **y**.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 2 · UNVERIFIED: 0
field-names-correct: y · degrades-safely: y · typecheck-clean: y · vite-build-clean: y
Verdict: **PASS-FINDINGS**  (no HIGH/MEDIUM; two LOW cosmetic/spec-deviation findings → optional follow-on, non-blocking)
