# Audit report — content-claim (security/git-readiness, independent re-scan) — 2026-06-14

## Target audited
File: `docs/design/GIT-READINESS-ANALYSIS.md`
Unit: U130 (read-only git-readiness analysis; NO repo/code/git changes claimed)

## Method (independent — did not trust the doc)
This is a pre-publish gate, so every load-bearing claim was re-verified directly against
disk rather than read from the doc. Performed: an independent literal secret scan (PATs,
GitHub tokens, AWS keys, private keys, `PGPASSWORD=`, secret-assignment patterns, bearer
tokens, connection strings with embedded passwords) over the committable surface
(excluding `.venv/`, `node_modules/`, `.databricks/`, `app/static/`, `.git/`); inspected
`app/app.yaml`, both `.databricks/.gitignore` files, the top-level `.gitignore`, both
`.claude/settings*.json`, the `LICENSE`, the root for dep manifests, `src/`/`jobs/`/
`migrations/`/`scripts/`/`app/frontend/src/` for parent-workspace imports, the live
`git config`, and the working tree for evidence of changes.

## Findings

### HIGH severity
- **None.** No real secret or credential exists in the committable surface. Independent
  scans for `dapi[0-9a-f]+`, `gh[pous]_…`, `AKIA[0-9A-Z]{16}`, `BEGIN … PRIVATE KEY`,
  literal `PGPASSWORD=<value>`, `password|client_secret|access_token|api_key|secret_key`
  assigned to string literals, `Bearer <literal>`, and `postgres://user:pw@` all returned
  **zero** real hits. The only matches were: (a) `migrations/APPLY.md:38,42`
  `PGPASSWORD=$TOKEN` — a shell variable bound to a runtime-minted short-lived Lakebase
  credential (`databricks postgres generate-database-credential`), not a literal; (b) test
  fixtures with dummy values (`tests/test_apply.py` `access_token="user-tok"`/`"ignored"`,
  `tests/test_providers.py:210` `"Bearer cluster-tok"`); (c) the HTTP header-name constant
  `_TOKEN = "x-forwarded-access-token"` in `identity.py`; (d) code comments / design-doc
  prose describing the `Bearer …` placeholder shape; (e) `node_modules` framework strings
  (outside the committable surface — `app/frontend/node_modules/` is gitignored). None is a
  credential. **The doc's §1 "PASS (no secrets)" verdict is correct.**

### MEDIUM severity
- **None.** The doc's one material functional finding — the reproducibility gap (§4) — is
  accurate and correctly scoped; it is an advisory for a future fix unit (U132), not a
  defect in this read-only analysis. Confirmed independently: no root `requirements*.txt`,
  `pyproject.toml`, `setup.py`, or `setup.cfg` exists; only `app/requirements.txt` (App
  runtime) is present, so the 375-test `.venv` is genuinely unpinned.

### LOW severity
- **`app/app.yaml` literal Lakebase host not explicitly tabled in §1.** §1 asserts app.yaml
  carries "secret-scope references only," which is true for *auth* (verified:
  `"auth": {"secret_scope": "geniefy", "key": "glean_token"}` and `GENIEFY_SECRET_SCOPE:
  geniefy`, refs only — no raw tokens). The file *does* also carry a literal endpoint host
  (`GENIEFY_PG_HOST: ep-curly-sunset-…`), but this is a non-secret identifier and the doc
  does capture it in the §2 environment-identifiers table and §5. The §1 phrasing is
  defensible; flagging only because a reader skimming §1 alone might infer app.yaml has no
  literals at all. Cosmetic.

### UNVERIFIED
- **None material.** All central claims were independently checkable on disk and checked.

## Independent verification log (claim → result)
- **No secrets in committable surface** → CONFIRMED (scans above all clean; app.yaml
  auth = secret-scope refs only).
- **Terraform state self-ignored** → CONFIRMED. Both `.databricks/.gitignore` and
  `jobs-bundle/.databricks/.gitignore` contain exactly `*`, so `terraform.tfstate` cannot
  be committed even though the top-level `.gitignore` omits `.databricks/`. Doc §1 correct.
- **Self-contained (no parent-workspace coupling)** → CONFIRMED. Zero hits for
  `from services` / `import services` / `fe-vibe` / `fe_vibe` / `/services/` across `src/`,
  `app/frontend/src/`, `jobs/`, `migrations/`, `scripts/`, `app/geniefy_core/`. Doc §6
  "push geniefy-v3 alone" correct.
- **License = Apache-2.0** → CONFIRMED (`LICENSE` header: "Apache License, Version 2.0,
  January 2004"). Doc §7 correct.
- **Reproducibility gap (no root dep manifest)** → CONFIRMED (only `app/requirements.txt`
  exists; no root `requirements*.txt` / `pyproject.toml` / `setup.py` / `setup.cfg`).
  Doc §4 correct — this is the genuine main functional finding.
- **`.gitignore` gaps** → CONFIRMED. Current `.gitignore` does NOT name `.databricks/`,
  `jobs-bundle/.databricks/`, `.claude/settings.local.json`, or `.DS_Store`; no `.DS_Store`
  exists yet. Doc §3 recommended-additions list is accurate.
- **`.claude` settings split** → CONFIRMED. `.claude/settings.local.json` is user-specific
  (absolute path `//Users/rohit.dashora/fe-vibe/**` + a local permission allow-list) → do
  NOT commit; `.claude/settings.json` is the shared GOTM PreToolUse immutability hook wiring
  → worth committing. Doc §3 correct.
- **Git author email = personal gmail** → CONFIRMED (`git config --global user.email` →
  `rohitdashora@gmail.com`). Doc §7/§8 correct.
- **Naming drift (README title still geniefy-v3)** → CONFIRMED (`README.md` line 1:
  `# geniefy-v3`). Doc §6 correct.
- **"No repo/code/git changes made"** → CONFIRMED. No `.git/` directory exists (git init
  not run). The only non-ignored files touched this turn are
  `docs/design/GIT-READINESS-ANALYSIS.md` (the new output) and `.gotm/LEDGER.md` (the
  write-back) — exactly as expected. Other recently-modified paths (`app/static/`,
  `app/geniefy_core/`, `app/geniefy_app/`) are all gitignored deploy-staging build
  artifacts from an unrelated deploy, NOT committable surface.

## Completeness check (did the doc miss anything material?)
- No overlooked secret, connection string, or embedded credential found anywhere in the
  committable surface (independent extra sweep for bearer/conn-string/Authorization
  literals was clean).
- Large-artifact coverage is adequate: the doc accounts for `.venv/`, `node_modules/`,
  both 131 MB `.databricks/` dirs (tfstate), `app/static/`, the deploy-staging copies, the
  ~1.1 MB `.gotm/` machinery, and the ~3.6 MB `docs/assets/` screenshots.
- Portability/legal coverage is complete: Apache-2.0, env-coupled (non-secret) identifiers,
  personal-vs-work commit identity, and naming drift are all surfaced with correct
  private-vs-public severity framing.
- Severity calls are reasonable: §1 (security) and §4 (reproducibility) are the right two
  to foreground; everything else is correctly cast as a decision (§6/§8) or a public-only
  concern (§5).
- Write-back is intact: the LEDGER U130 row (`done`, `Audit: pending`) and the dated recent-
  updates entry accurately summarize the doc's findings with no overclaim.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 1 · UNVERIFIED: 0
Verdict: **PASS-FINDINGS** (one cosmetic LOW; the secret-free / git-readiness claims all
independently re-verified clean — safe for downstream git-prep units U131–U134 to proceed)
