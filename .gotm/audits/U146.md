# Audit report — content-claim / internal-consistency — 2026-06-23

## Target audited
File: `src/geniefy_app/providers.py` (`_statement_state`, `_await_terminal`, `_rows_from_statement_response`, and the execute closures in `databricks_sql_execute` + `make_obo_sql_execute`)
File: `tests/test_providers.py` (the new `test_sql_execute_polls_running_on_the_sp_runner` SP-runner poll test + the existing `test_await_terminal_*` trio)
Unit: U146 (fix for the HIGH that FAILed U145 — `.gotm/audits/U145.md`)

## What was checked (HIGH-closure + 5-point + no-regression)

1. **HIGH closed (load-bearing)** — `grep -n "_await_terminal" src/geniefy_app/providers.py` returns exactly three lines: the definition (178) and **two** call sites — `databricks_sql_execute`'s `execute` (line **237**, the SP READ runner the HIGH was about) and `make_obo_sql_execute`'s inner `execute` (line **266**, the OBO write runner). Both read `return _rows_from_statement_response(_await_terminal(w, resp))`. Grep for `_rows_from_statement_response(resp)` (the unwired form the U145 audit flagged at the old line 237) returns **zero** matches — no runner maps rows directly anymore. The SP runner that feeds Profiler/ContextGatherer/enumeration is now wired. ✅ The U145 HIGH is closed.

2. **regression guard exists** — `test_sql_execute_polls_running_on_the_sp_runner` (tests/test_providers.py:236–271) exercises `databricks_sql_execute` end-to-end through a fake SDK whose `execute_statement` returns a RUNNING response and whose `get_statement` returns SUCCEEDED. It asserts `gets["n"] == 1` (the SP runner polled to terminal) and `rows == []` (the RUNNING response was NOT silently mapped as `[]`). Removing `_await_terminal` from the SP runner would make `gets["n"] == 0` → the test FAILs — exactly the guard the 379-suite lacked. It does **not** sleep for real: line 244 `monkeypatch.setattr(_time, "sleep", lambda *a, **k: None)` (the `import time` inside `_await_terminal` returns the same cached module object the test patched). Confirmed empirically: the whole `test_providers.py` module runs in 0.02s. ✅

3. **no regression** — `PYTHONPATH=src .venv/bin/python -m pytest -q` → **380 passed, 1 skipped, 1 warning** (the warning is a pre-existing StarletteDeprecationWarning, unrelated). Matches the claimed 380/1. The existing hermetic fakes all pass: `test_sql_execute_mints_fresh_client_per_call` (134), the FAILED-raises test `test_rows_from_statement_response_raises_on_failed_statement` (164), and the await-terminal trio `test_await_terminal_polls_running_until_terminal` (186) / `_gives_up_on_stuck_statement` (207) / `_noop_when_already_terminal_or_no_status` (220) — verified green both in the full run and run in isolation (6 load-bearing tests, 0.02s). ✅

4. **cross-reference integrity** — U145 row (LEDGER.md:186) annotates both outputs `superseded by U146` and its `Audit` cell reads `FAIL→.gotm/audits/U145.md … → BLOCKED, fixed by U146`. U146 row (LEDGER.md:187) cites `.gotm/audits/U145.md, U145` as inputs and `supersedes U145` for both files. The 2026-06-23 recovery-log entry (LEDGER.md:248) accurately describes the root cause (`replace_all` matched only the deeper-indented OBO closure; no SP-runner await test → gap shipped), the fix (wired into `databricks_sql_execute` providers.py:237 + the new SP poll test), and the count (380/1). The grep-cited line numbers (`:237 SP + :266 OBO`) match the code. ✅

5. **correctness unchanged** — `_await_terminal` (178–196) still: polls only while `_statement_state(resp) in ("PENDING","RUNNING")` (188); caps at `max_polls` then **raises** `RuntimeError("…did not reach a terminal state…")` (189–192) — no unbounded loop; returns the response at once on a terminal or absent status without calling `get_statement` (the loop guard, verified by `test_await_terminal_noop_…` whose `get_statement` raises `AssertionError` if touched). `_rows_from_statement_response` (199–223) still raises on FAILED/CANCELED/CLOSED (207–211) and returns rows/`[]` for SUCCEEDED/absent-status (212–223). `_statement_state` (170–175) unchanged. D48/U98 untouched: `databricks_sql_execute` still mints a fresh `WorkspaceClient` per call (line 233, U98 session-bound), `make_obo_sql_execute` resolves `host` once (line 257, the U90 fix) and builds a token-authenticated client per `make` (line 260); no new caching introduced — the U146 edit added only `_await_terminal(w, resp)` around the existing return. ✅

## Findings

### HIGH severity
- (none) — the U145 HIGH is closed: `_await_terminal` is now wired into BOTH runners (providers.py:237 SP + :266 OBO); no runner maps rows without polling; the regression guard exists and the suite is green at 380/1.

### MEDIUM severity
- (none)

### LOW severity
- (none)

### UNVERIFIED (carried from the U145 audit — out of U146 scope; non-blocking)
- **`max_polls=150 × poll_seconds=2s` ≈ 5-minute ceiling unjustified.** A reasonable stuck-statement guard, but no source ties the 5-min cap to a documented warehouse/profiling timeout expectation; could be short for a genuinely large-table profile or long as a hang guard. Carried verbatim from `.gotm/audits/U145.md`; U146's scope was "wire the SP runner + add the test," not re-tuning the ceiling. Route to the practitioner.
- **`statement_id=None` on a non-terminal response.** If `get_statement` is reached with a non-terminal response lacking `statement_id` (line 190/195 read it via `getattr(resp, "statement_id", None)`), `get_statement(None)` would surface as an SDK error rather than a silent hang — acceptable but unasserted. Carried from the U145 audit; not in U146 scope.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 0 · UNVERIFIED: 2 (both carried from U145, out of U146 scope, non-blocking)
Verdict: **PASS-FINDINGS** (the U145 HIGH is fixed — `_await_terminal` wired into both `databricks_sql_execute` (the SP read runner the HIGH named) and `make_obo_sql_execute`; the regression guard `test_sql_execute_polls_running_on_the_sp_runner` exists, is hermetic (no real sleep), and would FAIL if the SP wiring were removed; full suite green at 380 passed / 1 skipped; D48/U98 untouched. The 2 UNVERIFIED are pre-existing, out-of-scope notes carried from the U145 audit, not defects in U146 — they do not block. **This PASS-FINDINGS clears the U145 FAIL**; downstream/code units may proceed.)
