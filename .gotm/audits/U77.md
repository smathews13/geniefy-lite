# Audit report — code (live-deploy remediation: 10 integration fixes) — 2026-06-12

## Target audited
Files:
- `src/geniefy_app/providers.py`
- `src/geniefy_core/llm.py`
- `src/geniefy_app/store.py`
- `src/geniefy_app/api.py`
- `databricks.yml`
- `app/app.yaml`, `app/requirements.txt`, `.gitignore`, `deploy.sh`
- `tests/test_providers.py`, `tests/test_llm.py`

Unit: U77 (live-deploy remediation — 10 integration fixes surfaced by the U76 live deploy, D36)
Auditor: independent subagent (did not author U77). Mechanical only — no fixes, no ledger edits.

## Oracle
- `DEPLOY_VERIFY.md` (the findings→fixes record, 10 items)
- `.gotm/DECISIONS.md` — D36 (`src/geniefy_app/` packaging; app self-OAuths via injected `PG*`), D4 (FMAPI Claude / sanitized profile)
- `.gotm/audits/U62.md` (prior deploy-orchestration audit — PASS-FINDINGS)
- `.gotm/LEDGER.md` U77 row (10-item scope)

## Mechanical command results
- `PYTHONPATH=src /tmp/geniefy_pgvenv/bin/python -m pytest tests/ -q` → **280 passed**, 1 warning (StarletteDeprecationWarning, pre-existing/unrelated). Exit 0.
- `PYTHONPATH=src python3 -c "import geniefy_app.api, geniefy_app.store, geniefy_app.providers, geniefy_core.llm; print('ok')"` → **ok** (SDK-free imports hold; no eager `databricks`/`openai`/`psycopg2`).
- `databricks bundle validate -t dev -p fe-vm-classic` → **Validation OK!** (exit 0).
- `databricks bundle validate -t prod -p fe-vm-classic` → **Validation OK!** (exit 0; prod `root_path` principal-scoped to `/Workspace/Users/<user>/.bundle/geniefy-v3/prod`).

## Findings

### HIGH severity
- None.

### MEDIUM severity
- None.

### LOW severity
- None.

### UNVERIFIED
- **Live end-to-end run is integration-verified, not re-exercised here.** The deployed-App run on `samples.tpch.orders` (full loop → `awaiting_input`, profile-grounded comments) is recorded in `DEPLOY_VERIFY.md` (D36 boundary) and is explicitly out of hermetic scope per the audit prompt. Not counted against the verdict.
- **Operational follow-ons (not code defects).** Role-churn-on-deploy (`bundle deploy` re-creates the SP Lakebase role and drops the schema grant → re-grant per deploy) and the UI/`apps update`-added Autoscaling `postgres` binding are documented operator steps in `DEPLOY_VERIFY.md`, not code under U77. Per the audit prompt, do not fail.

## Detailed check results — the 10 fixes vs. their findings

**Fix 1 — app `database` binding removed (DEPLOY_VERIFY #1): PASS.**
`databricks.yml` app `resources:` lists only `sql-warehouse` (line 89); no `database`/`postgres` resource binding remains. The rationale (Autoscaling Lakebase projects aren't a DAB-expressible app resource; the app self-OAuths; the `postgres` binding added via UI/`apps update`) is documented inline (lines 83–87). `grep -E "database|postgres|lakebase"` shows only comments, the `lakebase_instance` var, the `pg_host` default, and the `geniefy_setup` job — no app-resource binding. Consistent with D36 (app self-OAuths via injected `PG*`).

**Fix 2 — core/SPA packaging via `sync.include` + deploy.sh staging (DEPLOY_VERIFY #2): PASS.**
`databricks.yml` `sync.include` force-uploads `app/geniefy_core/**`, `app/geniefy_app/**`, `app/static/**` (lines 16–19) — exactly the gitignored build artifacts (`.gitignore` lines 8–16) that `bundle sync` would otherwise drop. `deploy.sh` stages the core (`rm -rf app/geniefy_core app/geniefy_app; cp -R src/geniefy_core src/geniefy_app app/`, lines 94–95) and builds the SPA with a guard `[[ -f app/static/index.html ]] || die` (line 90). `sync.exclude` still drops `.gotm/`/`.claude/` (D25). Coherent.

**Fix 3 — missing runtime deps (DEPLOY_VERIFY #3): PASS.**
`app/requirements.txt` adds `openai`, `psycopg2-binary`, `pyyaml` (with correct per-line provenance: providers→openai, store→psycopg2, template→pyyaml). Matches the three modules' lazy imports.

**Fix 4 — deploy.sh starts the app (DEPLOY_VERIFY #4): PASS.**
`deploy.sh` step 4 runs `databricks bundle run geniefy -t "$TARGET"` after deploy (line 126), `|| die` on failure. Present and ordered after `bundle deploy`.

**Fix 5 — serving-endpoint OpenAI client w/ per-call OAuth (DEPLOY_VERIFY #5): PASS.**
`providers.py:databricks_chat_query` builds `OpenAI(base_url=f"{host}/serving-endpoints", api_key=w.config.oauth_token().access_token)` per call (lines 92–96) — no `serving_endpoints.get_open_ai_client()` (the nonexistent SDK helper). Lazy-imports `WorkspaceClient` + `openai` inside the factory (SDK-free module import — verified by the import check + `test_lazy_factories_import_sdk_only_when_called`). `completion.model_dump()` returns the dict `LLMClient._parse` consumes.

**Fix 5b — FmapiChatTransport omits `response_format` (DEPLOY_VERIFY #8): PASS — load-bearing, confirmed.**
`FmapiChatTransport.send` builds the payload with only `messages`/`max_tokens`/`temperature`; `response_format` is accepted for the `ChatTransport` protocol but explicitly NOT added (`_ = response_format`, lines 54–63). Verified the payload never contains `response_format`. A simulated forwarding-regression makes the guard test fail → the test bites.

**Fix 5c — `databricks_sql_execute` casts numeric columns via the manifest (DEPLOY_VERIFY, profiler arithmetic): PASS — load-bearing, confirmed.**
`_value_caster` casts INT/LONG/SHORT/BYTE/INTEGER/BIGINT/SMALLINT/TINYINT→int and FLOAT/DOUBLE/DECIMAL/REAL/NUMERIC→float (lines 102–116). Verified: the finding's named types INT/LONG/FLOAT/DECIMAL are all covered; numeric strings cast to int/float; non-numeric pass through; a bad cast falls back to the raw string (`_try_cast`); the SDK enum `type_name.value` form is handled (`getattr(type_name, "value", type_name)`). `databricks_sql_execute` maps manifest columns + `data_array` to row dicts applying the per-column caster (lines 134–149), guarding `None` and index bounds.

**Fix 6 — `_lakebase_connection` prefers binding-injected `PG*` w/ fallback (DEPLOY_VERIFY #6): PASS — load-bearing, confirmed.**
`api.py:_lakebase_connection` reads all five — `PGHOST`/`PGPORT`/`PGDATABASE`/`PGUSER`/`PGSSLMODE` — via `os.environ.get(...)` with `GENIEFY_*`/SDK-identity fallback (lines 327–331). Verified each of the five is read with a fallback. Password is a short-lived OAuth DB credential. `psycopg2`/SDK lazy-imported inside the function (module import stays SDK-free).

**Fix 7 — SessionStore rolls back a poisoned/aborted txn (DEPLOY_VERIFY #7): PASS — load-bearing, confirmed.**
`store.py:_reset()` swallows-rollback (lines 134–141) and is called at the **start** of `save` (line 70), `append_audit` (line 153), and `load` (line 170); `save` also wraps `_write` in try/except with a rollback-and-reraise (lines 71–77). Verified behaviorally with a fake connection: the first DB op on `save` is a rollback (reset-before-write), and a failing write triggers a second rollback + re-raise — so one failed op cannot cascade `InFailedSqlTransaction`.

**Fix 8 — `LLMResponse.json()` strips a ```json fence + outermost-`{}` fallback; `DEFAULT_MAX_TOKENS` 1024→4096 (DEPLOY_VERIFY #9, #10): PASS — load-bearing, confirmed.**
`llm.py:LLMResponse.json()` (lines 73–93): strips a leading ```` ``` ````/` ```json ` fence and trailing ```` ``` ````, then `json.loads`; on failure falls back to the outermost `{…}`; raises `LLMError("bad_json")` if still invalid. Verified all four cases: fenced-with-`json`, bare-fence (returns a list), **plain `{...}` with no fence still parses (no regression)**, preamble+object recovered, and `"oops"` raises `LLMError`. `DEFAULT_MAX_TOKENS = 4096` (line 27), documented as a cap (cost = what the model emits).

**Fix 9 — databricks.yml: no `${var}` on auth host, `geniefy_setup` job, prod `root_path`: PASS.**
No `${var}` interpolation on any `workspace.host`/auth field; `host` is a documentation-only `variables:` entry (lines 24–30, 59–62). `geniefy_setup` job present (`spark_python_task` → `run_migrations.py`, `psycopg2-binary` dep, lines 100–113). Prod `root_path` principal-scoped (line 71). Both targets `Validation OK!`.

**Fix 10 — the two new/changed tests bite: PASS.**
`tests/test_providers.py::test_chat_transport_does_not_forward_response_format` asserts `"response_format" not in captured["payload"]` — fails against a forwarding regression (confirmed). `tests/test_llm.py::test_response_json_strips_markdown_fence` asserts the ```json-fence strip, bare-fence list, and preamble+`{}` fallback — asserts the real fix. Both run green in isolation and within the full 280-pass suite.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 0 · UNVERIFIED: 2 (live e2e + operational follow-ons, both explicitly out of hermetic scope per the audit prompt)
Verdict: **PASS**

All 10 fixes are correct and match their DEPLOY_VERIFY findings; the five load-bearing ones are behaviorally confirmed (lenient JSON parse strips fences AND keeps plain-`{}` parsing with no regression; `_reset` runs before each op + failure re-rollback; `_lakebase_connection` reads all five `PG*` with fallback; `FmapiChatTransport` omits `response_format`; `databricks_sql_execute` casts INT/LONG/FLOAT/DECIMAL via the schema). Hermetic suite 280 passed; SDK-free imports hold; bundle validates dev + prod.
