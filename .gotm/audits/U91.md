# Audit report — content-claim / decision-fidelity — 2026-06-12

## Target audited
Files:
- `src/geniefy_app/identity.py` (NEW — `RequestIdentity`)
- `src/geniefy_app/api.py` (the `_identity` dependency + `/apply` wiring; `_actor()` unchanged)
- `tests/test_identity.py` (NEW)
- `tests/test_apply_wiring.py` (updated)
Unit: U91

## Oracle
- `.gotm/DECISIONS.md` → **D48 + its "Refinement (2026-06-12, human)"** (the binding contract: end-user identity used ONLY for the UC apply write-path + that apply's `audit_log.actor`; everything else runs as the app SP; session `created_by` = SP).
- `docs/design/LLD-amend-004.md` §A3 (RequestIdentity shape; doc frozen, R1 not in it — D48 Refinement is authoritative).

## Findings

### HIGH severity
- None.

### MEDIUM severity
- None.

### LOW severity
- None.

### UNVERIFIED
- None.

## Check-by-check

**1. Refinement compliance (critical) — PASS.** `RequestIdentity` is extracted at exactly one place: the `/apply` route (`api.py:248`, `identity: RequestIdentity = Depends(_identity)`), consumed at `api.py:254` (`created_by=identity.actor`). The `_identity` dependency (`api.py:196-199`) is referenced nowhere else. The `run` (`api.py:212-218`), `answers` (`api.py:227-236`), and `review` (`api.py:238-245`) routes ALL use `_actor()` → the SP literal `"app-service-principal"` (`api.py:191-194`). Grep of `_identity` / `RequestIdentity` / `identity.` in api.py confirms zero leakage of the forwarded identity into any non-apply route. No HIGH violation of the human directive.

**2. apply actor = user — PASS.** `/apply` passes `created_by = identity.actor`. `actor` (`identity.py:40-44`) returns `email → user_id → "anonymous"`, never the SP. `test_apply_wiring.test_apply_delegates_with_user_actor_from_header` asserts `applier.calls == [("sid1", "user@x.com")]` from an `X-Forwarded-Email` header; `test_apply_actor_anonymous_without_forwarded_identity` asserts `("sid1", "anonymous")` with no header. The SP literal can never reach the apply audit row.

**3. RequestIdentity correctness — PASS.** Header constants map correctly: `_EMAIL=x-forwarded-email`, `_USER=x-forwarded-user`, `_USERNAME=x-forwarded-preferred-username`, `_TOKEN=x-forwarded-access-token`, `_REQUEST_ID=x-request-id` (`identity.py:23-27`). `from_headers` lowercases keys (case-insensitive, `identity.py:54`) and turns blank/missing → `None` via `g()` strip-then-`or None` (`identity.py:56-59`). `actor` precedence email→user_id→"anonymous" (`identity.py:44`). `has_obo == bool(access_token)` (`identity.py:49`). `local()` returns `email="local-dev"` with no token (`identity.py:67`) → has an identity, `has_obo` False. All asserted in `test_identity.py` (6 tests covering full-set/case-insensitive, lowercased keys, blank→None, actor precedence, empty→anonymous, local no-token).

**4. hermetic — PASS.** RUN: `PYTHONPATH=src python3 -c "import sys, geniefy_app.identity; print('leaked:', [m for m in sys.modules if m.split('.')[0] in ('fastapi','starlette','databricks','openai','psycopg2')] or 'none')"` → output `leaked: none`. `identity.py` imports only `dataclasses` + `typing` (stdlib). The fastapi/Request/Depends imports live inside `create_app` in api.py, not in identity.py.

**5. tests bite + pass — PASS.**
- Targeted: `PYTHONPATH=src /tmp/geniefy_pgvenv/bin/python -m pytest tests/test_identity.py tests/test_apply_wiring.py -q` → **9 passed**.
- Full suite: `PYTHONPATH=src /tmp/geniefy_pgvenv/bin/python -m pytest tests/ -q` → **292 passed**.
- `test_apply_wiring` asserts the apply actor == `"user@x.com"` from `X-Forwarded-Email` and `"anonymous"` without it (both branches present). No non-apply route test broke (run/answers/review tests in test_api.py still assert the SP actor; full suite green).

**6. ledger hygiene — PASS.** `.gotm/LEDGER.md` U91 row (line 117) lists all 4 files (`identity.py` + `api.py` + `test_identity.py` + `test_apply_wiring.py`) with the supersede annotation `(supersedes U81 for api.py, U54 for test_apply_wiring.py)`. U81 row (line 107) carries `api.py (build_service, superseded by U91)`. U54 row (line 80) carries `tests/test_apply_wiring.py (superseded by U91)`. U84 (line 110) is `pending` and its description/output does NOT take `created_by` from identity (history/library backend only) — consistent with the revert; Recent-updates (line 190) records "session `created_by` = SP (reverted U84)".

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 0 · UNVERIFIED: 0
Verdict: PASS

apply-only-uses-identity: y
non-apply-routes-stay-SP: y
hermetic: y
pytest (targeted test_identity + test_apply_wiring): 9 passed
pytest (full suite): 292 passed
