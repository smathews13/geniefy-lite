# Audit report — code (3 backend LOW closures + tests) — 2026-06-23

## Target audited
Files:
- `src/geniefy_core/llm.py` — `TransportError` removal (closes U32 LOW)
- `src/geniefy_app/store.py` — `append_audit` rollback parity + `list_sessions` docstring (closes U93 LOWs)
- `src/geniefy_app/api.py` — `start_schema_run` honest status on no-progress (closes U110 LOW)
- `tests/test_api.py` — 2 new status-edge tests
Unit: U148

## Oracle
- `.gotm/audits/U32.md` LOW — dead `TransportError(Exception)` class in `llm.py` (declared, never caught; retry loop catches broad `Exception`) → remove it.
- `.gotm/audits/U93.md` LOWs — `append_audit` lacked the defensive rollback `save`/`upsert_library_entry` have → add parity; `list_sessions` docstring said "reuses the shared connection" (pre-D49) → fix to per-call/D49.
- `.gotm/audits/U110.md` LOW — `start_schema_run` left status `enumerating` when no Job triggered → mark honestly.
- `.gotm/LEDGER.md` U148 row (in_progress); supersession of U80 (llm.py), U109 (store.py), U138 (api.py).

## Method
- `grep -rn TransportError` over `src/` and all `*.py` repo-wide → confirm fully removed and never imported.
- Read `llm.py:100-247` (retry loop, error taxonomy), `store.py:90-300` (`save`/`append_audit`/`list_sessions`/`upsert_library_entry`), `api.py:236-305` (`start_schema_run` + neighbours).
- Read the 2 new tests + the existing `test_append_audit_inserts_row`; grepped for any rollback-path test.
- Ran full suite, then targeted `test_llm.py` / `test_store.py` / `test_api.py` and the retry/backoff-named tests.
- Mutation probe: reverted the U110 status-edge fix in a scratch copy and re-ran the 2 new tests to confirm they bite.

## Findings

### HIGH severity
- (none)

### MEDIUM severity
- (none)

### LOW severity
- **No regression test for the `append_audit` rollback path** (`tests/test_store.py:214` covers only the success/commit path; no test asserts `conn.rollback()` fires on a write error). The parity itself is correct by inspection — `append_audit` (`store.py:184-200`) reproduces the exact shape of the tested-by-shape `save` (`store.py:104-113`) and `upsert_library_entry` (`store.py:271-…`): `try` wraps execute+commit, `except` does a nested-try `conn.rollback()` (swallowed so a dropped-conn rollback can't mask the real error) then `raise`. Note this is a *uniform* gap — neither `save` nor `upsert_library_entry` has a rollback-path test either, so `append_audit` is now exactly as well-tested as its siblings, not worse. The pre-existing U93 finding anticipated this. Optional follow-on: one test asserting `rollbacks == 1` + re-raise on a failing-cursor `FakeConn` for all three writes. Non-blocking.

### UNVERIFIED
- (none) — every check was mechanically runnable from source + the hermetic suite.

## Detailed check results

**1. U32 — dead `TransportError` removed — PASS.** `grep -rn TransportError src/` → no matches; `grep -rn TransportError` over all `*.py` repo-wide → no matches (truly dead, no importer anywhere — confirms the U32 LOW's "never referenced" claim). The retry/backoff logic in `llm.py:204-223` is unchanged and still correct: `complete()` re-raises `LLMError` (structured, not retried, `:216-217`) and treats any other `Exception` as transient (`:218-221`), sleeping `_retry_delay` (Retry-After or full-jitter backoff, `:225-232`) up to `max_retries+1` attempts, then wrapping the last failure as `LLMError("transport_error", …)`. No behavior change — the class was never in the catch path.

**2. U93 — rollback parity + docstring — PASS (1 LOW above).**
- `append_audit` (`store.py:183-200`): now `with self._connection() as conn:` → `try:` { cursor.execute INSERT; `conn.commit()` (`:194`) } `except Exception:` { nested-try `conn.rollback()` swallowing a rollback failure (`:196-199`); `raise` (`:200`) }. Byte-for-byte the `save` pattern. Success path unchanged — still commits (`test_append_audit_inserts_row` asserts `conn.commits == 1`). The rollback guard cannot mask the real error: the inner `except` only swallows a *rollback* failure, the outer `raise` re-propagates the original.
- `list_sessions` docstring (`store.py:226`): now reads "Read-only; uses a per-operation connection (D49)." The stale "reuses the shared connection" phrase is gone. Behavior unchanged (`with self._connection() as conn`, `:230`).

**3. U110 — honest status on no-progress — PASS.** `start_schema_run` (`api.py:243-257`): the record is created at `'enumerating'` (store.py:424, the sole INSERT) and every exit transitions it:
- `run_schema_job is None` → `finalize_schema_run(run_id, status="failed")` + raise `SessionError(501)` (`:245-247`);
- trigger raises → `finalize_schema_run(…, "failed")` + raise `SessionError(502)` (`:250-252`);
- trigger returns falsy `job_run_id` → `finalize_schema_run(…, "failed")` + raise `SessionError(502)` (`:253-255`);
- success → `update_schema_run(…, status="running", job_run_id=int(job_run_id))` (`:256`).
`start_schema_run` is the only caller of `create_schema_run`, so no code path leaves a record stuck at `'enumerating'`.

**4. Tests + no regression — PASS.** Full suite `PYTHONPATH=src .venv/bin/python -m pytest -q` → **383 passed, 1 skipped, 1 warning** (matches the claim exactly). Targeted: `test_llm.py` 24 passed (incl. 5 retry/backoff/persistent/transport tests), `test_store.py` 39 passed, `test_api.py` 41 passed. The 2 new tests assert **both** the status code AND the record status:
- `test_start_schema_run_no_run_id_marks_failed_502` (`test_api.py:484-490`) — asserts `502` AND `store.schema_runs[rid]["status"] == "failed"`.
- `test_start_schema_run_no_job_wired_marks_failed_501` (`test_api.py:493-500`) — asserts `501` AND `status == "failed"`.
Mutation probe confirms they bite: reverting the status-edge fix (both edge paths just `return {"schema_run_id": run_id}`, leaving `'enumerating'`) → **both tests FAIL** (502-test gets no error; 501-test gets `202 == 501`). For the `append_audit` rollback path, no biting test exists — see LOW.

**5. Cross-references — PASS.** U148 row (`LEDGER.md:191`) lists outputs `src/geniefy_core/llm.py (supersedes U80)` + `src/geniefy_app/store.py (supersedes U109)` + `src/geniefy_app/api.py (supersedes U138)` + `tests/`; all three source files exist at those paths. U80 (`:124`, llm backoff, PASS-FINDINGS), U109 (`:154`, store per-call conn, PASS-FINDINGS), U138 (`:182`, api enumerate_tables, PASS) are all `done` and audited — the supersession lineage is well-formed. The U80 retry/backoff tests, the U109 store tests, and the prior U110/U138 schema-run API tests all still pass with no behavior change beyond the intended status-edge fix.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 1 · UNVERIFIED: 0
TransportError removed (repo-wide): **YES** · append_audit rollback parity: **YES** · list_sessions docstring fixed: **YES** · start_schema_run never leaves 'enumerating': **YES**
Full suite: **383 passed / 1 skipped** · new status-edge tests bite (mutation-confirmed): **YES**
Verdict: **PASS-FINDINGS**   (no HIGH/MED; 1 LOW = no rollback-path test, parity correct-by-inspection vs the equally-untested `save` sibling)
