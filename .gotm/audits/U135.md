# Audit report — content-claim + live-verification — 2026-06-22

## Target audited
Files: `app/app.yaml`, `deploy.sh`
Live state: `geniefy-dev` app (workspace `fe-vm-classic`) + production Lakebase branch
Unit: U135

## Method
Audited the two file outputs against D57/U135 (existence, spec match, internal consistency,
syntax), the cross-references (D57, U94/U117/U118 supersession), the api.py precedence logic the
claim relies on, then independently re-ran the load-bearing live checks (app binding, the three
API endpoints, and a direct psycopg2 connection to the production `geniefy` db to verify schema +
SP grants). Auditor did not author U135.

## Findings

### HIGH severity
- (none)

### MEDIUM severity
- (none)

### LOW severity
- (none)

### UNVERIFIED
- (none — every load-bearing claim was independently confirmed live; see Evidence)

## Evidence (each check + result)

1. **existence — PASS.** `app/app.yaml` line 25-31: `GENIEFY_PG_HOST` =
   `ep-blue-smoke-d2yetcmv.database.us-east-1.cloud.databricks.com` with an accurate comment
   ("Lakebase PRODUCTION-branch endpoint host (D57/U135)"). `deploy.sh` line 65:
   `DB_BRANCH="${GENIEFY_LAKEBASE_BRANCH:-production}"` with the override preserved (lines 60-64).

2. **spec match — PASS.** Both edits match D57 exactly: production host literal; default-to-production
   for every target; `GENIEFY_LAKEBASE_BRANCH` override retained; comments accurately explain the
   dev-endpoint-churn rationale and the DAB-no-`${var}`-substitution constraint.

3. **cross-reference integrity — PASS.** D57 exists (`.gotm/DECISIONS.md` line 671) and states what
   U135 claims (repoint dev→production, host churn rationale, `app.yaml`+`deploy.sh` changes, prod
   migrated 001-004, SP granted a role, data-loss trade-off acknowledged). U94 (line 123), U117
   (line 149), U118 (line 140) are each annotated `superseded by U135` for the relevant file. No
   dangling refs.

4. **internal consistency — PASS.** `bash -n deploy.sh` clean. `app.yaml` fallback host
   (`ep-blue-smoke-d2yetcmv…`) is the production endpoint and agrees with the live production binding
   — no contradiction. `git status` shows only intended files modified: `app/app.yaml`, `deploy.sh`
   (U135), plus `.gotm/DECISIONS.md`+`.gotm/LEDGER.md` (governance write-back) and
   `jobs-bundle/databricks.yml` (U136's output, not U135's — expected per the ledger). No unintended
   files touched.

5. **decision fidelity — PASS.** Honors D57: sustained persistence via the stable production endpoint;
   data-loss trade-off explicitly acknowledged in both the D57 text and the recovery log. api.py
   `_lakebase_connection` (lines 644-647) confirms the precedence the claim relies on:
   `PGHOST or config.pg_host`, `PGDATABASE or config.pg_database`, `PGUSER or current_user` — i.e. the
   binding-injected env wins, with `app.yaml` as the coherent fallback. The claim's framing (binding =
   operative path, file edits = fallback/coherence) is faithful.

6. **LIVE verification — PASS (all sub-checks):**
   - **App binding:** `databricks apps get geniefy-dev -p fe-vm-classic -o json` → `geniefy-db`
     postgres binding `branch = projects/geniefy/branches/production`,
     `database = projects/geniefy/branches/production/databases/db-t6z0-smq5yhsuiy`,
     `permission = CAN_CONNECT_AND_CREATE`. The other two bindings (`sql-warehouse`, `fmapi-endpoint`)
     are preserved (postgres = null, as expected). App `state = ACTIVE`, `app_status = RUNNING`.
   - **API reachability + Lakebase reads/writes:** with a fresh `fe-vm-classic` bearer token,
     `GET /api/sessions` → **HTTP 200** (and non-empty: a real session row,
     `rd_classic_catalog.gaming_data.gold_arpdau`); `GET /api/library` → **HTTP 200** `{"entries":[]}`;
     `GET /api/schema-runs` → **HTTP 200** (non-empty: `471f9b22…` completed, job_run_id
     403025164429105). No 500s. Live read+write against the production branch confirmed.
   - **Production schema migrated:** minted a prod DB credential and connected via psycopg2 to host
     `ep-blue-smoke-d2yetcmv…` db `geniefy` user `rohit.dashora@databricks.com`. The `geniefy` schema
     contains exactly the 9 tables the migrations 001-004 declare: `templates`, `context_providers`,
     `sessions`, `session_messages`, `table_drafts`, `column_drafts`, `comment_library`, `audit_log`
     (001) + `schema_runs` (004). (No `schema_migrations` tracking table — expected; `run_migrations.py`
     applies idempotent `CREATE … IF NOT EXISTS` DDL, no version ledger.)
   - **SP grants:** `has_schema_privilege('bcc7089c-deac-4f42-b125-4e5681c7d8c1','geniefy','USAGE')` =
     **true**; `has_table_privilege(SP,'geniefy.sessions','SELECT'/'INSERT'/'UPDATE')` = **true**;
     `has_table_privilege(SP,'geniefy.schema_runs','INSERT')` = **true**.

## Note (not a finding)
The audit prompt's sample check referenced `geniefy.library` as a SELECT-grant target; no such relation
exists (the table is `geniefy.comment_library`, and the `/api/library` endpoint reads it and returns 200).
This is an artifact of the audit prompt's example, not a U135 claim or defect — U135 never claims a
`library` table. All grant claims U135 actually makes are verified true.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 0 · UNVERIFIED: 0
Verdict: PASS
