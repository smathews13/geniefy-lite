# Audit report — content+code (tests for AppConfig) — 2026-06-12

## Target audited
File: `tests/test_app_config.py`
Unit: U44 (build: tests for `AppConfig`)

## Method
Independent, dedicated per-unit audit (auditor did not author U44; replaces the earlier
"covered-by-the-module-audit" stamp per D41). Mechanical only — no fixes, no ledger/source
edits. Read target (`tests/test_app_config.py`) + oracle (`src/geniefy_app/config.py` under
test; `geniefy_core/state.py` `RunConfig`/`SessionMode` for the field/default contract;
`.gotm/audits/U43.md` for module-audit context). Ran the suite, verified hermeticity, and
**mutation-tested the suite** (injected 4 defects into a `/tmp` scratch copy of `config.py`
to confirm the tests catch them; each reverted).

RUN: `cd … && PYTHONPATH=src /tmp/geniefy_pgvenv/bin/python -m pytest tests/test_app_config.py -q`
→ **11 passed in 0.01s**.

## Checks

### 1. Coverage is meaningful, not vacuous — PASS
The 11 tests exercise every dimension the unit promises:
- **Required-var fail-fast** — `test_missing_all_required_lists_them_all` (asserts every
  `REQUIRED` key appears), `test_missing_one_required`, `test_blank_required_treated_as_missing`
  (whitespace-only ⇒ missing).
- **Type coercion** — `test_full_env_parses_all_types` parses float (`keep_threshold`),
  ints (`profile/reason_batch_size`, `context_token_budget`, `max_input_tokens_per_call`,
  `summary_target_tokens`), bool (`summarize_over_budget=false`), CSV trim
  (`enabled_providers`), and `SessionMode` enum. Negatives: `test_invalid_number_reported`
  (bad float), `test_invalid_mode_reported` (bad enum), `test_multiple_errors_collected_at_once`
  (proves errors aggregate, not first-only).
- **RunConfig field mapping** — `test_minimal_valid_env_applies_defaults` asserts all `rc.*`
  defaults and `test_full_env_parses_all_types` asserts all overridden values. Cross-checked
  against `state.py:116-128`: defaults (mode INTERACTIVE, keep 0.75, profile 50, reason 25,
  ctx 4000, sample_mode "auto", summarize True, max_input/summary_target None) match exactly;
  `template_id` defaults to `"default"` via the loader's `or "default"`.
- **MCP-providers JSON (D34)** — valid array (`test_mcp_providers_parsed`), bad JSON
  (`test_mcp_providers_bad_json_reported`), non-list rejected (`test_mcp_providers_non_list_rejected`,
  asserts the "JSON array" message), and **absent ⇒ `[]`** asserted in
  `test_minimal_valid_env_applies_defaults` (line 43). Both "valid" and "absent/empty" paths covered.

**Mutation probes (4 injected, all CAUGHT, all reverted):**
1. Fail-fast disabled (never append missing-required error) ⇒ **4 failed**.
2. Type-cast error swallowed in `num()` (return default instead of recording) ⇒ **2 failed**
   (`test_invalid_number_reported`, `test_multiple_errors_collected_at_once`).
3. `keep_threshold` default 0.75 → 0.5 ⇒ **1 failed** (`test_minimal_valid_env_applies_defaults`).
4. MCP array-check removed (accept any parsed JSON) ⇒ **1 failed** (`test_mcp_providers_non_list_rejected`).
The suite is non-vacuous: it detects a broken fail-fast, a swallowed cast error, a wrong
default, and a relaxed MCP-array guard. Revert restored 11 passed.

### 2. Hermetic — PASS
Importing `geniefy_app.config` loads **zero** databricks/mlflow/sqlalchemy/psycopg modules
(checked via `sys.modules`). `from_env` takes a plain dict (`_full_env` helper builds one);
no `os.environ` dependence, no network/DB/SDK. The test file imports only `json`, `pytest`,
`geniefy_app.config`, and `geniefy_core.state.SessionMode`.

### 3. RUN — PASS
`PYTHONPATH=src /tmp/geniefy_pgvenv/bin/python -m pytest tests/test_app_config.py -q` →
**11 passed in 0.01s**.

## Findings

### HIGH severity
- none.

### MEDIUM severity
- none.

### LOW severity
- **No invalid-value negative for `GENIEFY_MAX_INPUT_TOKENS_PER_CALL` / `GENIEFY_SUMMARY_TARGET_TOKENS`.**
  These two ints have their valid-parse path covered (`test_full_env_parses_all_types`) but not a
  bad-value path. I confirmed by direct probe (and via mutation 2, which covers the shared `num()`
  helper they route through) that a bad value *is* correctly collected into `ConfigError` — so this
  is a minor coverage gap, not a defect. Identical to U43's first LOW finding; non-blocking, fold
  into a test-hardening sweep if convenient.

### UNVERIFIED
- none. (Oracle and the infra-free test path were fully available; no live infra needed.)

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 1 · UNVERIFIED: 0
pytest: 11 passed
tests-meaningful: yes
Verdict: PASS-FINDINGS   (no HIGH/MEDIUM; 1 LOW-only ⇒ PASS-FINDINGS)
