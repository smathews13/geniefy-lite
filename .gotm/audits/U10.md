# Audit — U10 (Design Amendment 001)

**Date:** 2026-06-11   **Auditor:** independent dispatched subagent (did not author U10)
**Verdict:** PASS-FINDINGS

---

## Scope & method

I read, in full: the audit target `docs/design/LLD-amend-001.md` (U10); the oracle `.gotm/audits/audit-001.md` (the prior audit whose F1–F6 U10 claims to close); the six frozen docs U10 amends (`LLD-data-layer.md` U2, `LLD-agent-core.md` U4, `LLD-app-backend.md` U5, `LLD-review-apply.md` U6, `LLD-packaging-deployment.md` U8, `LLD-ux-delight.md` U9) plus `LLD-profiling-tool.md` (U3, for the F4 cross-check); `.gotm/DECISIONS.md` (D1–D26); `.gotm/PROTOCOL.md` → *Audit gates*; and `.gotm/LEDGER.md`.

Method: applied the PROTOCOL 5-point checklist. For each finding F1–F6 I verified the resolution is concrete (not asserted) by mechanically diffing the state-vocabulary U10 defines against the actual enum values / endpoint contracts in the frozen docs. I paid special attention to the NEW-INCONSISTENCY risk the brief named: U10 changes the state vocabulary, so I checked whether any renamed/removed value is still referenced elsewhere, and whether `apply_status` and `draft_status` overlap incoherently. I did not apply any fixes.

---

## Finding-closure check (F1–F6)

### F1 (major) — `running` watchdog vs the enum — **CLOSED**
U10 defines `IN_FLIGHT = {profiling, gathering_context, reasoning, applying}` and rules the watchdog `status ∈ IN_FLIGHT AND now()-updated_at > stale_timeout → failed`, leaving the enum unchanged.
- **Verified against U2 §5:** all four names (`profiling`, `gathering_context`, `reasoning`, `applying`) exist verbatim in the `session_status` enum (`LLD-data-layer.md:232-234`). No phantom value.
- The set is correct: it excludes the legitimately-idle non-terminal states (`created`, `awaiting_input`, `paused`) and the terminal ones (`applied`, `failed`, `cancelled`) — exactly the states a stale-detector should *not* reap. `updated_at` is confirmed present on `sessions` (`LLD-data-layer.md:108`).
- This genuinely supersedes the `running` wording in U5 §8 (`LLD-app-backend.md:104`) and D18's Consequences. Concrete and sufficient.

### F2 (major) — homeless per-item states — **PARTIALLY-CLOSED**
U10 splits the axes: `draft_status += low_confidence, error`; new `apply_status` enum `not_applied | applied | conflict | failed | unsupported | skipped_noop`. This correctly homes every state audit-001 F2 named (`low_confidence`, `error` → draft_status; `conflict`, `unsupported`, `failed` → apply_status). The axis split is sound and matches how U4 (gating/generation) vs U6 (apply) actually produce states.
- **But it introduces a fresh inconsistency it does not reconcile** (see New Finding 1): the frozen U6 §6 endpoint `GET /sessions/{id}/apply-status` returns `applied/conflict/failed/**pending**` (`LLD-review-apply.md:64`). U10 renamed that fourth state to `not_applied` and never states it supersedes U6 §6 — so the frozen endpoint contract now references a value (`pending`) absent from the new enum. The amendment's "supersedes" line for F2 names U2 §4.5/§4.6/§5, U4 §3.6, U6 §4 — but **not** U6 §6 or U6 §8.
- A second smaller gap (New Finding 2): U6 §8 says the no-op case "mark `applied`", whereas U10 introduces a distinct `skipped_noop` value for exactly that case — left unreconciled at the `apply_status` level.

The states are no longer homeless (the F2 headline is addressed), but the new vocabulary is not fully wired back to the two frozen U6 surfaces that emit it. Hence PARTIALLY-CLOSED.

### F3 (minor) — phase ≠ status 1:1 — **CLOSED**
U10 declares orchestrator phases (`profiling · gathering_context · reasoning · judging · gating`) are **many-to-one** onto persisted `session_status`, with `judging`/`gating` rolling up under `reasoning`, and the U9 narration driven by finer phase events not the enum. This directly supersedes U4's "Maps 1:1" (`LLD-agent-core.md:46`) and the U9 §4 "(U4/U5 status)" mislabel (`LLD-ux-delight.md:41,46`). U4 §4 already folded judge/gate under `reasoning` (`LLD-agent-core.md:110`), so U10 is consistent with the frozen logic and merely corrects the overstated wording. Concrete and sufficient.

### F4 (minor) — profiling-provider sourced from wrong table — **CLOSED**
U10 moves profiling-provider selection to `config.profiling_provider` in `RunConfig`/`session.config`, explicitly separate from `context_providers` (context-only, D5). This resolves audit-001 F4 (which pinned `LLD-profiling-tool.md:45` conflating the two registries).
- **Cross-checked U3/U4:** U4 §3.2 already says the Profiler selects "from `config`/registry" (`LLD-agent-core.md:57`), so routing selection through `config` is consistent — no conflict introduced. The two registries are correctly held independent.
- Minor attribution looseness (New Finding 3, LOW): U10 cites "(U4 §7, D17)" for `config.profiling_provider`, but U4 §7's `RunConfig` list (`LLD-agent-core.md:126`) enumerates `enabled_providers` (a `context_providers` snapshot) and does **not** contain a `profiling_provider` field — U10 is *adding* it, not pointing at an existing one. Functionally fine; the citation just overstates that the field already lives there.

### F5 (minor) — blob vs normalized rehydration — **CLOSED**
U10 resolves the open choice to BOTH: keep normalized rows as source of truth, add `session_state jsonb` on `sessions` as a rehydration fast-path, write both in one transaction, and treat normalized rows as authoritative on divergence. This supersedes the U5 §3 open item (`LLD-app-backend.md:37,120`).
- **Checked against D17:** D17 says the caller owns persistence and writes `SessionState` to Lakebase. U10's "rows authoritative on divergence" rule does **not** contradict D17 — D17 is silent on the blob-vs-normalized representation; it only mandates that the *caller* persists the serializable state. Storing it twice (snapshot + normalized) with a clear precedence rule is a legal refinement, not a divergence. No contradiction.

### F6 (minor) — missing LICENSE — **CLOSED**
U10 adds a top-level `LICENSE` (Apache 2.0, D13) + a README license reference, kept in the GitHub repo but excluded from the deploy artifact like `.gotm/`. This honors D13 (`DECISIONS.md:185`: "repo carries a `LICENSE`") and is consistent with D25's exclude-from-`bundle sync` treatment of dev-only files. Concrete and sufficient.

---

## Checklist results (1–5)

1. **Existence — PASS.** `docs/design/LLD-amend-001.md` exists at the LEDGER path; LEDGER row U10 = `done`, `Audit: pending`.
2. **Spec match — PASS-WITH-NOTE.** U10 addresses all six findings with a per-finding section + a consolidated schema delta. Five are fully closed; F2 is only partially wired back (see New Findings 1–2).
3. **Cross-reference integrity — PASS-WITH-NOTE.** Every D#/U#/F# resolves and is correctly attributed, with one LOW exception: the "(U4 §7, D17)" citation for `config.profiling_provider` points at a field U4 §7 does not actually contain (New Finding 3). All decision citations (D5/D6/D7/D13/D17/D18/D21) check out against DECISIONS.md.
4. **Internal consistency — PASS-WITH-FINDINGS.** F1's IN_FLIGHT names match U2 verbatim; the apply_status/draft_status axis split is coherent (no overlap — they are orthogonal, and `draft_status='applied'` is correctly gated on `apply_status ∈ {applied, skipped_noop}`). The one real crack is the `pending`→`not_applied` rename not propagated to U6 §6/§8 (New Findings 1–2).
5. **Decision fidelity — PASS.** No silent divergence. D17 (F5), D13 (F6), D5 (F4), D18/D21 (F1/F2) are all honored; the watchdog/apply refinements extend rather than reverse the cited decisions.

---

## New findings

**New Finding 1 — `apply_status` renames U6's `pending` to `not_applied` but does not supersede the U6 endpoint that emits it.** *(MEDIUM)*
`LLD-amend-001.md` §F2 defines `apply_status` with default `not_applied`, but frozen `LLD-review-apply.md:64` (`GET /sessions/{id}/apply-status`) and the apply API are specified to return `applied/conflict/failed/**pending**`. U10's F2 "supersedes" clause names U2 §4.5/§4.6/§5, U4 §3.6, and U6 §4 — it omits U6 §6 (the endpoint contract) and U6 §8 (the error table), so the implementer is left with two names for the not-yet-applied state and no authoritative reconciliation. This will surface the moment the apply-status endpoint is coded against the new enum.
*Suggested fix (do not apply):* in a follow-on, extend U10's F2 supersession to U6 §6/§8, stating `pending` is renamed to `not_applied` (or have the endpoint map the enum value to a `pending` wire label) so exactly one name governs.

**New Finding 2 — no-op apply outcome: U6 says "mark `applied`", U10 introduces `skipped_noop`.** *(LOW)*
`LLD-review-apply.md:39,76,80` resolve the idempotent no-op to `mark applied`, but `LLD-amend-001.md:26-28` adds a distinct `apply_status='skipped_noop'` for `proposed == live`. U10 reconciles the *draft_status* side (it says `draft_status='applied'` is set for `applied` **or** `skipped_noop`), but never says it supersedes U6's per-item "mark `applied`" at the `apply_status` level — so it is ambiguous whether a no-op records `apply_status='applied'` or `'skipped_noop'`.
*Suggested fix (do not apply):* state explicitly that U6 §8's no-op row maps to `apply_status='skipped_noop'` (with `draft_status='applied'`), closing the ambiguity.

**New Finding 3 — `config.profiling_provider` cited as "(U4 §7, D17)" but U4 §7's RunConfig does not list it.** *(LOW)*
`LLD-amend-001.md:42,66` attribute the new `config.profiling_provider` field to U4 §7 / D17, but U4 §7's `RunConfig` enumeration (`LLD-agent-core.md:126`) contains `enabled_providers` (a `context_providers` snapshot) and no `profiling_provider`; D17's SessionState lists `config` generically but not this field. The field is being *introduced* by U10, which is fine, but the citation implies it already lives in U4 §7.
*Suggested fix (do not apply):* reword to "adds `config.profiling_provider` to `RunConfig` (U4 §7)" rather than citing it as pre-existing.

---

## Verdict rationale

**PASS-FINDINGS.**

- **No HIGH/blocker.** U10 genuinely closes five of six findings (F1, F3, F4, F5, F6) with concrete, verified resolutions: the IN_FLIGHT set matches the U2 enum verbatim; the phase→status mapping correction is consistent with U4 §4's existing fold; the profiling-provider move does not conflict with U3/U4; the BOTH resolution for F5 does not contradict D17; and the LICENSE addition honors D13. The architecture-level claims hold up.
- **The new state vocabulary is the one soft spot.** F2's *headline* (homeless states) is addressed — every state audit-001 named now has a column — but U10 created a fresh, smaller inconsistency by renaming U6's `pending` to `not_applied` and re-typing the no-op outcome without extending its "supersedes" scope to U6 §6/§8. That is a real, implementable-against mismatch (New Finding 1, MEDIUM) plus a related ambiguity (New Finding 2, LOW) and a citation overstatement (New Finding 3, LOW). None blocks implementation outright — they are reconciliation gaps in the same seam, not logic breaks — so per the PROTOCOL severity model (HIGH ⇒ FAIL; MEDIUM/LOW-only ⇒ PASS-FINDINGS) the verdict is **PASS-FINDINGS**.
- Per the gate, New Findings 1–3 become tracked follow-on units (or a tightening pass on the U10 amendment via a further follow-on, since U10 itself is now `done`/frozen) and should land — particularly New Finding 1 — before the apply-status endpoint is coded against the new `apply_status` enum.
