# Audit report — frontend (endpoint/shape fidelity + wiring + build gate) — 2026-06-13

## Target audited
Files:
- `app/frontend/src/api/hooks.ts` (`useSessions`, `useLibrary`, `qs()` helper)
- `app/frontend/src/screens/HistoryView.tsx` (NEW)
- `app/frontend/src/screens/LibraryView.tsx` (NEW)
- `app/frontend/src/App.tsx` (3-tab nav + open-from-history wiring)
Unit: U87 — History + Library SPA views (E6/E7/D46)

## Oracle
- `docs/design/LLD-amend-003.md` §E6 / §E7
- `.gotm/DECISIONS.md` D46
- Backend contracts (U84): `src/geniefy_app/api.py` (`GET /api/sessions`, `GET /api/library`), `src/geniefy_app/store.py` (`list_sessions`, `list_library` row shapes)
- `app/frontend/src/api/types.ts` (`SessionSummary`, `LibraryEntry`)

## Findings

### HIGH severity
- None.

### MEDIUM severity
- None.

### LOW severity
- None.

### UNVERIFIED
- None.

## Check-by-check evidence

**1. Endpoint + shape fidelity — PASS.**
- `useSessions` (hooks.ts:44-49) hits `/api/sessions${qs(params)}` with `{status, table, limit, offset}` and types the response `{ sessions: SessionSummary[] }`. Backend `api.py:258-262` declares `GET /api/sessions` with exactly `status, table, limit, offset` query params and returns `{"sessions": [...]}`. Paths + params + envelope key all match.
- `useLibrary` (hooks.ts:52-57) hits `/api/library${qs(params)}` with `{scope, limit, offset}`, typed `{ entries: LibraryEntry[] }`. Backend `api.py:264-267` declares `GET /api/library` with `scope, limit, offset` returning `{"entries": [...]}`. Match.
- `qs()` (hooks.ts:16-21) builds a clean query string, skipping `undefined`/`''` — correct, no malformed `?` when empty.
- `HistoryView` reads `session_id`, `target`, `status`, `created_by`, `updated_at`, `n_columns`, `n_applied` (HistoryView.tsx:32-49). Backend `store.list_sessions` returns a dict with exactly these keys plus an unused `created_at` (store.py:210-212) — every field the view reads is present and identically named. Match.
- `LibraryView` reads `id`, `scope`, `match_key`, `canonical_comment`, `tags`, `usage_count` (LibraryView.tsx:18-31). Backend `store.list_library` returns `id, scope, match_key, canonical_comment, tags, usage_count, source_table_ref, approved_by, updated_at` (store.py:285-287) — every field the view reads is present and identically named. Match. (`tags` is normalized to a list server-side, so `e.tags.length`/`.map` is safe.)

**2. Open/resume wiring (E7) — PASS.**
- `HistoryView` "Open" button calls `onClick={() => onOpen(s.session_id)}` (HistoryView.tsx:49).
- `App.open(id)` (App.tsx:37-40) does `setSessionId(id)` + `setView('run')`. The existing `useSession(sessionId)` poll (App.tsx:26) then refetches `GET /api/sessions/{id}` and RunView/ReviewView rehydrate based on status — exactly the generate-now-approve-later path D46/§E7 describes (no new state). Correct.

**3. States — PASS.**
- HistoryView: `data?.sessions ?? []` (line 20) + explicit `isLoading` / `isError` / empty (length === 0) branches (lines 25-29). No undefined-access crash.
- LibraryView: `data?.entries ?? []` (line 5) + matching loading/error/empty branches (lines 11-15). No crash.

**4. No regressions — PASS.**
- The entire prior run flow is wrapped in `{view === 'run' && (<>…</>)}` (App.tsx:82-133) and every original branch is preserved inside it: input form (84-114), session-loading (116), RunView for in-flight (117), failed banner (118-122), ReviewView for DONE statuses (123-125), and the catch-all other-status banner (126-131). No branch dropped.
- TanStack Query keys are distinct: `['sessions', params]`, `['library', params]`, `['session', sessionId]`, `['config']` — no cache collision.
- RunView.tsx, ReviewView.tsx, lib/cn.ts all still present and imported.

**5. Read-only + no heavy deps — PASS.**
- Both views render read-only lists; no mutations, no write actions. Only imports are `useSessions`/`useLibrary` from the existing hooks module. `package.json` dependencies unchanged (`@tanstack/react-query` already present; no new package).

**6. Build gate — PASS.**
- `node_modules/.bin/tsc --noEmit` → exit 0 (clean, no errors).
- `node_modules/.bin/vite build` → exit 0, built in ~1s, 1042 modules. `index-*.js` = 224.81 kB (≈ expected ~225 kB main); `JudgeRadar-*.js` = 352.52 kB as a separate lazy chunk (as expected).

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 0 · UNVERIFIED: 0
Verdict: PASS

endpoints-correct: y
open-resume-wired: y
run-flow-intact: y
typecheck-clean: y
vite-build-clean: y
