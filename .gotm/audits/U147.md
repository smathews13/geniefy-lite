# Audit report — content-claim / internal-consistency — 2026-06-23

## Target audited
File: `src/geniefy_app/providers.py` (`_await_terminal`; with `_statement_state` / `_rows_from_statement_response` / both runner closures confirmed behavior-unchanged)
File: `tests/test_providers.py` (`test_await_terminal_raises_on_nonterminal_without_statement_id` + the existing `test_await_terminal_*` trio + `test_sql_execute_polls_running_on_the_sp_runner`)
Unit: U147 (closes the 2 UNVERIFIEDs carried from `.gotm/audits/U145.md` / `.gotm/audits/U146.md`)

## What was checked

1. **Finding (a) — poll budget documented as a hang-guard, not an SLA, + stated in the timeout error.** `_await_terminal`'s docstring (providers.py:185–188) now reads: "The poll budget is `poll_seconds × max_polls` (default ≈ 5 min). This is a deliberate **hang-guard, not an SLA** (U147): warehouse statements … normally finish inside the 50s synchronous wait, so this loop is the rare long-statement path; one still non-terminal past the budget is treated as **stuck** and raised — failing loud, never a silent `[]`." The budget-exhausted `RuntimeError` (providers.py:202–206) now states the budget computed from the actual args: `f"…within the poll budget (~{int(poll_seconds * max_polls)}s = {max_polls}×{poll_seconds}s); statement_id=…"`. With defaults `poll_seconds=2.0, max_polls=150` this renders `~300s = 150×2.0s` — accurate, and self-describing rather than a hard-coded "~5 min". The U145/U146 UNVERIFIED (a) (the 5-min ceiling was "unjustified against any SLA") is now reframed as a deliberate guard with rationale, not an SLA claim. ✅ addressed, reasonable and accurate.

2. **Finding (b) — non-terminal response with no `statement_id` raises instead of `get_statement(None)` (the load-bearing check).** The loop now reads `sid = getattr(resp, "statement_id", None)` and, before any `get_statement`/`sleep`, `if not sid: raise RuntimeError("SQL statement is non-terminal but carries no statement_id — cannot poll for completion")` (providers.py:196–199). The guard sits *inside* the `for` loop, *after* the terminal-check `return` (line 194) but *before* `time.sleep`/`get_statement` (lines 200–201), so it fires only when the response is non-terminal (PENDING/RUNNING) AND has no id — exactly the edge the U145/U146 audits flagged. The new test `test_await_terminal_raises_on_nonterminal_without_statement_id` (tests/test_providers.py:236–249) feeds `statement_id=None, state="RUNNING"` with a `get_statement` that `raise AssertionError("get_statement(None) must not be called")`, and asserts `pytest.raises(RuntimeError, match="statement_id")`. The assertion is exact: it requires both that a `RuntimeError` mentioning `statement_id` is raised AND (via the `boom` stub) that `get_statement(None)` is never called.
   - **Mutation-verified:** I removed the `if not sid: raise …` guard and re-ran the test — it FAILED with `AssertionError: get_statement(None) must not be called` (the `boom` stub fired). Restored immediately. So the test would fail if the guard were removed — it is a genuine regression guard, not a tautology. ✅ addressed, load-bearing.

3. **No behavior regression.** `_statement_state` (170–175) and `_rows_from_statement_response` (210–234) are unchanged from the U146-audited form (still raise on FAILED/CANCELED/CLOSED, return rows/`[]` for SUCCEEDED/absent-status). `_await_terminal` was refactored from a `while`-with-counter to a bounded `for _ in range(max_polls)` loop, but the observable behavior is preserved: returns at once on a terminal/absent-status response without calling `get_statement` (the loop's first guard, line 194); polls a RUNNING→SUCCEEDED to terminal with one `get_statement` (line 201); raises after the budget if still non-terminal (lines 202–206). Verified by the existing trio, all green: `test_await_terminal_polls_running_until_terminal` (asserts `calls["n"] == 1`), `_gives_up_on_stuck_statement` (asserts `RuntimeError` match `"terminal"`), `_noop_when_already_terminal_or_no_status` (its `get_statement` raises `AssertionError` if touched). The SP-runner poll test `test_sql_execute_polls_running_on_the_sp_runner`, the `test_sql_execute_mints_fresh_client_per_call` (U98), and the FAILED-raises `test_rows_from_statement_response_raises_on_failed_statement` (U139) all pass. **Full suite: `PYTHONPATH=src .venv/bin/python -m pytest -q` → 381 passed, 1 skipped, 1 warning** (the warning is the pre-existing StarletteDeprecationWarning, unrelated) — matches the claimed 381/1. The 6 load-bearing provider tests run in 0.02s and the whole suite in 0.81s — no real sleep on any test path (`time.sleep` is reachable only on the real polling path, which the tests drive with `poll_seconds=0` or a monkeypatched `time.sleep`). ✅

4. **Both runners still wired (U146 not undone).** `grep -n "_await_terminal" src/geniefy_app/providers.py` → three lines: the definition (178) and **two** call sites, `databricks_sql_execute`'s `execute` (line **248**, SP READ runner) and `make_obo_sql_execute`'s inner `execute` (line **277**, OBO write runner), both `return _rows_from_statement_response(_await_terminal(w, resp))`. No bare `_rows_from_statement_response(resp)` call site remains. The U146 dual-wiring is intact. ✅

5. **Cross-reference integrity.** U146 row (LEDGER.md:191) annotates both outputs `superseded by U147`; U147 row (LEDGER.md:190) cites `.gotm/audits/U145.md, .gotm/audits/U146.md, U146` as inputs and `supersedes U146` for both files. The 2026-06-23 recovery-log entry (LEDGER.md:254) accurately describes the change: poll-ceiling rationale restructured to a bounded `for` loop + documented as a hang-guard with the budget in the timeout error; the `statement_id=None` edge now raises `RuntimeError` ("non-terminal but carries no statement_id"); new test added; prior 3 await tests + SP-runner test pass; "381 passed/1 skipped". All cited line numbers/claims match the code. D48 (SP-reads / OBO-writes split, DECISIONS.md:586) and U98 (fresh `WorkspaceClient` per call) are untouched: SP runner still mints a fresh client per call (line 244, U98); OBO runner resolves `host` once (line 268, U90) and builds a token-auth client per `make` (line 271); the U147 edit added no caching, only the in-loop guard + docstring/error text. ✅

## Findings

### HIGH severity
- (none) — both carried UNVERIFIEDs are addressed: the poll budget is documented as a deliberate hang-guard (not an SLA) and the timeout error states the budget; the `statement_id=None` edge now raises a clear `RuntimeError` instead of `get_statement(None)`, guarded and mutation-verified by a new test. No regression (381/1 green), both runners still wired, D48/U98 intact.

### MEDIUM severity
- (none)

### LOW severity
- (none)

### UNVERIFIED
- (none) — both prior UNVERIFIEDs are now resolved in scope. The remaining design judgement (whether ~5 min is the right wall-clock budget for a genuinely huge-table profile) is now an explicit, documented, tunable parameter (`max_polls`/`poll_seconds`) rather than an unexplained constant; no open verification remains for U147's claim.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 0 · UNVERIFIED: 0
Verdict: **PASS** (both carried UNVERIFIEDs closed: (a) the `poll_seconds × max_polls` budget is documented as a deliberate hang-guard — explicitly "not an SLA" — and the budget-exhausted `RuntimeError` states it (`~{Ns} = {max_polls}×{poll_seconds}s`); (b) a non-terminal response with no `statement_id` raises a clear `RuntimeError` mentioning `statement_id` instead of calling `get_statement(None)`, guarded inside the loop and proven load-bearing by mutation — removing the guard fails the new test. No behavior regression — the existing await trio, the SP-runner poll test, the U98 mint-fresh test, and the U139 FAILED-raises test all pass; full suite 381 passed / 1 skipped with no real sleep. Both runners remain wired (providers.py:248 SP + :277 OBO); cross-references and D48/U98 intact. Downstream/code units may proceed.)
