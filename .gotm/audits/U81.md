# Audit report — config-chain + per-phase-budget + ledger — 2026-06-12

## Target audited
Files:
- `src/geniefy_core/state.py` (RunConfig: 6 new fields + to_dict/from_dict)
- `src/geniefy_app/config.py` (AppConfig.from_env: 6 GENIEFY_* reads)
- `src/geniefy_app/api.py` (build_service: wires the 6 into LLMClient + Reasoner)
- `src/geniefy_core/reasoner.py` (per-phase table/column max_tokens threading)
- `src/geniefy_core/templates/default.yaml` (table 500 / column 150)
- `app/app.yaml` (declares the 6 env vars)
- `tests/test_state.py`, `tests/test_app_config.py`, `tests/test_reasoner.py`, `tests/test_template.py`
Unit: U81
Oracle: `docs/design/LLD-amend-003.md` §E1; `.gotm/DECISIONS.md` D43 + D47; `.gotm/LEDGER.md`

## Findings

### HIGH severity
- none.

### MEDIUM severity
- none.

### LOW severity
- none.

### UNVERIFIED
- none.

## Detail (mechanical verification)

**Check 1 — config-chain completeness/correctness (PASS).**
- All 6 fields exist on `RunConfig` with the oracle defaults: `max_retries=5`, `backoff_base=0.5`,
  `llm_temperature=0.0`, `default_max_tokens=4096`, `reason_table_max_tokens=20000`,
  `reason_column_max_tokens=2000` (`state.py:131-136`).
- `to_dict` emits all 6 (`state.py:153-157`); `from_dict` reads all 6 with matching defaults
  (`state.py:175-180`). Round-trip is symmetric.
- `from_env` reads `GENIEFY_MAX_RETRIES` (int,5), `GENIEFY_BACKOFF_BASE` (float,0.5),
  `GENIEFY_LLM_TEMPERATURE` (float,0.0), `GENIEFY_MAX_TOKENS` (int,4096),
  `GENIEFY_REASON_TABLE_MAX_TOKENS` (int,20000), `GENIEFY_REASON_COLUMN_MAX_TOKENS` (int,2000)
  — all names correct, casts correct, defaults correct (`config.py:69-74`), and all 6 are passed
  into the constructed `RunConfig` (`config.py:110-116`).
- `app.yaml` declares all 6 names with matching example values (`app.yaml:62-73`).
- `build_service` wires `LLMClient(default_temperature=rc.llm_temperature,
  default_max_tokens=rc.default_max_tokens, max_retries=rc.max_retries, backoff_base=rc.backoff_base)`
  (`api.py:295-298`) and `Reasoner(table_max_tokens=rc.reason_table_max_tokens,
  column_max_tokens=rc.reason_column_max_tokens)` (`api.py:309-310`). No misspelled name, no un-wired field.

**Check 2 — per-phase budgets take effect (PASS).**
- Table phase calls `_complete(..., max_tokens=self.table_max_tokens)` (`reasoner.py:106`); column
  phase calls `_complete(..., max_tokens=self.column_max_tokens)` (`reasoner.py:132`).
- `_complete` forwards `max_tokens` to `self.llm.complete_json(messages, max_tokens=max_tokens)`
  (`reasoner.py:203`); `complete_json` forwards to `complete`, which sends
  `max_tokens or self.default_max_tokens` to the transport (`llm.py:217`). So `None` (unset budget)
  falls back to the `LLMClient` `default_max_tokens`. Code path confirmed end-to-end.

**Check 3 — template caps (PASS).** `default.yaml` table `style.max_words: 500` (line 11),
column `style.max_words: 150` (line 25). `forbid` rules retained.

**Check 4 — hermetic (PASS).** `PYTHONPATH=src python3 -c "import geniefy_core.state, geniefy_core.reasoner; print('ok')"` → `ok`. state.py / reasoner.py stay SDK-free.

**Check 5 — tests bite + pass (PASS).**
- Targeted: `pytest tests/test_state.py tests/test_app_config.py tests/test_reasoner.py tests/test_template.py -q` → **81 passed**.
- Full suite: `pytest tests/ -q` → **285 passed** (1 unrelated Starlette deprecation warning).
- New tests assert the tunables, not just existence:
  - `test_state.py:88-105` — defaults (5/0.5/0.0/4096/20000/2000) AND a non-default round-trip
    (8/1.5/0.3/8192/30000/3000) survive `to_dict`→`from_dict`.
  - `test_app_config.py:44-46,58-73` — defaults AND env parse of non-default `GENIEFY_*` values
    (8/1.5/0.3/8192/30000/3000) map onto `RunConfig`.
  - `test_reasoner.py:234-240` — `test_table_and_column_phases_use_their_own_max_tokens` asserts the
    table call's `max_tokens == [20000]` and every column batch `== 2000`, captured at the transport.
  - `test_reasoner.py:243-246` — fallback test asserts every call uses the `LLMClient` default `4096`
    when no per-phase budget is set.
  - `test_template.py:33-34` — table `max_words == 500`, column `max_words == 150`.

**Check 6 — GOTM ledger hygiene (PASS).** U81's row (`LEDGER.md:107`) lists all 10 outputs
(`templates/default.yaml`, `state.py`, `config.py`, `api.py` build_service, `app/app.yaml`,
`reasoner.py`, and the 4 test files) and states it supersedes U25/U26/U27/U35/U36/U43/U71/U72/U77
for those files. Each of the 9 carries the per-file `(superseded by U81)` annotation:
U25 (`templates/default.yaml`), U26 (`test_template.py`), U27 (`state.py`), U35 (`reasoner.py`),
U36 (`test_reasoner.py`), U43 (`config.py`), U71 (`test_state.py`), U72 (`test_app_config.py`),
U77 (`app/app.yaml` + `api.py`). Ownership transferred cleanly — no frozen file is left double-owned.
(U25's `template.py` is correctly NOT annotated, since U81 did not touch it.)

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 0 · UNVERIFIED: 0
Verdict: PASS
