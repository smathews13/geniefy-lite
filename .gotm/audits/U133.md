# Audit report — existence/structure + accuracy/source-fidelity — 2026-06-14

## Target audited
Files: `README.md`, `docs/ARCHITECTURE.md`
Unit: U133 (Build, D56) — refresh README to geniefy-lite + current R1–R4 feature set + accurate
deploy (incl. `deploy_jobs.sh`) + a "run the tests" snippet; add a consolidated `docs/ARCHITECTURE.md`.

Oracle: D55 + D56 (`.gotm/DECISIONS.md`); the U133 ledger row; and — primarily — the actual
code/repo. Verdicts below are grounded in the code, not in the docs' own assertions.

## Method / what was actually checked

- **Existence/structure.** Both target files present, well-formed markdown. Every internal link
  spot-checked against disk: `docs/design/LLD-packaging-deployment.md`, `deploy.sh`, `deploy_jobs.sh`,
  `migrations/run_migrations.py`, `migrations/APPLY.md`, `src/geniefy_core/`, `src/geniefy_app/`,
  `jobs-bundle/`, `databricks.yml`, `LICENSE`, `requirements-dev.txt`, all `docs/design/LLD-*.md` incl.
  `LLD-amend-00{1..6}.md`, `app/app.yaml`, `app/main.py` — **all resolve.**
- **Mermaid.** 3 fenced ```mermaid blocks in ARCHITECTURE (6 fences, balanced); each rendered cleanly
  with `mmdc` — **all 3 VALID** (not merely plausible). README uses a markdown table, not mermaid.
- **Accuracy/source-fidelity (the heart):** every load-bearing claim traced to code:
  - **Two modes + Job-never-writes-UC.** `src/geniefy_app/schema_run.py::run_schema` only calls
    `service.run_hands_off(...)`, never `applier.apply`. `api.py::run_hands_off` (L287–299) runs the
    orchestrator + `store.save` only — no UC write. Apply is a separate route `POST
    /api/sessions/{id}/apply` (api.py:519). The orchestrator never applies in any mode
    (orchestrator.py:141–145 comment + code). **TRUE.**
  - **Pipeline order profile→context→reason→judge→gate.** orchestrator.py `run()` (L80–100):
    PROFILING → GATHERING → REASONING → JUDGING → `_gate`. **TRUE.** `resume`/`regenerate` re-run
    reason/judge/gate only (L103–123) — matches ARCHITECTURE §4.
  - **Security/OBO split.** providers.py: `databricks_sql_execute` = app SP for profiling/context
    (L187–189); `make_obo_sql_execute` returns `make(access_token)` running as the user (L203–221);
    tokens minted **per call, never cached** (L131, L189). apply.py uses `make_user_sql(access_token)`
    for the write, **no silent SP fallback** (L78–104); audit `actor=created_by=identity.actor`.
    DDL is `COMMENT ON TABLE` / `ALTER TABLE … ALTER COLUMN … COMMENT`, escaped (L191–196). **TRUE.**
  - **Component/module names (§3/§9).** All map to real files: `api.py`, `store.py`, `apply.py`,
    `providers.py`, `identity.py`, `config.py`, `schema_run.py` in `geniefy_app/`; `orchestrator.py`,
    `context.py`, `reasoner.py`, `gate.py`, `judge.py`, `profiler.py`, `state.py` in `geniefy_core/`.
    **TRUE.**
  - **Lakebase tables (§6).** `sessions`, `table_drafts`/`column_drafts`, `comment_library`,
    `schema_runs`, `audit_log` all exist in `migrations/`. `comment_library` lifecycle
    `approved→applied→sunset` is migration `002` (enum `library_status`, L15; `status` column L22).
    `schema_runs` carries `job_run_id` + rollup `counts` (004 L22–24). Draft columns `judge_scores`,
    `conditional_fields`(facts), `confidence`, tags present. **TRUE.**
  - **Deploy claims.** `deploy.sh --code-only` = `bundle sync` + `apps deploy`, **no** `bundle deploy`
    / no resource reconcile (deploy.sh L38–39, L155–159). Hands-off Job is a SEPARATE bundle
    `jobs-bundle/databricks.yml` (bundle name `geniefy-jobs`, job `geniefy_schema_run`, `run_as` SP
    L17–18,61) deployed by `deploy_jobs.sh` (independent `bundle deploy`). Migrations branch-aware
    (deploy.sh L61–62; run_migrations reads branch). `geniefy_setup` is migrate-only (databricks.yml
    L103). `.gotm/` + `.claude/` sync-excluded (databricks.yml L20–22). **TRUE.**
  - **"Run the tests" snippet.** `requirements-dev.txt` exists (runtime + test pins). Ran
    `PYTHONPATH=src .venv/bin/python -m pytest -q` → **`375 passed, 1 skipped`** — matches the doc's
    "375 passed, 1 skipped" verbatim. Frontend build script is `tsc --noEmit && vite build` (matches).
    **TRUE.**
  - **No stale geniefy-v3 branding in README.** `grep "geniefy-v3" README.md` → none. The `geniefy-dev`
    app-resource note (D55) is present and correct. **TRUE / clean.**
  - **Config-not-code.** 17 `GENIEFY_*` vars in `app/app.yaml` incl. `GENIEFY_MODEL_ENDPOINT`,
    `GENIEFY_MCP_PROVIDERS`. **TRUE.**
  - **Library reuse-on-generation (R3).** `LibraryProvider` real (context.py:177), wired into the
    ContextGatherer in `build_service` (api.py:547–552), queries the library as SP. **TRUE.**

## Findings

### HIGH severity
- None.

### MEDIUM severity
- None.

### LOW severity
- **Component-map labels drop the `.py` for two modules.** ARCHITECTURE §3 labels the core profiler/
  judge nodes as `profiler — data profile + PII` and `judge — scores drafts` (no extension), while the
  sibling nodes use file names (`context.py`, `reasoner.py`, `gate.py`, `orchestrator.py`). Both back to
  real single files `src/geniefy_core/profiler.py` and `judge.py`, so nothing is wrong — purely a
  cosmetic inconsistency in the diagram's naming convention. Fix only if convenient.
- **§9 lists a top-level `jobs/` row alongside `jobs-bundle/`.** Both dirs exist (`jobs/schema_run_entry.py`
  is the entrypoint; `jobs-bundle/` is the bundle), so the row is accurate; minor potential for reader
  confusion with the README's organization table, which folds the entrypoint into the `jobs-bundle`
  description. Cosmetic.

### UNVERIFIED
- None. The 1-skipped test was not investigated (the doc's "1 skipped" is correct as written); the live
  Databricks behavior (actual OBO write against a real workspace) is out of scope for a doc/code-fidelity
  audit and is exercised by the deploy-verify units, not here.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 2 · UNVERIFIED: 0
Verdict: **PASS-FINDINGS**

The documentation is accurate and true to the code on every material claim — the two modes, the
Job-never-writes-UC invariant, the agent pipeline order, the SP-vs-OBO security model, the component
map, the Lakebase schema (incl. the library lifecycle), the two-bundle grant-safe deploy topology, and
the test snippet (375 passed verified by running it). All links resolve and all mermaid diagrams render.
The only findings are two cosmetic LOW items in the ARCHITECTURE component map; neither blocks downstream
consumption. Recommend stamping the U133 `Audit` cell PASS-FINDINGS.
