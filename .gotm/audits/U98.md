# Audit report — content-claim (session-bound OAuth tokens) — 2026-06-13

## Target audited
File: `src/geniefy_app/providers.py` + `tests/test_providers.py`
Unit: U98

## Findings

### HIGH severity
- (none)

### MEDIUM severity
- (none)

### LOW severity
- (none)

### UNVERIFIED
- (none)

## Check detail

1. **per-call, no cache** — PASS.
   - `databricks_chat_query` (providers.py:82–98): `WorkspaceClient` is built **inside** `def query(...)` at line 92 (`w = WorkspaceClient(profile=profile) if profile else WorkspaceClient()  # fresh per call (U98)`). The OAuth token is derived per call from that fresh client (`w.config.oauth_token().access_token`, line 94). No client/token is bound at factory-build time and nothing is cached across calls.
   - `databricks_sql_execute` (providers.py:142–155): `WorkspaceClient` is built **inside** `def execute(...)` at line 149 (`# fresh per call (U98)`). The factory body holds no client; nothing cached.
   - `make_obo_sql_execute` (providers.py:158–186): already per-call (a token-authenticated `WorkspaceClient` is built inside `make(access_token)` at line 176); the only factory-time `WorkspaceClient` (line 172) is used solely to resolve `host` once (closes the prior U90-audit LOW), not to mint/cache a request credential.
   - Lazy-import contract (U73) preserved: every `from databricks.sdk import WorkspaceClient` stays inside the factory function (lines 88, 146, 170) — none at module top.

2. **tests** — PASS. `PYTHONPATH=src /tmp/geniefy_pgvenv/bin/python -m pytest tests/test_providers.py -q` → **10 passed in 0.02s**. `test_sql_execute_mints_fresh_client_per_call` (test_providers.py:134–161) injects a fake `databricks.sdk` via `monkeypatch.setitem(sys.modules, ...)`, asserts `built == []` after factory build (line 158: nothing minted at factory time), then calls `execute(...)` twice and asserts `len(built) == 2` (line 161: one fresh client per call, not cached).

3. **hermetic** — PASS. `PYTHONPATH=src python3 -c "...leaked..."` → `leaked: none`. Importing `geniefy_app.providers` pulls in no `databricks`/`openai`/`psycopg2` module — lazy import intact.

4. **ledger** — PASS. `.gotm/LEDGER.md` U98 (line 120) Output = `src/geniefy_app/providers.py` + `tests/test_providers.py` and notes `(supersedes U85 providers.py, U77 test_providers.py)`. U85 (line 111) carries `src/geniefy_app/providers.py` … `(superseded by U98)`. U77 (line 103) carries `tests/test_providers.py (superseded by U98)`. All supersession markers consistent.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 0 · UNVERIFIED: 0
Verdict: PASS   (HIGH ⇒ FAIL; MEDIUM/LOW-only ⇒ PASS-FINDINGS; clean ⇒ PASS)
