# Audit report — content-claim / caption-fidelity — 2026-06-23

## Target audited
File: `docs/SCREENSHOTS.md`
Assets: `docs/assets/{u97-usercard-header, u92-review-profileviz, judge-why-check, judge-radar-present, u96-column-delight, u89-live-gold-arpdau-applied}.png`
README section: `README.md` "Screenshots"
Unit: U141

## Oracle
- LEDGER U141 row + 2026-06-23 recovery-log entry
- The 6 PNGs under `docs/assets/` (opened with Read)
- `README.md`, `docs/ARCHITECTURE.md`
- `.gotm/DECISIONS.md` D22/D23 (honesty/explainability), D49 (geniefy-lite rebrand; "Document"→"Table")

## Findings

### HIGH severity
- None.

### MEDIUM severity
- None.

### LOW severity
- **README intro/SCREENSHOTS overlap is fine but the SCREENSHOTS intro links to "the `DEPLOY_VERIFY*` docs in this folder" generically** (`docs/SCREENSHOTS.md:6-7`): this is a prose pointer, not a markdown link, so nothing is broken — but a reader cannot click through. Four matching files exist (`DEPLOY_VERIFY.md`, `_R2`, `_R3-005`, `_R3-006`), so the reference resolves by inspection. Cosmetic only; fix if convenient.

### UNVERIFIED
- None. All six PNGs were opened and inspected (the high-res `u96` and the zoomed-out `judge-radar-present` were additionally cropped + upscaled to confirm fine detail).

## Check-by-check

**1. Existence / image integrity — PASS.**
`docs/SCREENSHOTS.md` exists (66 lines). It embeds exactly **6** markdown images, all in `![alt](assets/<name>.png)` form. All 6 referenced files exist under `docs/assets/` (verified on disk); 0 broken paths, 0 orphaned PNGs (the asset dir contains exactly these 6 PNGs). `README.md:32-36` has a "## Screenshots" section linking `[`docs/SCREENSHOTS.md`](docs/SCREENSHOTS.md)`, and that target exists. The one non-image relative link (`ARCHITECTURE.md`) resolves from `docs/`.

**2. Spec match — PASS.**
The 6 sections cover the claimed agent flow in order: (1) point at a table, (2) review w/ confidence + profile, (3) clarifying questions w/ suggested answers, (4) Judge-radar explainability, (5) column data-fingerprint viz, (6) applied / AI-ready. The honesty note (`docs/SCREENSHOTS.md:9-11`) is present and **accurate**: the captured header reads "geniefy" (not "geniefy-lite") and the first nav tab reads "Document" (not "Table") — both confirmed against every screenshot that shows the header (`u97`, `u92`, `u89` all render "geniefy" + "Document"/"History"/"Library"). This matches D49's rename (Document→Table) being post-screenshot.

**3. Caption fidelity (load-bearing) — PASS.** Each caption faithfully describes the image; no invented UI, no unsupported claims:
- `u97-usercard-header.png` — shows the "geniefy" header, tagline "Turn a Unity Catalog table into an AI/Genie-ready asset.", a "Table to document" card with a `catalog.schema.table` input + "Document" button, the signed-in identity card (R avatar + `rohit.dashora@databricks.com`), and Document/History/Library nav. Caption = faithful.
- `u92-review-profileviz.png` — shows the readiness ring (0% AI-ready), an "agent needs your input (1)" panel, per-draft confidence chips (91% / 85% / 94% / 60% **needs input**), per-column profile stats (distinct / null / min–max e.g. `1 … 6000000`, `1998-01-01 … 1998-08-02`), and Approve/Reject/Edit/Regenerate/Why? actions. Caption = faithful. (Caption says table is `samples.tpch.orders`; image header/columns `o_orderkey`/`o_orderdate`/`o_orderstatus` corroborate.)
- `judge-why-check.png` — shows "The agent needs your input (8)" with clarifying questions per column (enum-with-no-decode, key-whose-lineage-doesn't-resolve, low-confidence), each with a **pre-filled suggested answer** in the input field (e.g. "event identifier", "type of event", "timestamp when event happened") and a "Submit answers" button. Caption = faithful.
- `judge-radar-present.png` — after crop+2x: shows an expanded explainability view with an **Evidence** line, a **radar/spider chart** with axes grounding / specificity / completeness / template_conformance, and a per-subscore **reasoning** list below it. Matches "Judge radar of rubric subscores plus the reasoning." Caption = faithful.
- `u96-column-delight.png` — shows per-column cards with type pills (`event_id · string`), null meters ("0% null"), distinct counts ("766,104 distinct"), uniqueness ("110% unique"), horizontal **MiniBars** value-distribution charts on categorical columns, and min–max range bars on date/timestamp columns; data-type + tag pills present. Matches the "data fingerprint" caption. Caption = faithful.
- `u89-live-gold-arpdau-applied.png` — shows readiness ring at **100% AI-ready**, table comment + every column comment chipped with confidence and marked **applied** ("applied to UC"), on a gold table (`rd_classic_catalog.gaming_data.gold_arpdau`, columns event_date/country/dau/revenue_usd/arpdau). Matches "100% AI-ready, comments applied to a gold table." Caption = faithful.

**4. Internal consistency — PASS.**
No contradiction with README/ARCHITECTURE. SCREENSHOTS frames writes as "on-behalf-of you, honoring your grants" (§1, §6) and apply as human-approved ("Approve what you trust and Apply", §6) — matching `ARCHITECTURE.md:15,23,30` (governed, human-approved, on-behalf-of, never the service principal) and README's "apply is always human-in-the-loop." Links are relative and resolve from `docs/`.

**5. Decision fidelity (D22/D23) — PASS.**
Honors the honesty guardrails: confidence chips are shown truthfully including a low one flagged "needs input" rather than guessed (§2); the Judge radar is presented as explainability, not a black box (§4); and apply is consistently described as **human-approved** ("Approve … and Apply"), never auto-applied — the tour never claims the agent writes to UC without human sign-off.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 1 · UNVERIFIED: 0
Verdict: PASS-FINDINGS   (LOW-only ⇒ PASS-FINDINGS)
