# Audit report — content-claim + behavior (R3 hands-off core) — 2026-06-13

## Target audited
Files:
- `src/geniefy_core/state.py` — `SessionMode.HANDS_OFF`
- `src/geniefy_core/gate.py` — `Gate.apply` (interactive + hands_off question-producing)
- `src/geniefy_core/orchestrator.py` — `_gate` (suggested answers + `needs_input` for interactive + hands_off)
- `migrations/003_session_mode_hands_off.sql` — `ALTER TYPE … ADD VALUE IF NOT EXISTS 'hands_off'`
- `tests/test_orchestrator.py`, `tests/test_state.py` — hands-off tests + enum-parity fix

Unit: U108
Oracle: `docs/design/LLD-amend-005.md` §3; `.gotm/DECISIONS.md` → D51 + Refinement (M1/M2), D17, D50, D7/D48; `migrations/001_init.sql`; `migrations/run_migrations.py`

## Findings

### HIGH severity
- None.

### MEDIUM severity
- None.

### LOW severity
- **No dedicated hands_off case in `tests/test_gate.py`.** The Gate-level tests cover `INTERACTIVE` (produces Questions, sets `state.open_questions`, marks `needs_input`) and `BATCH` (marks `low_confidence`, asks nothing) but not `HANDS_OFF` at the Gate layer (`tests/test_gate.py:137-160`). This is low-risk because `gate.py:109` routes hands_off via the same `mode in (INTERACTIVE, HANDS_OFF)` tuple as interactive (so hands_off is behaviorally identical to the tested interactive path), and the hands_off behavior IS exercised end-to-end in `tests/test_orchestrator.py:188-207`. A one-line `HANDS_OFF` parametrization of the existing interactive Gate test would close the gap. Fix if convenient.
- **Oracle prose still says `RunMode`.** `docs/design/LLD-amend-005.md:46,143` literally name `RunMode.HANDS_OFF`. This is the *design* doc (U101's frozen output), and the D51 **Refinement** explicitly overrode it ("extend `SessionMode` … not a new `RunMode`"). The code correctly follows the Refinement; the stale doc prose is a known, governed delta, not a code defect. No action needed on the code; noted for traceability.

### UNVERIFIED
- **Live migration apply.** `003_session_mode_hands_off.sql` was not applied against a live Lakebase instance (explicitly OUT OF SCOPE per the unit; deferred to U113). The transaction-safety reasoning was verified statically (see below), but the actual `ALTER TYPE … ADD VALUE` execution on PG was not run.

## Detailed check results

1. **`SessionMode.HANDS_OFF`** — added with value `"hands_off"` (`state.py:33`), matching the migration enum label. Round-trips via `RunConfig.from_dict`/`to_dict` (`state.py:142,165`) using `SessionMode(...)`; parity test `test_runconfig_round_trip_incl_token_fields` plus the new enum-parity case cover it. **M1 satisfied:** `grep -rn "RunMode" src/ tests/ migrations/` returns nothing — `RunMode` appears ONLY in the design doc prose, never in code. **M2 satisfied:** migration adds the `hands_off` `session_mode` label.

2. **Gate** — `gate.py:109` `asks_questions = self.config.mode in (SessionMode.INTERACTIVE, SessionMode.HANDS_OFF)`. For both, a routed-low draft becomes `NEEDS_INPUT` + a `Question` (`gate.py:126-135`); `state.open_questions = questions` (`gate.py:140`). Batch alone falls to the `else` → `LOW_CONFIDENCE` + reason note, and `open_questions = []`. No other mode-branch in the module compares `== INTERACTIVE` while omitting hands_off (verified by grep of all `.mode`/`INTERACTIVE`/`HANDS_OFF` sites).

3. **Orchestrator `_gate`** — `orchestrator.py:146` `produces_questions = mode in (INTERACTIVE, HANDS_OFF)`. When questions exist it best-effort calls `reasoner.suggest_answers` inside `try/except pass` (D50, never blocks; `orchestrator.py:148-156`), sets `phase = AWAITING_INPUT`, returns `needs_input`; otherwise `READY_FOR_REVIEW` (`orchestrator.py:159-160`). Terminal states match the oracle table (`ready_for_review` | `awaiting_input`). **No apply path:** the only `apply` token in the file is `self.gate.apply(...)` (Gate routing, not a UC write) plus a comment; the real `Applier` lives in `src/geniefy_app/apply.py`. Orchestrator does not apply in any mode (D7/D48 honored).

4. **Resume reuse (D51/D17)** — no new resume engine: `resume()` is unchanged (`orchestrator.py:103-110`) and `test_hands_off_deferred_resume_reuses_the_existing_loop` (`test_orchestrator.py:200-207`) drives run(hands_off) → answer → resume → `ready_for_review` with `x` re-drafted to `DRAFT`. Real assertions, not a trivial pass.

5. **Migration 003** — `geniefy.`-qualified, `set search_path to geniefy;`, `alter type geniefy.session_mode add value if not exists 'hands_off';`. Idempotent (`IF NOT EXISTS`). Adds the label only (does not USE it in the same statement/txn) → safe under `run_migrations.py`'s per-file `cur.execute(file)` + `conn.commit()` on PG12+ (the runner does NOT set autocommit for migration files, so this txn-safety property is load-bearing and correctly relied upon; the migration's header comment documents exactly this). Does **not** touch grants (`grep grant|revoke|owner` → none). Numbered `003`; `ls migrations/` shows `001, 002 (U102 library_status), 003` — **no `004` exists yet**, no collision with U109's renumbered `004` schema_runs (and `schema_runs` appears in no migration yet, consistent with U109 being unbuilt).

6. **Parity guard (`test_state.py`)** — `_schema_enum` (`test_state.py:40-55`) now unions the base `create type … as enum(…)` with every `alter type … add value '…'` across all `migrations/[0-9]*.sql`. Verified the regex actually matches 003 (standalone re-run of the regex returns `['hands_off']`; union for `session_mode` = `{interactive, batch, hands_off}` == the Python enum) — **not a false pass**. The `(SessionMode, "session_mode")` parametrize case is present (`test_state.py:62`); the other three parity cases (session_status/draft_status/apply_status) and `draft_kind` all still pass.

7. **Hermeticity** — `state.py`/`gate.py`/`orchestrator.py` import only `__future__`, `dataclasses`, `enum`, `typing`, `contextlib`, and intra-package `geniefy_core.*`. `grep psycopg2|databricks|fastapi|requests|geniefy_app|httpx|sqlalchemy` → none. Hermetic (D1).

8. **Tests run:**
   - `tests/test_gate.py tests/test_orchestrator.py tests/test_state.py` → **65 passed**.
   - FULL `pytest -q` → **344 passed, 1 skipped** (matches the expected ~344/1).
   New hands-off tests assert real behavior: produce + persist questions (`test_orchestrator.py:188-197`, incl. the `state.open_questions` persistence assertion), batch contrast (`test_orchestrator.py:177-182`), resume→ready (`test_orchestrator.py:200-207`).

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 2 · UNVERIFIED: 1
Verdict: PASS-FINDINGS   (no HIGH/MEDIUM; 2 LOW cosmetic + 1 UNVERIFIED = deliberately-deferred live migration apply, U113)
