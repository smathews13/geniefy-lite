# Audit report — code (module + tests) — 2026-06-12

## Target audited
File: `src/geniefy_core/template.py`
File: `src/geniefy_core/templates/default.yaml`
File: `tests/test_template.py` (the U26 tests — assessed and run)
Unit: **U25** (Template module) — and **U26** (its tests), assessed jointly per the dispatch.

## Oracle
- `docs/design/HLD.md` §7 (comment-template spec)
- `docs/design/LLD-agent-core.md` U4 §3.1 (Template responsibilities), §3.5 (Judge rubric dimensions)
- `docs/design/LLD-data-layer.md` U2 §4.1 (`templates.spec` is `jsonb`)
- `.gotm/DECISIONS.md` D12 (no hardcoded infra)

## What was checked + how

**Code-specific runs (all executed):**
- Import smoke test — `PYTHONPATH=src python3 -c "import geniefy_core.template as t; print(t.default_template().name)"` → prints `default`. PASS.
- **Lazy-yaml — confirmed two ways.** (a) Source: the only `import yaml` is at `template.py:178`, *inside* `from_yaml`; no module-level yaml import. (b) Behavioral: importing the module with `yaml` blocked at `__import__` still succeeds, `from_dict` still works, and `from_yaml` degrades to a `TemplateError` (not a raw `ModuleNotFoundError`). PASS.
- **Tests — `PYTHONPATH=src /tmp/geniefy_pgvenv/bin/python -m pytest tests/test_template.py -q` → `23 passed in 0.13s`.** PASS.
- **Tests are meaningful (mutation-tested in a scratch copy; target untouched):** disabling the weight-sum check → 1 fail; making applicability ignore signals → 3 fails; disabling the `definition`-required check → 1 fail; dropping `rule` from `to_dict` round-trip → 1 fail. All four mutations caught. The suite is not vacuous.

**Spec match (HLD §7) — verified against the loaded default template:**
- table required `(purpose, grain, primary_keys, join_keys)` — exact match.
- table recommended `(use_cases, update_cadence, source_systems, caveats, sensitivity)` — exact match.
- column required includes `definition`; enforced (`from_dict` raises if absent).
- all six conditionals present with correct `when`: units→numeric_measure, allowed_values→enum, null_meaning→nullable, fk_reference→fk, derivation→computed, sensitivity→pii.
- styles: table `max_words=120` + voice; column `max_words=40` + forbid — match.
- rubric dimensions `{completeness, specificity, grounding, template_conformance}` with weights `0.30/0.30/0.25/0.15`, sum = 1.0 (validated to `_WEIGHT_TOLERANCE`). Matches U4 §3.5.
- `applicable_column_fields(signals)` resolves conditionals correctly (verified by tests + inspection): required field always, plus each conditional whose `when` is truthy, plus `when="always"`.

**Cross-reference integrity:** HLD §7 (line 159), U4 §3.1 (line 52), §3.4 Reasoner (line 72), §3.5 Judge (line 78), U2 `templates.spec jsonb` (data-layer line 66) all resolve and say what the docstrings claim.

**Internal consistency:** dataclasses coherent; `to_dict()`→`from_dict()`→`to_dict()` is stable (verified equal); validation messages match what they check; `default.yaml` parses and `from_yaml` equals `default_template()` (test `test_from_yaml_matches_default`, and `to_dict` equality verified directly).

**Decision fidelity:** JSONB-first storage honored — `from_dict` is the dependency-free primary path mapping to U2 `templates.spec`; YAML is authoring-only with lazy PyYAML. No hardcoded infra (D12) — pure in-memory spec model, no workspace/catalog/endpoint literals.

## Findings

### HIGH severity
- (none)

### MEDIUM severity
- (none)

### LOW severity
- **`default.yaml` adds `forbid` to `table_comment.style`, not shown in HLD §7.** HLD §7's table style is `{ max_words: 120, voice: ... }` with no `forbid`; `default.yaml:13` adds `forbid: ["marketing language", "speculation without signal"]`. This is an additive tightening consistent with the spec's intent ("factual ... no marketing"), not a contradiction. No fix required; optional to note in HLD if it should be canonical.
- **Rubric dimension named `grounding` vs U4 §3.5's "grounding / no-hallucination".** The module/yaml use the short key `grounding`; U4 §3.5 phrases the dimension "grounding / no-hallucination (every claim traces to provided evidence)". The `desc` field carries the no-hallucination meaning (`"every claim traces to provided evidence; no hallucination"`), so semantics match — only the key is abbreviated. Downstream Judge (U4 §3.5) must key off `grounding`; harmless as long as it reads the rubric from the Template (which it is parameterized to do).

### UNVERIFIED
- (none)

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 2 · UNVERIFIED: 0
Verdict: **PASS-FINDINGS**   (HIGH ⇒ FAIL; MEDIUM/LOW-only ⇒ PASS-FINDINGS; clean ⇒ PASS)

**U26 stamp note:** the U26 tests **pass (23/23)** and are **meaningful** (4/4 mutations caught). Both U25 and U26 are consumable. The two LOW findings are cosmetic/documentation-level and do not block the Reasoner/Judge from consuming the Template.
