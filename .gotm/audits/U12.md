# Audit report — code/spec (SQL migration) — 2026-06-11

## Target audited
File: `migrations/001_init.sql`
Unit: U12 — Build: Lakebase migrations (schema from U2 + amendments U10/U11)

Oracle: `docs/design/LLD-data-layer.md` (U2) · `docs/design/LLD-amend-001.md` (U10) · `docs/design/LLD-amend-002.md` (U11) · `.gotm/DECISIONS.md` (D5/D6/D7/D17/D21)

Audit kind: code/spec — existence · spec-match · cross-reference integrity · internal consistency · decision fidelity, plus SQL-specific checks (idempotency · Postgres syntax · pgcrypto · search_path · injection/escaping).

---

## Findings

### HIGH severity
None.

### MEDIUM severity
None.

### LOW severity

- **`audit_log` "append-only" is documented, not enforced** — `audit_log` is labeled append-only (`migrations/001_init.sql:200`) and is correctly excluded from the `updated_at` trigger set (`:220`), but the DDL adds no DB-level guard against UPDATE/DELETE (no `BEFORE UPDATE/DELETE` reject-trigger, no `REVOKE`). This faithfully matches U2 §4.8 ("Append-only. Never updated/deleted." — stated as a convention, not enforced) and D7/D21 (which require `before`/`after` jsonb on each apply event — both present at `:208`–`:209` — but do not mandate DB-level immutability), so it is *not* a spec deviation. It is a defense-in-depth opportunity only: the append-only invariant rests on application discipline. Fix if convenient.

- **`templates` immutability-per-version is by convention only** — U2 §4.1 says templates are "immutable per version"; the DDL enforces version uniqueness via `unique (name, version)` (`:84`) but does not block UPDATE of an existing version row, and `templates` is (correctly, per U2 §8) absent from the `updated_at` trigger set. Matches the spec's level of enforcement (a `unique` constraint, no row-freeze). Cosmetic/optional hardening only.

### UNVERIFIED

- **Migration was not executed against a live Postgres/Lakebase instance.** Per D27, code authoring proceeds infra-free; deploy/test against an FE-VM workspace + Lakebase is a later human provisioning step (deploy unit, D28). This audit is a static read of the DDL: syntax, structure, idempotency guards, and spec-conformance were verified by inspection, but the script was not run, so runtime success (e.g., `plpgsql` block execution, trigger creation, `gen_random_uuid()` availability under the bound role) is asserted from the text, not observed. Flagged for the deploy unit, not blocking the code audit.

---

## Audit detail (checklist trace)

**1. Existence** — PASS. Output exists at the ledger's stated path `migrations/001_init.sql` (12 KB).

**2. Spec match** — PASS. Every U2 entity, as amended by U10/U11, is present:
- **Enums (U2 §5, amended):** `session_mode` (`:15`), `session_status` (`:21`–`:23`, **unchanged** per U10 F1 — 11 values verbatim), `draft_status` (`:28`–`:30`) **includes `low_confidence` and `error`** (U10 F2) → 9 values, `apply_status` (`:37`–`:38`) present with the full U11 vocabulary `not_applied · applied · conflict · failed · unsupported · skipped_noop`, plus `message_role` (`:42`), `provider_kind` (`:46`–`:47`), `library_scope` (`:51`), `draft_kind` (`:55`), `audit_action` (`:59`–`:60`). All match U2 §5.
- **Tables (U2 §4):** all 8 present — `templates` (`:75`), `context_providers` (`:88`), `sessions` (`:99`), `session_messages` (`:121`), `table_drafts` (`:133`), `column_drafts` (`:156`), `comment_library` (`:183`), `audit_log` (`:201`). Columns match U2 column-for-column.
- **U10 F5 — `sessions.session_state jsonb`:** present (`:108`).
- **U10 F1 — watchdog index `sessions (status, updated_at)`:** present (`:118`).
- **U10 F2 / U11 — `apply_status` column on both draft tables:** `table_drafts.apply_status` (`:145`) and `column_drafts.apply_status` (`:170`), both `not null default 'not_applied'`.
- **D21 — per-item `applied_comment / applied_at / applied_by`:** present on `table_drafts` (`:147`–`:149`) and `column_drafts` (`:172`–`:174`).
- **D7/D21 — `audit_log.before` / `after` jsonb:** present (`:208`–`:209`).
- **D17 — SessionState persistence surface:** `sessions` (+ `session_state` snapshot) / `table_drafts` / `column_drafts` / `session_messages` all present; normalized rows + the F5 fast-path blob coexist as U10 F5 requires.
- **Indexes (U2 §4):** all present — `sessions (created_by, status)` (`:115`), `sessions (uc_catalog, uc_schema, uc_table)` (`:116`), `column_drafts (session_id)` (`:179`), `column_drafts (session_id, status)` (`:180`), `comment_library (scope, match_key)` (`:198`), `audit_log (session_id, created_at)` (`:212`).
- Nothing material missing; nothing extra/contradictory. The one extra index (watchdog) is mandated by U10 F1.

**3. Cross-reference integrity** — PASS. Every cited tag traces to the oracle and says what the comment claims:
- `:18`–`:19` "U10 F1 … IN_FLIGHT subset {profiling, gathering_context, reasoning, applying}" ↔ LLD-amend-001 F1 (verbatim set). ✓
- `:26` "U10 F2: += low_confidence, error" ↔ F2. ✓
- `:35` "U10 F2 / U11 NF1+NF2", `skipped_noop = proposed equals live (U11 NF2)`, `not_applied` initial = U6 prose "pending" ↔ F2 + NF1/NF2. ✓
- `:87` "U10 F4 — context retrieval only, NOT profiling" ↔ F4. ✓
- `:107` "RunConfig incl. profiling_provider (U11 NF3)" ↔ NF3 (newly introduced field). ✓
- `:108` "rehydration fast-path snapshot (U10 F5)" ↔ F5. ✓
- `:117` "watchdog support (U10 F1): … via (status, updated_at)" ↔ F1 (updated_at timeout). ✓
- `:230`–`:235` "built-in context providers (D5)" → seeds `uc_lineage` (`builtin_lineage`) + `query_history` (`builtin_query_history`), both `enabled` ↔ D5 + U2 §8 seeding. ✓

**4. Internal consistency** — PASS. No contradictions:
- All FK targets exist: `sessions.template_id → templates(id)` (`:106`); `session_messages.session_id`, `table_drafts.session_id`, `column_drafts.session_id`, `comment_library.source_session_id`, `audit_log.session_id` all → `sessions(id)`; `column_drafts.table_draft_id → table_drafts(id)`. `audit_log.draft_id` is an intentionally unconstrained uuid (loose ref, matching U2 §4.8 "loose ref by design").
- Every enum referenced by a column is defined above it (`session_mode`/`session_status`/`draft_status`/`apply_status`/`message_role`/`provider_kind`/`library_scope`/`draft_kind`/`audit_action`).
- Unique constraints coherent: `templates (name, version)`, `context_providers.name`, `session_messages (session_id, seq)`, `table_drafts (session_id)` (one table_draft per session, U2 §4.5), `column_drafts (session_id, column_name)`.
- The `updated_at` trigger (`:217`–`:227`) iterates exactly the 5 tables U2 §8 names — `context_providers, sessions, table_drafts, column_drafts, comment_library` — all of which carry an `updated_at` column; `templates`/`session_messages`/`audit_log` correctly excluded. The `format(... %I ...)` identifier interpolation is on a fixed literal array, so no injection surface.

**5. Decision fidelity** — PASS.
- **D6 (Lakebase/Postgres):** Postgres DDL, schema `geniefy`, `timestamptz`, jsonb, Postgres enums. ✓
- **D5 (built-in + MCP context providers):** `provider_kind` enum carries `builtin_lineage | builtin_query_history | mcp`; both built-ins seeded `enabled=true` (`:232`–`:235`). ✓
- **D7/D21 (diff-first / conflict-aware / per-item idempotent + audit before/after):** current (`current_comment`) vs proposed (`proposed_comment`) vs applied (`applied_comment`) columns on both draft tables; `apply_status` carries `conflict` (live changed since profiling) and `skipped_noop` (no-op idempotency); per-item `applied_*` checkpoint columns; `audit_log.before`/`after`. ✓
- **D17 (serializable SessionState ↔ sessions/*_drafts/session_messages):** persistence surface present incl. the F5 rehydration blob. ✓

**SQL-specific** — PASS.
- **Idempotency / safe re-run:** `create extension if not exists pgcrypto` (`:7`); `create schema if not exists` (`:8`); enums guarded by `do $$ … exception when duplicate_object then null; end $$` blocks; tables/indexes `if not exists`; trigger `drop trigger if exists … ; create trigger …`; seeds `on conflict (…) do nothing` (`:235`, `:241`). Fully re-runnable.
- **Postgres syntax:** `plpgsql` function + anonymous `do` blocks well-formed; `create or replace function … set_updated_at()` (`:66`–`:68`) correct.
- **pgcrypto / `gen_random_uuid()`:** extension created before first use; every PK defaults to `gen_random_uuid()`.
- **search_path:** `set search_path to geniefy` (`:9`); all objects also fully schema-qualified (`geniefy.*`), so DDL is robust regardless of session search_path.
- **Injection/escaping:** no string-interpolated user data in DDL; the only dynamic SQL (`execute format(...)` for triggers) interpolates `%I` over a fixed literal table-name array — no untrusted input.

---

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 2 · UNVERIFIED: 1
Verdict: PASS-FINDINGS   (HIGH ⇒ FAIL; MEDIUM/LOW-only ⇒ PASS-FINDINGS; clean ⇒ PASS)

The migration faithfully implements the U2 data-layer LLD as amended by U10 (F1/F2/F4/F5) and U11 (NF1/NF2/NF3), and honors D5/D6/D7/D17/D21. Every promised table, column, enum, index, and constraint is present; all U#/D#/F# citations trace correctly; the DDL is internally consistent and idempotent. The two LOW findings are optional defense-in-depth hardening (DB-level enforcement of the `audit_log` append-only / `templates` per-version immutability conventions) that match the spec's own (convention-level) bar and do not block consumption. The single UNVERIFIED item is the un-run migration, consistent with D27's infra-free authoring — to be exercised at the deploy unit (D28). U12 is consumable by downstream build units (U14).
