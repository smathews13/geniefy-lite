# Audit report — fix-correctness + test-bite + run (resume-liveness / per-phase-save / cosmetics) — 2026-06-12

## Target audited
Files:
- `src/geniefy_app/api.py` — `SessionService.submit_answers` (now persists an in-flight `Phase.REASONING` before the background resume); removed `# pragma: no cover` on the `/apply` delegation; updated apply docstring (lines ~16-18).
- `tests/test_api.py` — `FakeStore.saved_statuses`; tightened `test_run_persists_progressively`; new `test_submit_answers_marks_in_flight_before_resume`.
Unit: **U64** — Fix: resume-liveness (U59/U60 audit MED) + per-phase-save test tightening (U52 audit MED) + stale pragma/docstring (U54 audit LOW)

## Oracle
- `.gotm/audits/U59-U60.md` (MED: after `/answers`, the session sat in `awaiting_input` — not in-flight — so the frontend poll loop stopped and the UI didn't advance through resume).
- `.gotm/audits/U52.md` (MED: `test_run_persists_progressively` asserted only `>= 2` saves — too loose to catch a broken per-phase save).
- `.gotm/DECISIONS.md` D18 (async run + poll), D36 (hermetic split).
- `_IN_FLIGHT` in `api.py`; frontend `IN_FLIGHT` poll set in `app/frontend/src/api/hooks.ts`; `Phase`/`SessionStatus` rollup in `src/geniefy_core/state.py`.

## Method
Read target + oracle + the phase→status rollup in `state.py`. Verified the in-flight set membership on both sides of the contract. Ran the suite, then mutation-tested both new/tightened tests in an isolated copy (`/tmp/u64_mut`, since source is frozen) to prove they bite. Confirmed `api.py` imports SDK-free.

## Findings

### HIGH severity
- none.

### MEDIUM severity
- none.

### LOW severity
- none.

### UNVERIFIED
- **Frontend behavior is verified by contract, not by browser.** The fix is backend-only and relies on the *existing* frontend poll gate (`hooks.ts:40-43`: `refetchInterval` polls while `status ∈ IN_FLIGHT`, stops otherwise). I confirmed by static read that `reasoning ∈ IN_FLIGHT` and `awaiting_input ∉ IN_FLIGHT` on the frontend (`hooks.ts:15-17`), and `reasoning ∈ _IN_FLIGHT` on the backend (`api.py:62`). The full React re-poll → re-render loop was not exercised in a live browser (out of scope for a hermetic backend audit; the same UNVERIFIED note stands as in U59-U60). The contract-level reasoning is sound: with the session now reading `reasoning` immediately after `submit_answers`, `useSession`'s `refetchInterval` returns `POLL_MS` instead of `false`, so polling stays alive until `execute_resume` flips the status to `ready_for_review` (terminal, `∉ IN_FLIGHT`) and the loop stops — no frontend change required.

## Check-by-check

1. **Fix is correct + sufficient — YES.** `submit_answers` (`api.py:104-108`) sets `state.phase = Phase.REASONING` and calls `self.store.save(...)` *before* the route schedules `execute_resume` (the bg task is added in the route at `api.py:227`, strictly after `service.submit_answers` returns at `api.py:224`). `Phase.REASONING.to_session_status()` returns `SessionStatus.REASONING` = `"reasoning"` (`state.py:59,70-75`). `reasoning ∈ _IN_FLIGHT` (`api.py:62`) AND `reasoning ∈` frontend `IN_FLIGHT` (`hooks.ts:16`), and `awaiting_input ∉` frontend `IN_FLIGHT` (`hooks.ts:15-17`). So a polling client transitions from "stopped" (`awaiting_input`) to "polling" (`reasoning`) with zero frontend change, then stops again on the terminal `ready_for_review` flip in `execute_resume` (`api.py:117`). The fix genuinely revives the poll loop. **409 guard still fires first:** the in-flight guard (`api.py:102-103`) precedes the new save (`api.py:104-108`), so a session already in `_IN_FLIGHT` raises 409 *before* any second in-flight write — no double-resume. Verified live by `test_answers_rejected_while_in_flight_409` (passes).

2. **New test bites — YES.** `test_submit_answers_marks_in_flight_before_resume` (`test_api.py:158-167`) calls `service.submit_answers(...)` **directly** (not via the `/answers` route, whose bg task TestClient would run synchronously and mask the intermediate state — the test's comment states exactly this), then asserts the session reads `"reasoning"`. Because `submit_answers` itself never invokes `execute_resume`, the asserted state is genuinely the post-submit / pre-resume state. Mutation test: removing the fix block (the `Phase.REASONING` + save) makes the test FAIL with `assert 'awaiting_input' == 'reasoning'` — confirmed by running the reverted copy (1 failed, 14 passed). The test exercises the pre-resume state correctly.

3. **Progressive test tightened — YES, meaningfully.** `test_run_persists_progressively` (`test_api.py:124-132`) now asserts `"reasoning" in store.saved_statuses` (a mid-run partial persisted DURING the run) and `saved_statuses[-1] == "ready_for_review"` (terminal) and `save_calls >= 3` — not just the old `>= 2`. Mutation test: no-op'ing the `PersistingTracer._save` (simulating a broken per-phase save) makes it FAIL with `assert 'reasoning' in ['created', 'ready_for_review']` — confirmed by running the mutated copy. Under the old `>= 2` assertion that same regression would have PASSED (start_run + final = 2 saves), so the U52 MED is genuinely closed. `FakeStore.saved_statuses` (`test_api.py:37,45`) records the status at each save, which is what makes the assertion possible.

4. **Cosmetics — closed.** `# pragma: no cover` is gone from the `/apply` delegation (grep over `api.py`: zero `pragma: no cover` occurrences). Removal is justified — the delegation branch is genuinely exercised by `tests/test_apply_wiring.py` (FakeApplier asserts the `(sid, actor)` call; passes). The apply docstring (`api.py:16-18`) now reads "wired by `build_service` in U54; 501 only if no applier is set" — it no longer says "501 until that unit lands". Both LOW cosmetics resolved.

5. **RUN.** `PYTHONPATH=src /tmp/geniefy_pgvenv/bin/python -m pytest tests/test_api.py -q` → **15 passed** (was 14; the +1 is the new in-flight test). `PYTHONPATH=src python3 -c "import geniefy_app.api; print('ok')"` → **ok** (api.py still SDK-free; infra imports remain lazy inside `build_service`/`_lakebase_connection`).

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 0 · UNVERIFIED: 1
Verdict: **PASS**   (no findings above the trivial bar; the lone UNVERIFIED is the pre-existing browser-loop note inherited from U59-U60, not a U64 defect — the backend contract is verified)
pytest: tests/test_api.py → 15 passed; api.py import → SDK-free (ok)
Resume-liveness MED genuinely closed: **y** · New test bites: **y**
