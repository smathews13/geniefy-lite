# Audit report — spec-match + correctness + build — 2026-06-12

## Target audited
Files:
- `/Users/rohit.dashora/fe-vibe/geniefy-v3/app/frontend/src/App.tsx` (run→status routing)
- `/Users/rohit.dashora/fe-vibe/geniefy-v3/app/frontend/src/screens/RunView.tsx`
- (context) `/Users/rohit.dashora/fe-vibe/geniefy-v3/app/frontend/src/screens/ResultsView.tsx` — superseded orphan (see note)
Unit: U59 — frontend run lifecycle + "watch it think" run view (input→run→poll→phase narration→drafts)

Oracle:
- `docs/design/LLD-ux-delight.md` (U9 §4 "watch it think"; Principle 5 honesty)
- `.gotm/DECISIONS.md` D18 (async run + DB-backed status + frontend poll)
- `app/frontend/src/api/hooks.ts` + `types.ts` (useRun/useSession; poll predicate)
- `migrations/001_init.sql` (`session_status` enum — ground truth for statuses)

## Findings

### HIGH severity
- none.

### MEDIUM severity
- none.

### LOW severity
- **Two terminal statuses route to nothing.** `App.tsx:8-9` defines `IN_FLIGHT = [created, profiling, gathering_context, reasoning, applying]` and `DONE = [awaiting_input, ready_for_review, applied]`, plus a dedicated `failed` branch (`App.tsx:83`). The `session_status` enum (`migrations/001_init.sql:21-23`) also includes `paused` and `cancelled`, which match none of the three branches — for those states `<main>` renders empty (no view, no message). These are off-happy-path edge states (watchdog/D17, user-cancel) outside U59's core input→run→review lifecycle, so this is cosmetic for the demo path, but a future "Run was paused/cancelled — start a new run" branch would close the gap. Suggested follow-on unit: add `paused`/`cancelled` fallback messaging to App routing.
- **Bundle size warning (known/tracked).** `vite build` emits the ~570 kB (`569.95 kB`, gzip 165 kB) single-chunk warning (recharts/visx). Per the audit brief this is the known LOW tracked as **U70** — flagged, not failed.

### UNVERIFIED
- **Live "watch it think" browser behavior.** Per the audit brief, the live browser exercise (column placeholders populating live, phase narration advancing as the backend polls) is a separate web-devloop step tracked under **U70**; not exercised here. Static read confirms the wiring is correct (see spec-match below).

## Detail — checks run

**1. Spec match (U9 §4 + D18) — PASS.**
- input→run→poll→narration→drafts chain is intact and honest:
  - Input gating: `App.tsx:10,18` validate `catalog.schema.table` via `TABLE_RE` before enabling Document.
  - run: `useRun()` (`hooks.ts:27-32`) POSTs `/api/run`; `start()` (`App.tsx:19-20`) sets `sessionId` from `r.session_id` on success — matches `RunResponse` (`types.ts:70-73`) and D18's "POST /run returns 202 + session id."
  - poll: `useSession(sessionId)` (`hooks.ts:35-45`) polls `GET /api/sessions/{id}` at `POLL_MS=1500`, `enabled: !!sessionId`, and stops (`refetchInterval → false`) once status leaves the in-flight set — exactly D18's "frontend polls until terminal."
  - narration: `RunView.tsx:4-10` maps each REAL orchestrator phase (`profiling`/`gathering_context`/`reasoning`/`applying`, the enum's running subset per `001_init.sql:18-19`) to honest copy, and `RunView.tsx:13` falls back to a truthful "Working…" rather than inventing progress. Columns populate live from `session.column_drafts` (`RunView.tsx:14,23-39`) as partials arrive — the U9 §4 "populate live" behavior, poll-driven, no scripted timeline.
  - App routes by status (D18): in-flight→`RunView` (`App.tsx:82`), `failed`→error card (`App.tsx:83-87`), terminal→`ReviewView` (`App.tsx:88-90`).
- Honesty (U9 §4 / Principle 5): the only animation is a single status-dot pulse (`RunView.tsx:18`) tied to real in-flight state; there is **no fabricated/animated fake progress bar or scripted percentage**. Narration idles truthfully on the generic label. Compliant.

**2. Correctness — PASS.**
- Hook signatures used correctly: `useRun()` returns a mutation; `run.mutate({table}, {onSuccess})`, `run.reset()`, `run.isPending`, `run.isError` all valid TanStack v5 surface. `useSession(string | null)` accepts the nullable id and guards with `enabled`.
- Status gating consistent: App's IN_FLIGHT mirrors hooks' IN_FLIGHT (both include `created` so the poll runs and RunView shows immediately after creation; `created` is a genuine pre-running in-flight state, so this is honest, not fabricated). DONE includes `applied` so a finished run lands on the review/glow-up view rather than disappearing.
- React hygiene: list keys present and stable — `RunView.tsx:31` keys on `c.column_name`; `ReviewView.tsx:54` keys on `c.target`. Null-guards present: `cols.length > 0` (`RunView.tsx:23`), `td &&` / `?? '—'` in the views, `sessionId &&` before passing to `ReviewView` (`App.tsx:88`). No stale-state risk: `reset()` clears `sessionId` and calls `run.reset()` together (`App.tsx:21-24`); query key `['session', sessionId]` keys cache per session.
- `types.ts` SessionStatus (11 values) matches `migrations/001_init.sql:21-23` verbatim; `SessionView` shape consumed by RunView (`target`, `column_drafts[].column_name`) matches `types.ts:53-60`.

**3. Build (this auditor owns it) — PASS.**
- `node_modules`: present.
- `npm run typecheck` (`tsc --noEmit`): **PASS**, clean, no errors.
- `npm run build` (`tsc --noEmit && vite build`): **PASS** — 1038 modules transformed, emitted `index.html` + CSS (14.6 kB) + JS (`569.95 kB`, gzip 165 kB) in ~1.1 s. Only output is the >500 kB chunk-size warning (known LOW → U70).

## Note (per brief, not a finding against U59)
`ResultsView.tsx` is a superseded read-only drafts view; `App.tsx` routes the terminal path to `ReviewView` (the interactive review + apply screen), not `ResultsView`. `ResultsView` is orphaned-but-harmless — it still typechecks/builds and is imported nowhere in App's routing. Flagged as expected by the audit brief; not failed.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 2 · UNVERIFIED: 1
Verdict: PASS-FINDINGS   (HIGH ⇒ FAIL; MEDIUM/LOW-only ⇒ PASS-FINDINGS; clean ⇒ PASS)
