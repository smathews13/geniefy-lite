# Audit report — content-claim / shell+yaml+python-correctness — 2026-06-14

## Target audited
Files:
- `migrations/run_migrations.py` (the `BRANCH` precedence, lines 32–35; `sys` import line 26)
- `databricks.yml` (`geniefy_setup` job: migrate-task `parameters`, lines 105–113; job `parameters` block, lines 120–122)
- `deploy.sh` (the `--use-job` migrate arm, lines 171–175; the local arm, lines 176–182; `DB_BRANCH` derivation, line 64)
Unit: U118

## Oracle
- `.gotm/audits/U117.md` — the MEDIUM: `--use-job` → `bundle run geniefy_setup` did not pass the branch, so `run_migrations.py` fell back to its default `production`, re-introducing the U117 wrong-branch bug on the alternate path. U117's `DB_BRANCH` derivation (prod→`production`, else target name) at `deploy.sh:64`.
- The pre-existing `run_migrations.py` consumer of `GENIEFY_LAKEBASE_BRANCH` (default `production`) and its branch→host/endpoint derivation (`_coords`, lines 49–63).
- U118 ledger row (`.gotm/LEDGER.md:134`).

## Findings

### HIGH severity
- None.

### MEDIUM severity
- None.

### LOW severity
- **Empty-string env on the local path resolves to `BRANCH=""` rather than falling through to `production`.** In `run_migrations.py:34-35`, when `argv[1]` is absent and `GENIEFY_LAKEBASE_BRANCH=""` is *present-but-empty*, `os.environ.get("GENIEFY_LAKEBASE_BRANCH", "production")` returns `""` (the default only fires on a *missing* key), so `BRANCH=""` and `_coords` would build a malformed endpoint path `projects/geniefy/branches//endpoints/primary`. This is **not reachable on any real path**: deploy.sh's local arm always sets `GENIEFY_LAKEBASE_BRANCH="$DB_BRANCH"` and `DB_BRANCH` is never empty (line 64 always yields `production` or the target name), and the `--use-job` arm always passes a non-empty `lakebase_branch`. The `argv[1]` half *is* hardened (`.strip()` collapses blank/whitespace argv to the env fallback). Cosmetic robustness gap only; no action required.

### UNVERIFIED
- **Live `bundle run geniefy_setup … --params lakebase_branch=<branch>`** — explicitly out of scope. Whether the DAB `{{job.parameters.lakebase_branch}}` placeholder is substituted into `spark_python_task.parameters` → `sys.argv[1]` at runtime, and whether `--params` is the correct CLI flag for the installed `databricks` version, is verified here only by static reading of the YAML wiring + the U111/U119 idiom (those units already use `{{job.parameters.*}}` placeholders against the same job machinery, audited PASS-FINDINGS). Not executed against a live workspace.

## Detail of checks performed

1. **`run_migrations.py` BRANCH precedence** (`run_migrations.py:34-35`). Simulated the exact expression across 9 cases. Results — `argv[1]=dev`→`dev`; `argv[1]=production`→`production`; **NO argv + env=dev (the LOCAL deploy.sh path)**→`dev` (falls through to env, the critical non-regression); NO argv + NO env→`production`; `argv[1]=''`→env (`dev`); `argv[1]='   '` (whitespace)→env (`.strip()` collapses it); `argv[1]=dev` + env=production→`dev` (argv wins); `argv[1]='  staging  '`→`staging` (trimmed). Precedence is exactly `argv[1] (non-blank) > env > production`. `import sys` present at line 26. **PASS.** (The one degenerate `env=""`→`""` case is the LOW above — unreachable.)

2. **`databricks.yml` — geniefy_setup.** YAML parses (`yaml.safe_load`, OK). The migrate task's `spark_python_task.parameters` = `["{{job.parameters.lakebase_branch}}"]` (→ becomes `sys.argv[1]`); the job declares `parameters: [{name: lakebase_branch, default: production}]`. The rest of the job is unchanged from the post-U119 migrate-only revert: `name: geniefy-setup-${bundle.target}`, single `migrate` task with `python_file: ./migrations/run_migrations.py` + `environment_key: migrate`, the `migrate` environment (`client: "2"`, `psycopg2-binary`). No `schema_run` task (correctly stays in jobs-bundle/, D54/U119). The comment (lines 109–110) accurately states the param threading. **PASS.**

3. **`deploy.sh` — `--use-job` arm** (lines 171–175). `bundle run geniefy_setup -t "$TARGET" --params "lakebase_branch=${DB_BRANCH}"` where `DB_BRANCH` is the U117 per-target value (line 64, unchanged). **PASS.**

4. **`deploy.sh` — local (non-`--use-job`) arm** (lines 176–182). Unchanged from U117: still exports `GENIEFY_LAKEBASE_BRANCH="$DB_BRANCH"` (+ project/endpoint/profile) and invokes `python3 migrations/run_migrations.py` with **no argv** → run_migrations falls through to the env branch (check 1). No regression. **PASS.**

5. **`bash -n deploy.sh`** — clean. **PASS.**

6. **`py_compile migrations/run_migrations.py`** — clean. **PASS.**

7. **End-to-end consistency.** `-t dev`: `DB_BRANCH=dev` → (`--use-job`) `--params lakebase_branch=dev` → job param → argv[1]=`dev` → migrates the `dev` branch (matches the dev app's `geniefy-db` binding, U117/NFR-D); (local) env `GENIEFY_LAKEBASE_BRANCH=dev` → migrates `dev`. `-t prod`: both arms → `production`. Both deploy paths now migrate the same branch the app binds. The U117 MEDIUM (job path defaulting to `production`) is closed. **PASS.**

8. **Full suite** `PYTHONPATH=src .venv/bin/python -m pytest -q` → **375 passed, 1 skipped** (1 deprecation warning, pre-existing). Matches the expected ~375/1. run_migrations.py has no unit tests (consistent with the spec); nothing broke. **PASS.**

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 1 · UNVERIFIED: 1
Verdict: PASS-FINDINGS   (no HIGH/MED: the branch is genuinely threaded to run_migrations on the `--use-job` path via the job parameter → argv[1]; precedence is argv>env>production and the local no-argv env path is preserved; YAML/bash/python all clean; 375 passed / 1 skipped. 1 LOW = unreachable empty-env edge; 1 UNVERIFIED = live `bundle run`, out of scope.)
